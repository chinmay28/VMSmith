# VMSmith Project Roadmap

> **Last updated:** 2026-06-01
> **Status:** Active roadmap — foundation work, auth/TLS/systemd/quotas, templates, bulk ops, host + VM metrics APIs/CLI/UI, event storage/streaming/UI, webhook delivery + Settings UX, OpenAPI tooling, and clone integration/E2E coverage are now complete; the main remaining gaps are the libvirt clone implementation, metrics soak/E2E coverage, advanced operations, and long-tail production polish.

This document outlines planned improvements, new features, and technical debt items for VMSmith. Tasks are organized into phases by theme, with rough effort estimates and dependency notes.

**Effort key:** S = small (hours), M = medium (1-2 days), L = large (3-5 days), XL = extra-large (1+ week)

---

## Phase 1: Foundation & Quality (Week 1-2)

These tasks strengthen the project's reliability, developer experience, and code quality before adding new features.

### 1.1 CI/CD Pipeline

There are currently no automated checks. This is the single highest-impact improvement.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 1.1.1 | Create GitHub Actions workflow for Go build + unit tests on every PR | S | ✅ Done — `.github/workflows/ci.yml` runs `make build-go` and `make test-unit` on Ubuntu 22.04 with Go 1.22 + `libvirt-dev` |
| 1.1.2 | Add `golangci-lint` step to CI | S | ✅ Done — `.github/workflows/ci.yml` runs `golangci-lint-action` (currently scoped to `govet`) in CI |
| 1.1.3 | Add frontend build + Playwright mock tests to CI | M | ✅ Done — `.github/workflows/ci.yml` runs a dedicated frontend job that installs Node dependencies, builds the frontend bundle, installs Chromium via Playwright, and runs `make test-web` |
| 1.1.4 | Add API integration test step (`make test-integration`) | S | ✅ Done — included in `.github/workflows/ci.yml` backend job |
| 1.1.5 | Create release workflow: build + attach `vmsmith-linux-amd64` binary on tag push | M | ✅ Done — `.github/workflows/release.yml` builds the frontend + `make dist` on `v*` tags and publishes `bin/vmsmith-linux-amd64` via GitHub Releases |
| 1.1.6 | Add branch protection rules for `main` (require CI pass, no force push) | S | GitHub repo settings |

### 1.2 Input Validation & Error Handling

Several API inputs currently pass through to libvirt without validation, producing confusing 500 errors instead of clear 400 responses.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 1.2.1 | Validate VM name: non-empty, max 64 chars, alphanumeric + hyphens only, unique | M | ✅ Done — API create validation trims names, enforces the 1-64 char alphanumeric/hyphen rule, and rejects duplicate VM names with HTTP 400 `invalid_name` before calling the manager |
| 1.2.2 | Validate CPU/RAM bounds: CPUs 1-128, RAM 128MB-1TB, Disk 1GB-10TB | S | ✅ Done — create/update validation enforces CPUs 1-128, RAM 128MB-1TB, and Disk 1GB-10TB when values are provided, while still allowing omitted create-time values to fall back to configured defaults |
| 1.2.3 | Validate port forward ranges: host/guest port 1-65535, protocol tcp/udp only | S | ✅ Done — `internal/api/validation.go` rejects out-of-range ports and non-`tcp`/`udp` protocols before any store or iptables work; covered by `internal/api/validation_test.go` and `internal/api/api_test.go` |
| 1.2.4 | Validate image upload: reject empty files, enforce `.qcow2` extension, check disk space | M | ✅ Done — upload handler rejects empty/non-`.qcow2` files with `invalid_image` and returns 507 `insufficient_storage` when free disk is too low |
| 1.2.5 | Standardize error responses: introduce error codes (`invalid_name`, `resource_not_found`, `disk_shrink_not_allowed`, etc.) | M | ✅ Done — `pkg/types/errors.go` carries typed API errors, every API error response now includes structured `code` + `message` fields, and plain handler failures use explicit codes such as `invalid_request_body`, `request_too_large`, `missing_file`, and `vm_ip_unavailable` |
| 1.2.6 | Return 400 (not 500) for all client input errors; reserve 500 for internal failures | M | ✅ Done — merged validation/error-response follow-ups moved client-input failures onto explicit 4xx paths while keeping 5xx responses for actual internal errors |
| 1.2.7 | Sanitize error messages: strip libvirt internal details from user-facing responses | S | ✅ Done — API handlers now translate backend/libvirt-facing failures into user-friendly messages instead of leaking raw internal details |

### 1.3 Test Coverage Improvements

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 1.3.1 | Add unit tests for VM name/CPU/RAM validation rules (after 1.2.1-1.2.2) | S | ✅ Done — `internal/api/validation_test.go` covers VM name trimming/rules plus CPU/RAM/disk bound validation for create/update payloads |
| 1.3.2 | Add API tests for all 400-class error paths (invalid JSON, missing fields, out-of-range values) | M | ✅ Done — `internal/api/api_test.go` now covers additional create/update validation failures for disk bounds and tags, plus upload-image missing-name handling, alongside the existing invalid JSON and missing-field cases |
| 1.3.3 | Add port forward collision test (duplicate host:port+protocol) | S | ✅ Done — duplicate `host_port` + protocol conflicts are covered in `internal/network/portforward_test.go` and `internal/api/api_test.go` |
| 1.3.4 | Add image upload edge-case tests: zero-byte file, oversized file, non-qcow2 file | M | ✅ Done — `internal/api/api_test.go` covers zero-byte, non-`.qcow2`, and insufficient-storage upload paths via multipart `httptest` cases |
| 1.3.5 | Add CLI output tests: verify `vmsmith vm list` table format, `vmsmith image list` output | S | ✅ Done — `internal/cli/commands_test.go` captures stdout and verifies table headers/rows for both `vm list` and `image list` |

---

## Phase 2: Core Feature Additions (Week 3-5)

New features that fill the most-requested gaps.

### 2.1 VM Cloning

Currently the only way to duplicate a VM is export-to-image then create-from-image — a slow multi-step process.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 2.1.1 | Add `Clone(ctx, sourceID, newName) (*VM, error)` to `vm.Manager` interface | S | ✅ Done — cloning groundwork now exists at the manager layer so API/CLI/frontend work can target a stable method |
| 2.1.2 | Implement in `LibvirtManager`: `qemu-img create` overlay from source disk, new domain XML, new cloud-init ISO | L | Must handle: new MAC, new DHCP reservation, new VM ID. Source VM should be stopped |
| 2.1.3 | Implement in `MockManager` | S | ✅ Done — mock cloning copies the source VM spec/metadata into a new stopped VM for fast API/CLI test coverage |
| 2.1.4 | Add `POST /api/v1/vms/{id}/clone` endpoint | S | ✅ Done — API now exposes VM cloning with request validation, duplicate-name checks, typed error responses, and handler coverage for success/not-found/error cases |
| 2.1.5 | Add `vmsmith vm clone <id> --name <name>` CLI command | S | ✅ Done — CLI now supports `vmsmith vm clone <id> --name <name>` with test coverage and updated docs |
| 2.1.6 | Add "Clone" button to VMDetail page in frontend | S | ✅ Done — VM detail now offers a clone action modal that posts the new VM name to `POST /api/v1/vms/{id}/clone` and redirects to the cloned VM on success |
| 2.1.7 | Add integration + E2E tests | M | ✅ Done — clone coverage now spans API integration + failure-path validation in `internal/api/api_test.go`, the real-daemon clone flow in `tests/e2e/test_api_vm_lifecycle.py`, and the VM-detail Playwright flow in `tests/web/gui.spec.js`, including duplicate-name error handling and redirect to the newly created machine |

### 2.2 VM Tags & Metadata

No way to organize or annotate VMs. Tags enable filtering, grouping, and automation.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 2.2.1 | Add `Tags []string` and `Description string` fields to `types.VM` | S | ✅ Done — added top-level VM metadata plus mirrored create/update payload support in `pkg/types/vm.go` |
| 2.2.2 | Accept tags/description in `POST /vms` and `PATCH /vms/{id}` | S | ✅ Done — API accepts, trims, normalizes, and persists metadata on create/update |
| 2.2.3 | Add `--tag` flag (repeatable) and `--description` to `vmsmith vm create` and `vmsmith vm edit` | S | ✅ Done — CLI create/edit now support tags + description |
| 2.2.4 | Add `GET /vms?tag=<tag>` filter support | M | ✅ Done — list handler supports case-insensitive tag filtering |
| 2.2.5 | Show tags as badges in VMList and VMDetail frontend pages | S | ✅ Done — VM list/detail now render description + tag badges, and the list page supports quick tag filtering |
| 2.2.6 | Add `vmsmith vm list --tag <tag>` CLI filter | S | ✅ Done — CLI list supports tag filtering and shows tags in the table |
| 2.2.7 | Mirror tags/description onto images: `Description` + `Tags` on `types.Image`, accepted on `POST /api/v1/images` create-from-VM and `POST /api/v1/images/upload`, mutable via new `PATCH /api/v1/images/{id}`, surfaced as `?tag=` filter on `GET /api/v1/images`, `vmsmith image create --description --tag`, `vmsmith image edit`, `vmsmith image list --tag`, image-list tag chips + edit modal in the GUI | M | ✅ Done — `pkg/types/image.go` adds `Description` + `Tags` (and an `UpdatedAt` stamp); `internal/storage/image.go` accepts `CreateImageOptions{Description, Tags}` on create + import and exposes `UpdateImage(id, ImageUpdateSpec)`; `internal/api/handlers_image.go` validates description (1024-char cap) + normalises tags (re-using `normalizeTags`), wires `PATCH /api/v1/images/{id}` and `?tag=` filter; `vmsmith image create --description --tag` / `vmsmith image edit` / `vmsmith image list --tag` and the ImageList edit modal + tag-filter chips ship the GUI surface. Coverage: storage round-trip + clear semantics, API integration (PATCH + tag-filter + upload + 1024-char limit + invalid-tag rejection), CLI edit + filter unit tests, mock-server PATCH/?tag= round-trip, and Playwright + JSDOM scenarios for the badges, filter chip, and edit modal |
| 2.2.8 | Add free-text `description` to snapshots, surfaced through the API, CLI, and GUI; persist via libvirt's `<description>` element so it round-trips through `dumpxml`. CreatedAt is also parsed from the libvirt snapshot XML so list responses carry an accurate timestamp | S | ✅ Done — `pkg/types/snapshot.go` adds `Description` and a `SnapshotSpec` create payload; `LibvirtManager.CreateSnapshot` writes `<description>` into the snapshot XML and `ListSnapshots` parses both description and `creationTime` back out via `parseSnapshotXML`; `POST /api/v1/vms/{id}/snapshots` accepts an optional `description` (≤1024 chars, validated by `validateCreateSnapshotRequest`); `vmsmith snapshot create --description` and a new `DESCRIPTION` column on `vmsmith snapshot list`; the VMDetail snapshot list shows the description inline and the create modal exposes a textarea. Coverage: API integration (`TestCreateSnapshot_WithDescription`, `TestCreateSnapshot_RejectsLongDescription`), CLI (`TestCLI_SnapshotCreate_DescriptionFlag`, `TestCLI_SnapshotCreate_DescriptionTooLong`, updated `TestCLI_SnapshotList_WithSnapshots`), unit (`internal/vm/snapshot_xml_test.go`), and Playwright/JSDOM scenarios for create-with-description and the seeded description rendering |
| 2.2.9 | Add free-text `description` to port forwards (max 256 chars) so operators can label rules ("ssh-jumpbox", "metrics scrape"), surfaced through the API, CLI, and GUI | S | ✅ Done — `pkg/types/network.go` adds `PortForward.Description`; `network.PortForwarder.Add` accepts `AddOptions{Description}`; `POST /api/v1/vms/{id}/ports` validates `description` (≤256 chars, `invalid_port_forward`); `vmsmith port add --description` and a new `DESCRIPTION` column on `port list`; the VMDetail port-forward card shows the description inline and the add modal exposes a labeled text input. Coverage: unit (`internal/network/portforward_test.go`), API integration (`TestAddPort_RejectsLongDescription`), CLI (`TestCLI_PortAdd_DescriptionTooLong`, updated `TestCLI_PortList_WithForwards`), mock-server, and Playwright. |
| 2.2.10 | Editable template metadata: `PATCH /api/v1/templates/{id}` + `?tag=<tag>` list filter + CLI `template edit` / `template list --tag` + GUI tag-chip + description preview on the Create-VM template selector | M | ✅ Done — `pkg/types/template.go` adds `TemplateUpdateSpec` (empty `description` = no change; nil `tags` = no change; explicit `[]` clears); `internal/storage/image.go` adds `Manager.UpdateTemplate` with no-op detection (UpdatedAt only advances when something actually changes) and a 1024-char description cap; `internal/api/handlers_template.go` ships PATCH (404 for unknown id, 400 `invalid_spec` for over-long description) plus `?tag=<tag>` (case-insensitive, applied before pagination so `X-Total-Count` reflects the filtered population); `internal/cli/template.go` adds `template edit` (with `--clear-tags` for explicit clear) and `template list --tag`; `web/src/pages/VMList.jsx`'s template hint now renders the selected template's description plus tag chips; `docs/openapi.yaml` documents the new path and `UpdateTemplateRequest` schema |
| 2.2.12 | Editable port-forward metadata: `PATCH /api/v1/vms/{id}/ports/{portId}` + `vmsmith port edit <id> --description ""` CLI + GUI edit modal (Pencil button) on the VMDetail port-forward card. Surface only the free-text `description` for now — the iptables 5-tuple (host_port/guest_port/guest_ip/protocol) is intentionally immutable since changing any of those is a delete-and-re-add. The URL VM is the authoritative scope: a request that targets a rule owned by another VM returns 404 `resource_not_found`, mirroring the foreign-VM safety property of `bulk_delete` (2.3.7) | S | ✅ Done — `pkg/types/network.go` adds `PortForwardUpdateSpec{Description *string}` (nil = leave as-is, empty string = clear). `network.PortForwarder.Update(id, UpdateOptions)` looks up the rule globally, applies the patch with `strings.TrimSpace`, and persists; missing ids surface as typed `resource_not_found`. `PATCH /api/v1/vms/{vmID}/ports/{portID}` validates the description (≤256 chars, `invalid_port_forward`), pre-loads the URL VM's forwards to enforce the foreign-VM 404, emits `port_forward.updated` on success. CLI `vmsmith port edit <port-id> --description "..."` (omit `--description` for a clear no-op-rejection error; pass `""` to clear). The VMDetail port-forward card adds a Pencil-icon edit button alongside the existing Trash icon and an `EditPortModal` that round-trips through `ports.update`. Coverage: network unit (`TestPortForwarder_Update_SetsDescription`, `_ClearsDescription`, `_NilDescriptionIsNoOp`, `_NotFound`); API integration (`TestUpdatePort_SetsDescription`, `_ClearsDescription`, `_OmittedDescriptionIsNoOp`, `_RejectsLongDescription`, `_NotFound`, `_PortForwardOnDifferentVM_NotFound`, `_BadJSON`); CLI (`TestCLI_PortEdit_SetsDescription`, `_ClearsDescription`, `_RequiresFlag`, `_DescriptionTooLong`, `_NotFound`); mock-server PATCH route + Playwright (`edit port forward description`, `clear port forward description via edit`) + JSDOM (`edit port forward description`). |
| 2.2.13 | Free-text VM search filter: `GET /api/v1/vms?search=<text>` does a case-insensitive substring match against name, description, and tags (ID intentionally excluded — opaque `vm-<unix-nano>` strings rarely match a useful operator query). Combines additively with the existing `?tag=` and `?status=` filters. Mirrored as `vmsmith vm list --search <text>` and a debounced search input on the VMList page that round-trips through the URL via `?search=`. The matching helper lives in `pkg/types.VMMatchesSearch` so API and CLI share the same predicate | S | ✅ Done — `pkg/types/vm_search.go` adds `VMMatchesSearch` (lowercase substring scan over name + description + tags); `internal/api/handlers_vm.go` and `internal/cli/vm.go` route through it after trimming + lowercasing the query so callers can't accidentally regress the case-insensitive contract; `tests/web/mock-server.js` mirrors the same predicate so Playwright assertions work; `web/src/pages/VMList.jsx` adds a 250ms-debounced search input next to the tag chips with a clear-button and URL-search-param round-trip; `docs/openapi.yaml` adds the `SearchFilter` parameter under `/vms`. Coverage: 6 `TestVMMatchesSearch_*` unit cases (empty / nil / name / description / tag substring / case-handling contract / empty description), 7 API integration cases (`TestListVMs_FilterBySearch_MatchesName`, `_MatchesDescription`, `_MatchesTag`, `_IsCaseInsensitive`, `_NoMatch`, `_CombinesWithStatus`, `_CombinesWithTag`, `_TrimsWhitespace`), 5 CLI cases (`TestCLI_VMList_FilterBySearch_MatchesName`, `_MatchesDescription`, `_MatchesTag`, `_NoMatch`, `_CombinesWithStatus`), and 2 Playwright scenarios (`search input filters the VM list and round-trips through the URL`, `search input matches no VMs when query has no hits`) |
| 2.2.11 | Editable snapshot metadata: `PATCH /api/v1/vms/{id}/snapshots/{name}` + `vmsmith snapshot edit` CLI + GUI edit modal on the VMDetail snapshot list. Surface only the `description` for now — libvirt has no in-place rename, so a name change would need copy-on-write of the underlying disk state and is intentionally out of scope | S | ✅ Done — `pkg/types/snapshot.go` adds `SnapshotUpdateSpec{Description *string}` (nil = leave as-is, empty string = clear). `LibvirtManager.UpdateSnapshot` round-trips the snapshot XML through `SnapshotCreateXML(DOMAIN_SNAPSHOT_CREATE_REDEFINE)` via a pure-Go `rewriteSnapshotDescription` helper — disk/memory state, parent pointer, creation timestamp, and root-element attributes survive verbatim. `MockManager.UpdateSnapshot` mirrors the contract with a `UpdateSnapshotErr` injection hook. `PATCH /api/v1/vms/{id}/snapshots/{name}` enforces the 1024-char `invalid_description` cap and emits `snapshot.updated` on success. CLI ships `vmsmith snapshot edit <vm-id> <snap-name> --description "..."` (omit `--description` for a no-op; pass `""` to clear). The VMDetail snapshot list adds a Pencil-icon Edit button and an `EditSnapshotModal` that round-trips through `snapshots.update`. Coverage: API (`TestUpdateSnapshot_SetsDescription`, `_ClearsDescription`, `_OmittedDescriptionIsNoOp`, `_RejectsLongDescription`, `_NotFound`, `_BadJSON`, `_ManagerError`); CLI (`TestCLI_SnapshotEdit_SetsDescription`, `_ClearsDescription`, `_NoFlagIsNoOp`, `_DescriptionTooLong`, `_NotFound`); MockManager (`TestMockManager_UpdateSnapshot_SetsDescription`, `_TrimsWhitespace`, `_NilDescriptionIsNoOp`, `_VMNotFound`, `_SnapshotNotFound`, `_ErrorInjection`); pure-Go XML rewrite (`TestRewriteSnapshotDescription_AddsDescription`, `_ReplacesExistingDescription`, `_ClearsDescription`, `_PreservesAttributes`, `_EscapesUnsafeText`, `_BadXML`); mock-server PATCH route + Playwright `edit snapshot description` and `clear snapshot description via edit` |
| 2.2.16 | Free-text `tags` field on port forwards, surfaced through the API, CLI, and GUI. Port forwards already carried a free-form `description` (2.2.9) but lacked the `tags []string` axis that every other "tagged" resource ships — VMs (2.2.1, 2.2.4), images (2.2.7), templates (2.2.10), and webhooks (2.2.15) all expose tag-based grouping + `?tag=<tag>` filtering. Operators routinely accumulate 5–20 forwards on a single jumpbox VM ("ssh-jumpbox", "metrics-scrape", "audit-syslog", "tenant-A-web", "tenant-B-web") and want to slice them by purpose (production vs staging) or by team (sre vs platform) rather than by host port alone. Accepted on `POST /api/v1/vms/{id}/ports` and editable in place via `PATCH /api/v1/vms/{id}/ports/{portId}` with pointer semantics (nil = no change, `[]` = clear). Included in the case-insensitive `?search=` haystack alongside description / protocol / host_port / guest_port / guest_ip so the existing 5.4.11 search filter becomes useful for tag-only queries. New `?tag=<tag>` exact-match filter on `GET /api/v1/vms/{id}/ports` composes additively with `?search=` (the tag filter is applied first). CLI: `vmsmith port add --tag <t>` (repeatable), `vmsmith port edit --tag <t>` / `--clear-tags`, `vmsmith port list --tag <t>`, plus a new `TAGS` column in `port list`. GUI: the Add port-forward modal grows an optional Tags input; the Edit modal pre-fills the current tags as a comma-separated list and clears via empty submission; each port-forward row renders tag chips beneath the URL/description line. | S | ✅ Done — `pkg/types/network.go` adds `Tags []string` on `PortForward` and `Tags *[]string` on `PortForwardUpdateSpec` (nil = no change, `[]` = clear). `pkg/types/portforward_search.go` extends the haystack to include tags (stored lowercased on persistence, so the substring check is a straight `Contains`). `internal/network/portforward.go` adds `AddOptions.Tags` and `UpdateOptions.Tags` and persists them verbatim (the API layer normalises). `internal/api/validation.go` adds `normalizePortForwardTags` which re-wraps `validate.NormalizeTags`'s error as `invalid_port_forward`. `internal/api/handlers_network.go`: `AddPort` accepts `tags[]`; `UpdatePort` accepts `tags` with pointer semantics; `ListPorts` applies `?tag=<tag>` before `?search=` (case-insensitive exact match) and emits `port_forward.updated` with the new tag set. `internal/cli/network.go` adds `port add --tag`, `port edit --tag --clear-tags` (mutually exclusive with `--tag`), `port list --tag`, and a `TAGS` column on `port list`. `web/src/pages/VMDetail.jsx`: AddPortModal grows a Tags input; EditPortModal pre-fills + clears tags; each row renders `port-tags-<id>` chips. `tests/web/mock-server.js` mirrors the POST/PATCH validation + `?tag=` filter + extended `?search=` predicate. Coverage: unit (`pkg/types/portforward_search_test.go` × 3 new — `_TagSubstring`, `_TagCaseInsensitive`, `_EmptyTagsHandled`); network unit (`TestPortForwarder_Add_PersistsTags`, `_Update_SetsTags`, `_Update_ClearsTagsWithEmptySlice`, `_Update_NilTagsIsNoOp`); API integration (`TestAddPort_WithTags`, `_RejectsInvalidTag`, `_RejectsEmptyTag`, `TestUpdatePort_SetsTags`, `_ClearsTags`, `_OmittedTagsIsNoOp`, `_RejectsInvalidTag`, `TestListPorts_FilterByTag`, `_FilterByTag_CaseInsensitive`, `_FilterByTag_ComposesWithSearch`, `_FilterBySearch_MatchesTag`); CLI (`TestCLI_PortAdd_WithTags`, `_RejectsInvalidTag`, `TestCLI_PortEdit_SetsTags`, `_ClearsTags`, `_RejectsConflictingTagFlags`, `TestCLI_PortList_FilterByTag`, `_ShowsTagsColumn`); Playwright (`add port forward with tags renders chips inline`, `edit port forward tags and clear via empty input`, `port search input matches port-forward tags`); JSDOM mock-GUI (`edit port forward tags renders chips and clears`). `docs/openapi.yaml` documents the new `tags` field on `PortForward` / `AddPortRequest` / `UpdatePortRequest` and the new `?tag=<tag>` query parameter. CLAUDE.md REST quick reference for `GET /vms/{id}/ports` updated accordingly. |
| 2.2.14 | Free-text `description` field on webhooks, surfaced through the API, CLI, and GUI. Webhooks were the last resource shipping without a description — every other resource (VMs 2.2.1, images 2.2.7, snapshots 2.2.8, port forwards 2.2.9, templates 2.4.1) carries one, and operators routinely register 5–20 webhooks (Slack notifiers for crashes, PagerDuty escalations, internal audit log forwarders, …) without a per-hook label to disambiguate them. Accepted on `POST /api/v1/webhooks` and editable in place via `PATCH /api/v1/webhooks/{id}` with pointer semantics (nil = no change, empty = clear, capped at 1024 chars). Included in the case-insensitive `?search=` haystack alongside `url` and `event_types` (5.4.14) so the existing search box becomes useful for operators who tag webhooks by purpose rather than URL. CLI `vmsmith webhook add --description` and `vmsmith webhook edit --description "..."` (pass `""` to clear) | S | ✅ Done — `pkg/types/webhook.go` adds `Description string` on `Webhook` / `WebhookCreateRequest` and `*string` on `WebhookUpdateSpec` (nil = no change, "" = clear). `pkg/types/webhook_search.go` extends the haystack to include description, with explicit tests locking in case-insensitivity and the empty-description short-circuit. `internal/api/validation.go` adds `validateWebhookDescription` (≤1024 chars, `invalid_webhook` error code). `internal/api/handlers_webhook.go` validates + persists on `POST` (trims first) and applies pointer-semantics on `PATCH` (no-op detection so a description-equals-current PATCH doesn't bounce the worker). `internal/cli/webhook.go` adds `--description` to `webhook add` and `webhook edit`, prints `Description: …` in the output, surfaces a `DESCRIPTION` column in `webhook list`, and documents the field in `webhook list --search` help. The Settings page (`web/src/pages/Settings.jsx`) renders the description under the URL in the webhook row, adds an optional Description input on the Add modal, and a pre-filled Description input on the Edit modal (clearing it sends `description: ""`). Coverage: unit (`pkg/types/webhook_search_test.go` × 3 new — description substring, case-insensitive, empty-description-doesn't-match); API integration (`TestCreateWebhook_WithDescription`, `_RejectsLongDescription`, `TestUpdateWebhook_SetsDescription`, `_ClearsDescription`, `_OmittedDescriptionIsNoOp`, `_RejectsLongDescription`, `_DescriptionNoOpDoesNotBounceWorker`, `TestListWebhooks_FilterBySearch_MatchesDescription`); CLI (`TestCLI_WebhookAdd_WithDescription`, `_DescriptionTooLong`, `TestCLI_WebhookEdit_SetsDescription`, `_ClearsDescription`, `_DescriptionTooLong`); mock-server (POST + PATCH accept description with the same 1024-char cap; search predicate mirrors the API); Playwright (Settings add/edit description flows). `docs/openapi.yaml` documents the new fields on `Webhook` / `WebhookCreateRequest` / `WebhookUpdateSpec` and extends the `/webhooks` search description. |
| 2.2.15 | Free-text `tags` list on webhooks, surfaced through the API, CLI, and GUI. Closes the parity gap with VMs (2.2.1, 2.2.4), images (2.2.7), and templates (2.2.10) — webhooks were the last resource without a tag list. Operators routinely register 5–20 webhooks (Slack notifiers, PagerDuty escalations, audit log forwarders) and want to organise them by team / purpose / environment instead of by URL alone. Accepted on `POST /api/v1/webhooks` (normalised lowercase, deduplicated, alphabetised, capped at 1-32 chars per tag with the same `[a-z0-9][a-z0-9._:-]*` alphabet every other tagged resource uses) and editable in place via `PATCH /api/v1/webhooks/{id}` with pointer semantics (nil = no change; `[]` = clear; permutation of the current set is a no-op that does not bounce the delivery worker). Included in the case-insensitive `?search=` haystack alongside `url`, `event_types`, and `description`; the new `?tag=<tag>` query parameter does exact-match (any-of) filtering and composes additively with `?search=`. CLI: `vmsmith webhook add --tag <t>` (repeatable), `vmsmith webhook edit --tag <t>` / `--clear-tags`, `vmsmith webhook list --tag <t>`, new `TAGS` column in `webhook list`. GUI: tag chip rendering under each Settings page row plus tag inputs on the Add / Edit modals. | S | ✅ Done — `pkg/types/webhook.go` adds `Tags []string` on `Webhook` / `WebhookCreateRequest` and `*[]string` on `WebhookUpdateSpec` (nil = no change; `[]` = clear). `pkg/types/webhook_search.go` extends the haystack to include tags with explicit tests locking in substring / partial / case-insensitive / empty-tags-handled contracts. `internal/api/validation.go` adds `validateWebhookTags` (re-uses the shared `NormalizeTags` validator and re-wraps `invalid_spec` errors as `invalid_webhook` for the webhook surface). `internal/api/handlers_webhook.go` validates + persists on `POST`, applies pointer-semantics on `PATCH` (with a `tagSetsEqual` no-op detector so a permutation of the current set doesn't bounce the in-memory delivery worker), and adds `?tag=<tag>` filtering to `ListWebhooks` (applied before `?search=`). `internal/cli/webhook.go` adds `--tag` to `webhook add` / `webhook edit` / `webhook list` plus `--clear-tags` on edit (mutually exclusive with `--tag`); `webhook list` grows a `TAGS` column. The Settings page (`web/src/pages/Settings.jsx`) renders tag chips under each row, adds an optional Tags input on the Add modal, and a pre-filled Tags input on the Edit modal (clearing it sends `tags: []`). Coverage: unit (`pkg/types/webhook_search_test.go` × 4 new — `_TagSubstring`, `_TagPartialSubstring`, `_TagCaseInsensitive`, `_EmptyTagsHandled`); API integration (`TestCreateWebhook_WithTags`, `_RejectsInvalidTagCharacters`, `_RejectsEmptyTag`, `TestUpdateWebhook_SetsTags`, `_ClearsTags`, `_OmittedTagsIsNoOp`, `_TagsNoOpDoesNotBounceWorker`, `_RejectsInvalidTag`, `TestListWebhooks_FilterBySearch_MatchesTag`, `TestListWebhooks_FilterByTag`, `_FilterByTag_CaseInsensitive`, `_FilterByTag_TrimsWhitespace`, `_FilterByTag_NoMatchReturnsEmpty`, `_FilterByTag_ComposesWithSearch`); CLI (`TestCLI_WebhookAdd_WithTags`, `TestCLI_WebhookEdit_SetsTags`, `_ClearsTags`, `_RejectsConflictingTagFlags`, `TestCLI_WebhookList_ForwardsTagFilter`, `_EmptyTagOmitsParam`, `_ShowsTagsColumn`); mock-server (POST + PATCH accept tags with full normalisation + invalid-tag rejection; `?tag=` filter + extended search haystack mirror the API); Playwright (`create webhook with tags renders chips in the row`, `edit webhook tags and clear them via empty input`, `search input matches webhook tags`); JSDOM (`create webhook with tags, edit, and clear`). `docs/openapi.yaml` documents the new `tags` field on `Webhook` / `WebhookCreateRequest` / `WebhookUpdateSpec` plus the `?tag=` query parameter on `GET /webhooks`. |
| 2.2.17 | Free-text `tags []string` on snapshots, surfaced through the API, CLI, and GUI. Snapshots already carried `description` (2.2.8 / 2.2.11) but lacked the `tags []string` axis every other tagged resource ships — VMs (2.2.1, 2.2.4), images (2.2.7), templates (2.2.10), port forwards (2.2.16), and webhooks (2.2.15) all expose tag-based grouping + `?tag=` filtering. Closes the symmetry gap so operators tailing a multi-environment VM with 20+ snapshots (`pre-may-patch`, `pre-jun-patch`, `auto-2026-05-06`, …) can slice them by purpose ("audit checkpoints") or by team ("sre rollbacks") rather than by name alone. Accepted on `POST /api/v1/vms/{id}/snapshots`, editable via `PATCH /api/v1/vms/{id}/snapshots/{name}` with pointer semantics (nil = no change, `[]` = clear), exposed through `?tag=` exact-match filter on the list endpoint, and folded into the `?search=` haystack so the existing search box becomes useful for tag-based queries. CLI mirrors via `vmsmith snapshot create --tag`, `snapshot edit --tag` / `--clear-tags`, `snapshot list --tag` with a new TAGS column; GUI grows tag chips on the VMDetail snapshot rows plus tag input on the Create/Edit modals | S | ✅ Done — Tags persist out-of-band from libvirt because the libvirt domainsnapshot XML schema does not permit `<metadata>`, so the previously-unused `snapshots` bbolt bucket is reused as a key-value store of `{vmID}/{name}` → `{tags:[…]}` JSON records. `internal/store/bolt.go` adds `PutSnapshotTags` / `GetSnapshotTags` / `DeleteSnapshotTags` / `ListSnapshotTagsByVM` (a `vmID + "/"` prefix-bounded cursor walk so `vm-1` does not bleed into `vm-10`). `pkg/types/snapshot.go` adds `Tags []string` to `Snapshot` / `SnapshotSpec` and `Tags *[]string` to `SnapshotUpdateSpec` (pointer = nil leaves untouched; explicit `[]` clears). `pkg/types/snapshot_search.go` extends the haystack to include tags. `internal/vm/lifecycle.go` writes tags to bbolt after the libvirt snapshot create succeeds, merges them in via one cursor walk on list, applies the patch on update, and drops the record on delete (orphan tag records would be self-healing on the next list cycle anyway). `internal/vm/mock_manager.go` mirrors the in-memory contract. `internal/api/handlers_snapshot.go` validates tags via the shared `validate.NormalizeTags` re-wrapped as `invalid_snapshot`, applies the `?tag=` filter before `?search=` (so `X-Total-Count` reflects the post-filter population), and extends search. `internal/cli/snapshot.go` adds `--tag` to `snapshot create` and `snapshot edit`, `--clear-tags` to `snapshot edit` (mutually exclusive with `--tag`), `--tag` to `snapshot list`, and a new TAGS column. The VMDetail snapshot card renders tag chips beneath the description and the Add/Edit modals grow a Tags input — the edit modal sorts both sides before comparing so a permutation is recognised locally and skips the round-trip. Coverage: unit (`pkg/types/snapshot_search_test.go` × 3 new — tag substring, case-insensitive, empty-tags-handled; `internal/store/bolt_test.go` × 7 new — round-trip, missing-returns-nil, empty-clears, delete-removes, delete-missing-idempotent, list-by-vm, rejects-empty-keys, list-empty-store, prefix-isolation `vm-1` vs `vm-10`); API integration (`TestCreateSnapshot_WithTags`, `_RejectsInvalidTag`, `_EmptyTagsArrayPersistsNoneAndListOmitsField`, `TestUpdateSnapshot_SetsTags`, `_ClearsTags`, `_OmittedTagsIsNoOp`, `_RejectsInvalidTag`, `TestListSnapshots_FilterByTag`, `_CaseInsensitive`, `_NoMatch`, `_ComposesWithSearch`, `_FilterBySearch_MatchesTag`); CLI (`TestCLI_SnapshotCreate_WithTags`, `_PrintsTags`, `TestCLI_SnapshotEdit_SetsTags`, `_ClearTagsFlag`, `_RejectsConflictingTagFlags`, `TestCLI_SnapshotList_FilterByTag`, `_CaseInsensitive`, `_ShowsTagsColumn`); mock-server (POST + PATCH normalise + persist tags; GET supports `?tag=` exact-match and extended `?search=` haystack); Playwright (`create snapshot with tags renders chips inline`, `edit snapshot tags via the edit modal`, `clear snapshot tags via empty input`, `snapshot search input matches against tag values`); JSDOM (`create snapshot with tags renders chips and edit clears them`). `docs/openapi.yaml` documents `tags` on `Snapshot` / `CreateSnapshotRequest` / `UpdateSnapshotRequest` and the new `?tag=` query parameter on `GET /vms/{vmID}/snapshots`. |

### 2.3 Bulk Operations

Operating on VMs one-at-a-time is tedious when managing many.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 2.3.1 | Add `POST /api/v1/vms/bulk` endpoint: `{ "action": "start|stop|delete", "ids": [...] }` | M | ✅ Done — `POST /api/v1/vms/bulk` now performs start/stop/delete across multiple VM IDs and returns per-VM success/failure results, including typed error codes for missing/invalid entries |
| 2.3.2 | Add `vmsmith vm start --all`, `vmsmith vm stop --all`, with `--tag` filter | M | ✅ Done — CLI `vm start` / `vm stop` now accept `--all` with optional `--tag`, skip VMs already in the wrong state, and document/test the bulk lifecycle flow |
| 2.3.3 | Add multi-select checkboxes + bulk action bar to VMList frontend page | M | ✅ Done — VMList now supports per-row selection, select-all for the current filtered view, and a bulk action bar that fans out start/stop/delete over existing per-VM endpoints with selection-aware skip messaging |
| 2.3.5 | Bulk-delete snapshots in one request (`POST /api/v1/vms/{id}/snapshots/bulk_delete` with `names` or `prefix`) plus matching CLI (`vmsmith snapshot delete <vm-id> --prefix <s>`) and GUI (multi-select checkboxes + "Delete selected" on the VMDetail snapshot list). Exactly one of `names` / `prefix` is accepted; partial failures surface per-snapshot in the response. | S | ✅ Done — `POST /vms/{id}/snapshots/bulk_delete` accepts `{names: [...]}` or `{prefix: "..."}`, returns per-target results, and emits one `snapshot.deleted` event per successful target. CLI `vmsmith snapshot delete <id> --prefix <s>` enumerates matches via the existing manager API and prints per-snapshot status. The VMDetail snapshot list now shows per-row + select-all checkboxes and a "Delete selected" action driving the bulk endpoint. Coverage: API integration (`TestBulkDeleteSnapshots_*` covering names, prefix, partial failure, both-flags + empty-payload rejection, no-match), CLI (`TestCLI_SnapshotDelete_*` for prefix matches, no-match message, mutual-exclusion validation), Playwright (`bulk delete selected snapshots`, `bulk delete via select-all snapshots`), and JSDOM mock-GUI (`bulk-delete selected snapshots`). |
| 2.3.6 | Bulk-delete images in one request (`POST /api/v1/images/bulk_delete` with `ids` or `tag`) plus matching CLI (`vmsmith image delete --tag <tag>`) and GUI (multi-select checkboxes + "Delete selected" on the ImageList page). Exactly one of `ids` / `tag` is accepted; tag matching is case-insensitive; partial failures surface per-image in the response. | S | ✅ Done — `POST /images/bulk_delete` accepts `{ids: [...]}` or `{tag: "..."}`, returns per-target results, and emits one `image.deleted` event per successful target with `bulk=true`. CLI `vmsmith image delete --tag <tag>` enumerates matches via `FilterImagesByTag` and prints per-image status. The ImageList page now shows per-row + select-all checkboxes, a "Delete selected" action driving the bulk endpoint, and a dismissible "X of Y succeeded" summary banner. Coverage: API integration (`TestBulkDeleteImages_ByIDs`, `_ByTag`, `_PartialFailure`, `_EmptyRequestRejected`, `_BothIDsAndTagRejected`, `_TagNoMatchEmptyResponse`), CLI (`TestCLI_ImageDelete_NoArgsErrors`, `_BothIDAndTagErrors`, `_TagDeletesAllMatching`, `_TagNoMatchPrintsMessage`), mock-server `/images/bulk_delete` route, and Playwright `bulk delete selected images` + `bulk delete via select-all images`. |
| 2.3.7 | Bulk-delete port forwards in one request (`POST /api/v1/vms/{id}/ports/bulk_delete` with `ids` or `protocol`) plus matching CLI (`vmsmith port remove --vm <id> [--protocol <p>]`) and GUI (multi-select checkboxes + "Delete selected" on the VMDetail port-forward list). Exactly one of `ids` / `protocol` is accepted; the protocol selector is always scoped to the URL VM so it never deletes another VM's rules. Partial failures surface per-rule in the response. | S | ✅ Done — `POST /vms/{id}/ports/bulk_delete` accepts `{ids: [...]}` or `{protocol: "tcp"\|"udp"}`, validates exactly-one-of, scopes the protocol selector to the URL VM, surfaces missing ids as per-target `resource_not_found`, and emits one `port_forward.removed` event per successful target with `bulk=true`. CLI `vmsmith port remove --vm <id> [--protocol tcp\|udp]` enumerates matches and prints per-rule status (the existing single-positional `port remove <port-id>` is preserved). The VMDetail port-forward card now shows per-row + select-all checkboxes and a "Delete selected" button driving the bulk endpoint. Coverage: API integration (`TestBulkDeletePorts_ByIDs`, `_ByProtocol`, `_ProtocolCaseInsensitive`, `_PartialFailure`, `_IDForOtherVM_NotDeleted`, `_EmptyRequestRejected`, `_BothIDsAndProtocolRejected`, `_UnknownProtocolRejected`, `_ProtocolNoMatchEmptyResponse`), CLI (`TestCLI_PortRemove_NoArgsErrors`, `_PositionalAndVMRejected`, `_InvalidProtocolRejected`, `_AllForVM`, `_AllForVM_ProtocolFilter`, `_NoMatchPrintsMessage`), mock-server (`/ports/bulk_delete` route enforcing the same 400 contract), Playwright (`bulk delete selected port forwards`, `bulk delete via select-all port forwards`), and JSDOM mock-GUI (`bulk-delete selected port forwards`). |
| 2.3.8 | Extend the bulk endpoint and bulk-action bar with the lifecycle verbs that already ship per-VM: `restart`, `force-stop`, `reboot`, `suspend`, `resume`. `POST /api/v1/vms/bulk` accepts the new `action` values (alongside `start`/`stop`/`delete`); each per-VM success emits the same `vm.<action>_requested` event the single-VM endpoint emits, with `bulk=true` so audit consumers can distinguish operator clicks from bulk fan-out. CLI mirrors as `vmsmith vm restart\|force-stop\|reboot\|suspend\|resume --all [--tag <t>]`, sharing the existing `runBulkVMAction` state-filter machinery (eligible state per verb: `running` for restart/force-stop/reboot/suspend; `paused` for resume). VMList's bulk-action bar grows new buttons (Restart, Force Stop, Reboot, Suspend, Resume) plus a `paused`-count chip, with each button auto-disabled when no selected VM is in the eligible state. | S | ✅ Done — `internal/api/handlers_vm.go` replaces the action map with a `bulkVMActionSpec`-based registry that carries the manager call + matching event type; the handler rejects unknown actions with a self-documenting message that lists every supported verb. CLI ships a `vmLifecycleVerbLabels` table so `Force-stopped`/`Suspended`/`Resumed` past-tense and `force-stoppable`/`suspendable`/`resumable` adjective forms stay in sync between bulk reporting and "no matches" messages. Frontend `BulkActionBar` builds a per-action `bulkActions` map (eligibility predicate + empty-selection message), so adding the next bulk verb is a one-row change. Coverage: API (`TestBulkVMAction_LifecycleVerbs` table covering all five verbs, `TestBulkVMAction_SuspendPartialFailure` for per-VM 409 propagation, `TestBulkVMAction_InvalidActionMessageListsAllVerbs` for the listing contract); CLI (`TestCLI_VMLifecycleAll_Verbs` table for restart/force-stop/reboot/suspend/resume, `TestCLI_VMRestart_AllWithTag`, `TestCLI_VMSuspend_AllNoMatches`, `TestCLI_VMForceStop_AllRejectsID`); Playwright (`bulk restart only acts on running VMs`, `bulk reboot button is disabled when no running VM is selected`); plus updates to the existing `TestBulkVMAction_InvalidRequest` to use a non-listed verb (`shutdown`). OpenAPI's `BulkVMActionRequest.action` enum now lists every supported verb. |
| 2.3.9 | Bulk-delete templates in one request (`POST /api/v1/templates/bulk_delete` with `ids` or `tag`) plus matching CLI (`vmsmith template delete --tag <tag>`). Exactly one of `ids` / `tag` is accepted; tag matching is case-insensitive; partial failures surface per-template in the response. Closes the bulk-delete symmetry gap for templates alongside images (2.3.6), snapshots (2.3.5), and port forwards (2.3.7). GUI surface is deferred — the only Templates GUI today is the Create-VM modal's selector, which is for *selecting* a template to apply; a dedicated admin/list page is out of scope for this slice. | S | ✅ Done — `internal/api/handlers_template.go` adds `BulkDeleteTemplates` mirroring the image bulk-delete shape: accepts `{ids: [...]}` or `{tag: "..."}` (exactly one), returns `{"results": [{id, success, code?, message?}]}`, emits one `template.deleted` event per successful target with `bulk=true`. `internal/cli/template.go` extends `template delete` to accept `--tag <tag>` (mutually exclusive with positional `<template-id>`); enumerates matches via `filterTemplatesByTag` and prints per-template status using the same `OK ... / FAIL ...` shape as image delete. Coverage: API integration (`TestBulkDeleteTemplates_ByIDs`, `_ByTag` with case-insensitive matching, `_PartialFailure` for per-target `resource_not_found`, `_EmptyRequestRejected`, `_BothIDsAndTagRejected`, `_TagNoMatchEmptyResponse`, `_BadJSON`); CLI (`TestCLI_TemplateDelete_NotFound`, `_NoArgsErrors`, `_BothIDAndTagErrors`, `_TagDeletesAllMatching` with case-insensitive tag, `_TagNoMatchPrintsMessage`); mock-server `/templates/bulk_delete` route enforcing the same 400 contract; OpenAPI documents the new path + `BulkDeleteTemplatesRequest` / `BulkDeleteTemplatesResponse` / `BulkDeleteTemplateResult` schemas; the regenerated frontend client exposes `templatesApi.bulkDelete({ ids \| tag })` for any future GUI surface. |
| 2.3.10 | Bulk-delete webhooks in one request (`POST /api/v1/webhooks/bulk_delete` with `ids` or `event_type`) plus matching CLI (`vmsmith webhook delete --event-type <type>`) and Settings-page GUI (multi-select checkboxes + "Delete selected" button + per-batch result banner). Exactly one of `ids` / `event_type` is accepted; the `event_type` selector matches webhooks whose `event_types` filter list contains the exact value (catch-all webhooks with empty `event_types` are NOT swept — delete them by id). Partial failures surface per-webhook in the response. Closes the symmetric bulk-delete surface across every list endpoint alongside snapshots (2.3.5), images (2.3.6), port forwards (2.3.7), and templates (2.3.9). | S | ✅ Done — `internal/api/handlers_webhook.go` adds `BulkDeleteWebhooks` mirroring the template/image bulk-delete shape: accepts `{ids: [...]}` or `{event_type: "..."}` (exactly one), returns `{"results": [{id, success, code?, message?}]}`, notifies the runtime `WebhookRegistrar.Unregister` for each successful target. `internal/cli/webhook.go` extends `webhook delete` to accept `--event-type <type>` (mutually exclusive with positional `<id>`); on bulk path it POSTs to `/webhooks/bulk_delete` and prints per-webhook `OK ... / FAIL ...` status, returning a non-zero exit when any target failed. `web/src/pages/Settings.jsx` adds per-row + select-all checkboxes, a "Delete selected" button, and an "X of Y succeeded" result banner — same shape as the ImageList bulk-delete UI. `web/src/api/client.ts` exposes `webhooks.bulkDelete({ ids \| event_type })` against the regenerated OpenAPI types. Coverage: API integration (`TestBulkDeleteWebhooks_ByIDs`, `_ByEventType` covering catch-all exclusion, `_PartialFailure`, `_EmptyRequestRejected`, `_BothIDsAndEventTypeRejected`, `_EventTypeNoMatchEmptyResponse`, `_BadJSON`, `_DisabledWhenStoreUnset`); CLI (`TestCLI_WebhookDelete_NoArgsErrors`, `_BothIDAndEventTypeErrors`, `_SinglePositional`, `_EventTypeBulk`, `_EventTypeTrimsWhitespace`, `_EventTypeNoMatchPrintsMessage`, `_PartialFailureSurfaces`); mock-server (`tests/web/mock-server.js`) `/webhooks/bulk_delete` route enforcing the same 400 contract + catch-all exclusion; Playwright (`bulk delete selected webhooks via checkbox + Delete-selected`, `select-all then Delete-selected sweeps every webhook`, `Delete-selected button stays disabled when no webhook is selected`) and JSDOM mock-GUI (`bulk-delete selected webhooks via checkbox + Delete-selected`, `select-all then Delete-selected sweeps every webhook`). `docs/openapi.yaml` documents the new path + `BulkDeleteWebhooksRequest` / `BulkDeleteWebhooksResponse` / `BulkDeleteWebhookResult` schemas. |

#### 2.3.4 VM Restart action ✅

`vm.Manager.Restart(ctx, id)` performs a graceful shutdown (30s grace before
forced destroy) followed by a start. Surfaced everywhere the rest of the
lifecycle is:

- API: `POST /api/v1/vms/{vmID}/restart` (returns `{"status":"restarted"}`)
- CLI: `vmsmith vm restart <id>`
- GUI: "Restart" button next to Stop on VMDetail, only when the VM is running
- Events: emits `vm.restart_requested` (`app` source) on success
- Tests: unit (MockManager + happy-path / not-found / error-injection),
  integration (`internal/api/api_test.go`: `TestRestartVM`,
  `TestRestartVM_Error`), CLI (`TestCLI_VMRestart`,
  `TestCLI_VMRestart_NotFound`), and Playwright mock GUI
  (`restart running VM`)

This is the manager-layer primitive that the future scheduler restart action
(5.2.5) will reuse.

### 2.4 VM Templates

Save and reuse VM configurations without re-specifying every parameter.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 2.4.1 | Define `VMTemplate` type: name, image, CPUs, RAM, disk, networks, tags, default_user | S | ✅ Done — `pkg/types/template.go` defines reusable template records backed by a dedicated bbolt `templates` bucket |
| 2.4.2 | Add `POST /api/v1/templates`, `GET /api/v1/templates`, `DELETE /api/v1/templates/{id}` | M | ✅ Done — API now supports template create/list/delete with validation, pagination, and CRUD test coverage |
| 2.4.3 | Add `POST /vms` support for `template_id` field — merges template defaults with request overrides | M | ✅ Done — `POST /api/v1/vms` now accepts `template_id`, applies stored template defaults for image/resources/metadata/networks, preserves explicit request overrides, and returns a clear 400 when the requested template is missing |
| 2.4.4 | Add `vmsmith template create|list|delete` CLI commands | M | ✅ Done — CLI now supports local template create/list/delete flows with coverage for the happy-path CRUD workflow |
| 2.4.5 | Add template selector dropdown to Create VM modal in frontend | S | ✅ Done — the Create VM modal now lists saved templates, prefills form defaults when one is selected, and keeps manual field edits as explicit overrides |
| 2.4.6 | Dedicated Templates admin page in the GUI — closes the deferral called out in 2.3.9 ("the only Templates GUI today is the Create-VM modal's selector"). New top-level `/templates` route + `nav-templates` sidebar entry; `web/src/pages/TemplateList.jsx` mirrors `ImageList.jsx` shape: paginated table with name / image / resources / tags / created-at columns, debounced free-text search input round-tripped through `?search=`, sort dropdowns (`?sort=&order=`) over the existing `id`/`name`/`created_at` whitelist, tag-chip filter, per-row Edit (description + tags via `PATCH /api/v1/templates/{id}`) and Delete (with confirm), select-all + per-row checkboxes driving "Delete selected" against `POST /api/v1/templates/bulk_delete`, and an "X of Y succeeded" result banner. No new backend code — reuses the existing CRUD, bulk_delete (2.3.9), search (5.4.12), sort (5.4.7), and PATCH (2.2.10) endpoints | S | ✅ Done — `web/src/pages/TemplateList.jsx` (new page), `web/src/App.jsx` (route), `web/src/components/Layout.jsx` (`nav-templates` sidebar entry with the `Layers` icon). Coverage: Playwright (`Templates › lists seeded templates with descriptions and tag chips`, `filters templates by tag chip`, `search input filters templates and round-trips through the URL`, `sort dropdowns reorder templates and round-trip through the URL`, `edit modal updates description and tags via PATCH`, `bulk delete selected templates`, `bulk delete via select-all sweeps every template`, `delete single template via row action`); JSDOM mock-GUI (`Templates › lists seeded templates`, `renders description and tag chips`, `filters templates by tag chip`, `edit modal updates description and tags`, `bulk-delete selected templates`); Navigation tests (Playwright + JSDOM) extended to visit the new page |

### 2.5 Daemon-managed VM Lifecycle

Operator-friendly lifecycle hooks driven by the daemon itself, separate from
explicit user actions.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 2.5.1 | Auto-start VMs at daemon boot via a per-VM `auto_start` flag on `VMSpec`/`VMUpdateSpec`, exposed in API, CLI (`--auto-start`), and GUI (checkbox + summary card). Daemon performs a sweep on startup, calling `Start` for any VM marked `auto_start=true` that is currently stopped, emitting `vm.auto_started` / `vm.auto_start_failed` events for observability | M | ✅ Done — `pkg/types/vm.go` adds `AutoStart bool` (always serialised, no `omitempty`) plus `*bool` pointer in `VMUpdateSpec` so toggles are unambiguous; `LibvirtManager.Update` and `MockManager.Update` honour the new flag (an AutoStart-only edit is metadata-only and skips the stop/restart path); `internal/daemon/daemon.go` runs `runAutoStartSweep` after metrics + scrape startup but before the HTTP server begins serving so the daemon settles into a fully running state. Unit, mock-GUI, daemon, and API integration tests cover create/update toggling, the daemon sweep happy path, and the per-VM error path |
| 2.5.2 | Delete-protection / lock flag on VMs to guard against accidental removal. Per-VM `locked` flag on `VMSpec`/`VMUpdateSpec`, exposed in API, CLI (`vmsmith vm lock|unlock <id>` plus `--locked` on `vm create` and `vm edit`), and GUI (lock badge in VMList, edit-modal toggle + summary card on VMDetail). `Manager.Delete` rejects locked VMs with a typed `vm_locked` API error so the daemon, API (HTTP 409), CLI, and GUI all surface a consistent message. Stop/start/restart still work — Lock is a delete guard, not a freeze | S | ✅ Done — `pkg/types/vm.go` adds `Locked bool` on `VMSpec` (always serialised) and `*bool` on `VMUpdateSpec`; `LibvirtManager.Update` / `MockManager.Update` treat a Locked-only edit as metadata-only, alongside the `AutoStart` metadata-only path; `LibvirtManager.Delete` and `MockManager.Delete` return `types.NewAPIError("vm_locked", ...)` when the flag is set so `statusForAPIError` maps it to HTTP 409. CLI ships `vm lock` / `vm unlock` plus `--locked` on `vm create` / `vm edit` and prints the flag in `vm info`. GUI adds a Lock badge in the VMList row, an edit-modal checkbox, and a "Delete protection" summary card on VMDetail. Unit (`TestMockManager_Update_Locked`, `TestMockManager_Delete_Locked`), API (`TestCreateVM_LockedFlag`, `TestUpdateVM_LockToggle`, `TestDeleteVM_Locked_Returns409`), CLI (`TestCLI_VMLockUnlock`, `TestCLI_VMLock_NotFound`), and Playwright/mock-GUI scenarios cover create/update/delete behavior |
| 2.5.3 | VM suspend/resume — freeze a running VM's CPU + memory state via libvirt pause without releasing host resources, then resume later without rebooting. New `Manager.Suspend(ctx, id)` / `Resume(ctx, id)` on the VM manager interface, `POST /api/v1/vms/{id}/suspend` and `/resume` endpoints, `vmsmith vm suspend|resume <id>` CLI commands, and Suspend/Resume buttons on VMDetail. New `paused` VM state with a dedicated badge style. State-machine errors are typed: `vm_not_running` on suspend of a stopped VM, `vm_already_paused` on suspend of an already-paused VM, `vm_not_paused` on resume of a non-paused VM — all surfaced as HTTP 409. App events `vm.suspend_requested` / `vm.resume_requested` show up in the activity timeline | S | ✅ Done — `pkg/types/vm.go` adds `VMStatePaused`; `LibvirtManager.Suspend`/`Resume` call `dom.Suspend()`/`dom.Resume()` after pre-flight state checks, and `domainStateToVMState` now maps `DOMAIN_PAUSED` to `paused` instead of `stopped`; `MockManager` mirrors the same state guards for tests; `quotaManager` forwards. `internal/api/handlers_vm.go` adds `SuspendVM` / `ResumeVM`, `validation.go`'s `statusForAPIError` returns 409 for `vm_not_running` / `vm_not_paused` / `vm_already_paused`, and `docs/openapi.yaml` documents both paths (including `paused` in the `VMState` response enum). CLI lives at `internal/cli/vm.go`. Frontend client and VMDetail expose `vms.suspend`/`vms.resume` plus state-aware buttons, and `Shared.jsx` ships a `paused` badge style. Coverage: 8 MockManager cases (`TestMockManager_SuspendResume_HappyPath`, `_Suspend_NotRunning`, `_Suspend_AlreadyPaused`, `_Resume_NotPaused`, `_Suspend_NotFound`, `_Resume_NotFound`, plus error-injection), 9 API cases (`TestSuspendVM`, `TestSuspendVM_NotRunning_Returns409`, `TestSuspendVM_AlreadyPaused_Returns409`, `TestSuspendVM_NotFound_Returns404`, `TestSuspendVM_Error`, and the Resume mirrors), 6 CLI cases (`TestCLI_VMSuspend`, `_NotRunning`, `_NotFound`, plus the Resume mirrors), and a Playwright spec (`suspend running VM and resume`) covering the round-trip via the mock server |
| 2.5.4 | Force-stop a VM (immediate libvirt `dom.Destroy()` without ACPI graceful shutdown), exposed in API, CLI, and GUI. The regular `Stop` flow sends an ACPI signal first and only force-destroys when that errors — leaving operators with no clean way to kill a VM whose guest OS is wedged or unresponsive. Adds a dedicated `ForceStop(ctx, id)` on the `vm.Manager` interface, `POST /api/v1/vms/{id}/force-stop` (returns 409 `vm_already_stopped` when the VM is not in a running state), `vmsmith vm force-stop <id>`, and a confirmation-gated "Force Stop" button on VMDetail. Emits `vm.force_stop_requested` for audit | S | ✅ Done — `internal/vm/lifecycle.go` adds `LibvirtManager.ForceStop` (libvirt state pre-flight, then `dom.Destroy()`); `internal/vm/mock_manager.go` mirrors the state guard with a `ForceStopErr` injection hook; `internal/vm/quota_manager.go` forwards the new method; `internal/api/handlers_vm.go` adds `ForceStopVM`, the router exposes `POST /vms/{vmID}/force-stop`, and `validation.go` extends `statusForAPIError` so `vm_already_stopped` maps to HTTP 409. CLI ships `vmsmith vm force-stop <id>`. The frontend `vms.forceStop` client method drives a confirmation-gated VMDetail button (visible only while running) using the regenerated OpenAPI types. Coverage: `TestMockManager_ForceStop` + `_AlreadyStopped` + `_NotFound` + `_ErrorInjection`, API `TestForceStopVM` + `_AlreadyStopped_Returns409` + `_NotFound_Returns404` + `_Error`, CLI `TestCLI_VMForceStop` + `_AlreadyStopped` + `_NotFound`, mock-server stub, and a Playwright `force-stop running VM` scenario that round-trips through the confirm dialog |
| 2.5.5 | In-guest VM reboot via libvirt's `dom.Reboot()` (ACPI signal to the running guest, no power cycle), exposed in API, CLI, and GUI. The existing `restart` flow is a stop+start cycle that power-cycles the QEMU process — slower, more disruptive, and renegotiates the libvirt domain. `reboot` keeps the domain alive and asks the guest OS to reboot itself, preserving the IP / MAC / DHCP reservation and avoiding a fresh cloud-init pass. New `Reboot(ctx, id)` on the `vm.Manager` interface, `POST /api/v1/vms/{id}/reboot` (returns 409 `vm_not_running` when the VM is not in a running state), `vmsmith vm reboot <id>`, and a Reboot button on VMDetail (visible only while running). Emits `vm.reboot_requested` for audit | S | ✅ Done — `internal/vm/lifecycle.go` adds `LibvirtManager.Reboot` (libvirt state pre-flight, then `dom.Reboot(0)`); `internal/vm/mock_manager.go` mirrors the state guard with a `RebootErr` injection hook; `internal/vm/quota_manager.go` forwards the new method; `internal/api/handlers_vm.go` adds `RebootVM`, the router exposes `POST /vms/{vmID}/reboot`. CLI ships `vmsmith vm reboot <id>`. The frontend `vms.reboot` client method drives a `RefreshCw`-icon VMDetail button using the regenerated OpenAPI types. Coverage: `TestMockManager_Reboot` + `_NotRunning` + `_NotFound` + `_ErrorInjection`, API `TestRebootVM` + `_NotRunning_Returns409` + `_NotFound_Returns404` + `_Error`, CLI `TestCLI_VMReboot` + `_NotRunning` + `_NotFound`, mock-server stub, and a Playwright `reboot running VM keeps it running` scenario |

---

## Phase 3: Operational Excellence (Week 5-8)

Features for running VMSmith in production or team environments.

### 3.1 Authentication & Authorization

The API is completely open. This blocks any multi-user or network-exposed deployment.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 3.1.1 | Add API key authentication middleware (check `Authorization: Bearer <key>` header) | M | ✅ Done — API routes now enforce `Authorization: Bearer <key>` when `daemon.auth.enabled` is true |
| 3.1.2 | Add `daemon.auth.enabled` and `daemon.auth.api_keys` config fields | S | ✅ Done — config loader, example config, and tests cover `daemon.auth.enabled` / `daemon.auth.api_keys` |
| 3.1.3 | Add `--api-key` flag to CLI commands for remote daemon usage | S | ✅ Done — CLI now exposes a persistent `--api-key` flag that adds `Authorization: Bearer <key>` for HTTP-based remote daemon operations such as `image pull http://...` |
| 3.1.4 | Add login screen to frontend when auth is enabled | M | ✅ Done — the embedded web UI now prompts for an API key after a 401, stores it in `localStorage`, and retries authenticated API calls without a full reload |
| 3.1.5 | (Future) Role-based access: `admin` (full), `operator` (start/stop/list), `viewer` (read-only) | L | Optional follow-up; start with single-role API keys |

### 3.2 TLS / HTTPS Support

Required for any non-localhost deployment.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 3.2.1 | Add `daemon.tls.cert_file` and `daemon.tls.key_file` config fields | S | ✅ Done — `internal/config/config.go` defines `daemon.tls.cert_file` / `key_file`, and `internal/config/config_test.go` covers loading them from YAML |
| 3.2.2 | Switch `http.ListenAndServe` to `http.ListenAndServeTLS` when TLS configured | S | ✅ Done — `internal/daemon/daemon.go` switches to `ListenAndServeTLS` when both TLS files are configured, with daemon tests covering both HTTP and HTTPS paths |
| 3.2.3 | Add `daemon.tls.auto_cert` option for Let's Encrypt via `autocert` package | M | ✅ Done — `internal/config/config.go` adds `daemon.tls.auto_cert` / `auto_cert_cache_dir`, `internal/daemon/daemon.go` wires `autocert.Manager` into the HTTPS server for a single public FQDN, and tests/docs/examples cover the new config |
| 3.2.4 | Document reverse proxy setup (nginx/caddy) as alternative to built-in TLS | S | ✅ Done — `docs/PRODUCTION_DEPLOYMENT.md` covers reverse proxy deployment with both nginx and Caddy, TLS, and firewall guidance |

### 3.3 Systemd Integration

Make VMSmith a proper system service.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 3.3.1 | Create `vmsmith.service` systemd unit file | S | ✅ Done — `vmsmith.service` is committed at the repo root with `network-online.target` + `libvirtd.service` ordering, runtime directory settings, and installable defaults for `/etc/vmsmith/config.yaml` |
| 3.3.2 | Add `make install-service` target to copy unit file and enable service | S | ✅ Done — `make install-service` now installs `vmsmith.service` into `/etc/systemd/system`, reloads systemd, and enables/starts the unit |
| 3.3.3 | Add `vmsmith daemon status` command (check if daemon is running) | S | ✅ Done — `internal/cli/daemon.go` implements `vmsmith daemon status`, and the command is documented in `README.md` |
| 3.3.4 | Implement graceful shutdown: drain in-flight requests, close libvirt connection cleanly | M | ✅ Done — API router now rejects new requests with HTTP 503 during shutdown while in-flight requests drain, and daemon cleanup closes VM manager, network manager, store, and logger resources in order |
| 3.3.5 | `vmsmith host stats` + `vmsmith host quotas` CLI — thin HTTP clients for `GET /api/v1/host/stats` and `GET /api/v1/quotas/usage`. Same data that already powers the Dashboard cards (VM count, CPU/RAM/Disk utilisation, active SSE clients, current allocation vs configured quota caps), now reachable from a shell or cron job without scraping the web UI. Both subcommands reuse the persistent `--api-url` / `--api-key` (and `$VMSMITH_API_KEY`) plumbing established by `vmsmith logs list` / `vmsmith webhook list`, render a human-readable table by default, and accept `--json` for scripting. Closes the API↔CLI parity gap for the host-capacity surface | S | ✅ Done — `internal/cli/host.go` adds the `host` parent command + `stats` / `quotas` subcommands, sharing a small `hostGET` helper for URL resolution + Authorization plumbing + daemon-error propagation; `formatBytes` mirrors the GUI's binary-unit rendering (`8.0 GiB`, `1.0 TiB`) so CLI and web UI agree on units; `quotaLimit` renders `unlimited` when the daemon reports `limit: 0` so uncapped resources are obvious. Coverage: `internal/cli/host_test.go` × 11 (happy path, `--json` pass-through, Authorization header, daemon-error propagation for `host stats`; happy path, unlimited-cap rendering, `--json` pass-through, Authorization header, daemon-error propagation for `host quotas`; plus `formatBytes` / `quotaLimit` table-driven unit tests). CLAUDE.md REST quick reference and the `internal/cli/` directory listing extended accordingly |

### 3.4 API Rate Limiting & Request Size Limits

Prevent abuse and resource exhaustion.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 3.4.1 | Add per-IP rate limiting middleware (token bucket, configurable rate) | M | ✅ Done — `/api/v1/*` now uses configurable per-client token buckets via `daemon.rate_limit_per_second` / `daemon.rate_limit_burst`, returning HTTP 429 `rate_limit_exceeded` plus `Retry-After` |
| 3.4.2 | Add configurable max request body size (default 50MB, override for image uploads) | S | ✅ Done — added `daemon.max_request_body_bytes` and `daemon.max_upload_body_bytes`, applied request-size middleware, and covered 413 behavior in API tests |
| 3.4.3 | Add concurrent VM creation limit (prevent fork-bombing the host) | S | ✅ Done — `daemon.max_concurrent_creates` bounds simultaneous `POST /api/v1/vms` operations and returns HTTP 429 `create_limit_reached` when the queue is full |

### 3.5 Resource Quotas

Prevent VMs from consuming all host resources.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 3.5.1 | Add `quotas.max_vms`, `quotas.max_total_cpus`, `quotas.max_total_ram_mb`, `quotas.max_total_disk_gb` config fields | S | ✅ Done — added `QuotasConfig` to `internal/config/config.go`, covered defaults/YAML loading in `internal/config/config_test.go`, and documented the new config block in README + deployment docs |
| 3.5.2 | Check quotas before VM create and VM update; return 429 or 403 if exceeded | M | ✅ Done — quota wrapper sums current allocations from `List()`/`Get()` and returns HTTP 429 `quota_exceeded` |
| 3.5.3 | Show quota usage in dashboard (e.g., "12/32 CPUs allocated") | S | ✅ Done — dashboard now polls `GET /api/v1/quotas/usage` and shows current allocation vs limit |

---

## Phase 4: Monitoring & Observability (Week 7-10)

### 4.1 VM Resource Metrics

VM metrics are now broadly shipped across the API, CLI, Prometheus, and frontend charts. The remaining work in this track is mostly soak and end-to-end validation, plus any follow-up polish that falls out of real-world usage.

#### 4.1.0 Architectural overview

**Sampling.** A single goroutine in a new `internal/metrics/` package polls libvirt's bulk stats API every `daemon.metrics.sample_interval` (default 10s) using `Connect.GetAllDomainStats(stats, flags)` with the bitmask `STATS_STATE | STATS_CPU_TOTAL | STATS_BALLOON | STATS_VCPU | STATS_INTERFACE | STATS_BLOCK`. One bulk call returns stats for every running domain in milliseconds — far cheaper than per-VM calls.

**Counter → rate conversion.** libvirt returns cumulative counters (CPU nanoseconds, bytes in/out, blocks read/written). Charts need rates. The collector keeps the previous sample in memory and computes deltas:

| Metric | libvirt field | Computed |
|---|---|---|
| CPU % | `cpu.time` (ns, cumulative) | `(Δns / Δt_ns / vcpus) * 100` clamped to [0, 100*vcpus] |
| RAM used MB | `balloon.rss` if present, else `balloon.current` | absolute |
| RAM available MB | `balloon.available - balloon.unused` (guest-agent dependent) | absolute, may be missing |
| Disk read B/s | `block.<n>.rd.bytes` summed across disks | `Δ / Δt_seconds` |
| Disk write B/s | `block.<n>.wr.bytes` summed | `Δ / Δt_seconds` |
| Net rx B/s | `net.<n>.rx.bytes` summed across interfaces | `Δ / Δt_seconds` |
| Net tx B/s | `net.<n>.tx.bytes` summed | `Δ / Δt_seconds` |

First sample after a VM starts produces no rate (no prior counter); the API marks it `null` and the chart skips. Counter resets (libvirt reports a smaller value than the prior sample — happens after VM restart) reset the baseline; rate for that interval is `null` rather than negative.

**In-memory ring buffer per VM.** A fixed-size circular buffer of `MetricSample` per VM ID:

```go
type MetricSample struct {
    Timestamp     time.Time `json:"timestamp"`
    CPUPercent    *float64  `json:"cpu_percent,omitempty"`     // pointer for null-on-reset
    MemUsedMB     *uint64   `json:"mem_used_mb,omitempty"`
    MemAvailMB    *uint64   `json:"mem_avail_mb,omitempty"`
    DiskReadBps   *uint64   `json:"disk_read_bps,omitempty"`
    DiskWriteBps  *uint64   `json:"disk_write_bps,omitempty"`
    NetRxBps      *uint64   `json:"net_rx_bps,omitempty"`
    NetTxBps      *uint64   `json:"net_tx_bps,omitempty"`
}
```

Buffer size = `daemon.metrics.history_size` (default 360 samples = 1 hour at 10s). Memory cost: ~80 bytes per sample × 360 × 100 VMs ≈ 2.8 MB worst case.

**Why in-memory only (v1).** Persisting metrics to bbolt is rejected for v1:
1. 10s sampling × 100 VMs × 30 days = 25.9 M samples per bucket. bbolt is not a TSDB; range scans get expensive and write amplification is high.
2. The compelling use case is "what is this VM doing right now / in the last hour" — covered by an in-memory ring.
3. Long-term metrics belong in a real TSDB. Defer to 4.1.6 (Prometheus exporter), which lets operators ship metrics to Prometheus/VictoriaMetrics/Grafana Cloud without us reinventing storage.

If durable metrics become a requirement before 4.1.6, the API shape stays the same — only the storage backend swaps.

**Stale-sample handling.** When a VM stops, its ring keeps the last samples but adds no new ones. `GET /api/v1/vms/{id}/stats` includes `state` and `last_sampled_at` in the response so the UI can mark the chart "VM stopped at <time>". Rings for deleted VMs are pruned on the next sweep (which checks each ring's owner against `Store.GetVM`). Rings for VMs that vanish from `GetAllDomainStats` for >5 minutes are also pruned to handle libvirt restarts.

**Metrics manager API.**
```go
type Manager interface {
    Start(ctx context.Context) error  // launches the sampler goroutine
    Stop() error
    Snapshot(vmID string) (*VMStatsSnapshot, error)  // current + history
    Subscribe(vmID string) (<-chan MetricSample, cancel func())  // for SSE / future events
}
```

The subscription path is what powers real-time charts: when 4.2 (events) lands, the metrics manager publishes `metrics.sample` events at a sampled-down rate (1/min) so the events stream stays a low-traffic audit log; the `Subscribe` path is the high-frequency path the SSE chart endpoint uses directly.

**API contract (`GET /api/v1/vms/{id}/stats`).**

```jsonc
{
  "vm_id": "vm-1741234567890123",
  "state": "running",
  "last_sampled_at": "2026-04-28T12:34:50Z",
  "current": { /* MetricSample */ },
  "history": [ /* MetricSample, oldest first */ ],
  "interval_seconds": 10,
  "history_size": 360
}
```

Query params:
- `?since=<rfc3339>` truncates `history` to samples after the timestamp.
- `?fields=cpu,mem` projects the response (cuts payload for the dashboard's compact charts; default = all).
- 404 with `resource_not_found` if VM doesn't exist; 200 with `state: "stopped"` and frozen history if VM exists but is stopped.

**Real-time stream (`GET /api/v1/vms/{id}/stats/stream`).** SSE, one frame per sample. Reuses the SSE machinery from 4.2.10. Frames carry the same `MetricSample` JSON. No replay needed (the REST history call provides initial backfill); on connect, the client sends the most recent `history` GET, then subscribes for new samples.

**Chart library choice.** uPlot (~45 KB gzipped) is preferred over recharts (~95 KB) for time-series — its canvas renderer handles 360+ points smoothly and re-renders at every new sample without React reconciliation pressure. Wrap in a `<MetricChart>` component to keep the dependency local.

**Privacy/perf considerations.**
- `balloon.available` requires the QEMU guest agent. When absent, return `null` rather than estimating; surface an info badge in the UI ("Install qemu-guest-agent for memory pressure metrics").
- VM owners should not see other VMs' metrics — once 3.1.5 RBAC lands, `/stats` enforces VM ownership. v1 (single-tenant API key) skips this check.
- Sampling adds ~5ms per call regardless of VM count (bulk API) but holds the libvirt connection mutex briefly. Don't go below 5s sampling without measuring under load.

#### 4.1.1 Task list

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 4.1.1 | Define `pkg/types/metrics.go` (`MetricSample`, `VMStatsSnapshot`) with all fields as pointers (nil = unavailable) | S | ✅ Done — `pkg/types/metrics.go` defines the VM metrics sample/snapshot contract used by the API and CLI |
| 4.1.2 | Create `internal/metrics/` package: sampler goroutine using `Connect.GetAllDomainStats`, per-VM ring buffer, counter→rate conversion with reset detection, sweep for stopped/deleted VMs. Unit tests with a mock libvirt-stats provider | L | ✅ Done — `internal/metrics/collector.go` polls bulk libvirt stats, maintains per-VM history, and is covered by unit tests for rate math and stale/reset behavior |
| 4.1.3 | Wire `metrics.Manager` into daemon startup; honor `daemon.metrics.enabled` (default true), `sample_interval` (10s), `history_size` (360) config fields. Document in `vmsmith.yaml.example` | S | ✅ Done — metrics manager startup/config wiring landed with disable-safe behavior for tests and non-libvirt environments |
| 4.1.4 | `GET /api/v1/vms/{id}/stats` endpoint with `?since=` and `?fields=` query params; 200/404/410-on-deleted contract above | M | ✅ Done — `internal/api/handlers_metrics.go` ships the REST stats endpoint with projection/filter coverage in `internal/api/metrics_test.go` |
| 4.1.5 | `GET /api/v1/vms/{id}/stats/stream` SSE endpoint reusing 4.2.10's SSE helper. Frame per sample. Closes on VM delete or daemon shutdown | M | ✅ Done — `internal/api/handlers_metrics.go` adds `StreamVMStats` which delivers a `vm.stats` SSE frame per new `MetricSample` (poll-on-change against the metrics ring), heartbeats every 30s, and closes when the VM disappears from the manager or the client disconnects. `internal/api/sse.go` was switched to `http.ResponseController` and `responseRecorder.Unwrap()` was added so SSE works through the request-logging middleware. Coverage in `internal/api/metrics_test.go` (`TestStreamVMStats_*`) |
| 4.1.6 | Prometheus `/metrics` endpoint exposing per-VM `vmsmith_vm_cpu_percent{vm="..."}` etc. plus host-level metrics. Allow scraping behind same auth or unauthenticated on a dedicated `daemon.metrics.scrape_listen` (e.g., `127.0.0.1:9101`) so Prometheus doesn't need an API key. Add deployment notes | M | ✅ Done — `/metrics` now exports latest in-memory VM gauges, `metrics.scrape_listen` can serve a dedicated scrape listener, and deployment/docs examples cover Prometheus scraping |
| 4.1.7 | Frontend: `web/src/components/MetricChart.jsx` using uPlot; `web/src/hooks/useVMStats.js` doing initial REST fetch + SSE subscription with polling fallback. Add 4 charts (CPU, RAM, disk, net) to VMDetail under a new "Metrics" tab | L | ✅ Done — `web/src/hooks/useVMStats.js` does the initial `/stats` REST fetch and then subscribes to `/stats/stream` via `EventSource` (with `?api_key=` for browser auth fallback), reconnects with exponential backoff, and falls back to short-poll mode after 3 consecutive failures. `web/src/components/MetricChart.jsx` wraps uPlot in a React component with a `ResizeObserver`-driven width handler. The Metrics tab on VMDetail renders four charts (CPU, Memory, Disk I/O read+write, Network rx+tx) plus a `LiveIndicator` so operators can see the live/polling state. Mock-server SSE endpoint and Playwright tests cover the chart mount, multi-series legend, and the `live` status transition |
| 4.1.8 | Frontend: dashboard "top 5 VMs by CPU" widget driven by an aggregator endpoint `GET /api/v1/vms/stats/top?metric=cpu&limit=5` (computed from latest sample per ring; no extra storage) | S | ✅ Done — `GET /api/v1/vms/stats/top` ranks running VMs by CPU/mem/disk/net using their latest in-memory sample, the dashboard exposes a metric-switchable "Top 5 Machines" leaderboard, and `vmsmith vm top --metric <m>` provides the same view in the CLI |
| 4.1.9 | CLI: `vmsmith vm stats <id> [--watch] [--fields cpu,mem]` — one-shot prints latest sample + 5-min averages; `--watch` streams via SSE and refreshes a tabular UI in place | S | ✅ Done — `internal/cli/vm.go` now provides one-shot and refresh-in-place `vm stats` output using the REST stats endpoint, including field filtering and command coverage |
| 4.1.10 | Tests: unit (rate math edge cases incl. counter reset / overflow / stopped VM), integration (`/stats` endpoint with stopped/missing/running VMs, SSE stream replay-not-supported behavior, Prometheus scrape format), E2E (real VM under load shows non-zero CPU + non-zero net during apt install) | L | E2E uses `stress-ng` over SSH to drive CPU; verify the chart hook reflects it within 30s |
| 4.1.11 | Docs: `docs/ARCHITECTURE.md` "Metrics" subsection (sampling, ring buffer, rate math, deferred persistence, Prometheus integration). Add to `docs/PRODUCTION_DEPLOYMENT.md` an example `prometheus.yml` scrape config | S | ✅ Done — architecture docs now describe the metrics pipeline/persistence model, and production docs include a sample Prometheus scrape job |

The original 4.1.4 (host-level stats on dashboard) is already done and remains in place above the table for context — kept here as the pre-existing baseline.

#### 4.1.2 Open architectural questions

1. **Guest-agent dependency.** RAM pressure metrics depend on `qemu-guest-agent` running inside the guest. Should the create flow install + enable it via cloud-init by default? Trade-off: smaller base image vs richer metrics out of the box. Recommendation: default-on for VMs created via vmsmith; document the cloud-init line so operators can opt out for embedded distros.
2. **Disaggregating disk/net metrics.** Right now we sum across all disks and interfaces. Once multi-NIC VMs exist (already supported), the chart loses signal. Add `?disaggregate=true` returning a per-device breakdown later.
3. **High-frequency sampling for short-lived spikes.** A 10s sample misses 1s CPU spikes. Adding adaptive sampling (5s when CPU > 80%, 10s otherwise) is tempting but doubles complexity. Defer; document as a known limitation.
4. **Bulk-stats RPC failure modes.** `GetAllDomainStats` can return partial results (some domains missing) when libvirt is under load. Detect and log when the returned VM count is less than the running-VM count, but don't gap-fill — let the chart show the gap.

#### 4.1.3 Dependencies

- **4.2 (events)** — required for 4.1.5 (SSE stream) and the `metrics.sample`-as-event flow. Without 4.2, fall back to polling for the frontend.
- **3.1 (auth)** — required before exposing `/stats` outside localhost; per-VM RBAC waits for 3.1.5.
- **5.5 (multi-host)** — Prometheus labels should include a `host` label from day one (`vmsmith_vm_cpu_percent{vm="...",host="..."}`) so a future multi-host setup can shard by host without renaming series.

### 4.2 Event System & Notifications

No way to know when a VM crashes, completes creation, or changes state. The work in this section is the foundation for several downstream features (audit log, dashboards without polling, schedules in 5.2, future alerting).

**Status (2026-05-08):** Broadly shipped. Events are now persisted, queryable via `GET /api/v1/events`, streamable via `GET /api/v1/events/stream`, surfaced in the CLI and web UI, and fanned out through the in-process bus to webhook delivery plus the Settings/test-delivery UX. The main remaining gaps in this track are the remaining integration/E2E test matrix (`4.2.17`) and the lingering libvirt-state refactor cleanup called out in `4.2.4`.

#### 4.2.0 Architectural overview

**Event taxonomy.** Three sources, all flowing through one in-process bus:

| Source | Origin | Examples |
|---|---|---|
| `libvirt` | `DomainEventLifecycleRegister` callback (already wired) | `vm.started`, `vm.stopped`, `vm.crashed`, `vm.shutdown`, `vm.suspended` |
| `app` | API/CLI mutating handlers, post-success | `vm.created`, `vm.cloned`, `vm.updated`, `vm.deleted`, `snapshot.created`, `snapshot.restored`, `image.uploaded`, `port_forward.added` |
| `system` | Daemon internals | `daemon.started`, `daemon.shutdown`, `quota.exceeded`, `dhcp.exhausted`, `webhook.delivery_failed` |

A typed `Event` record (new `pkg/types/event.go`):

```go
type Event struct {
    ID         string            `json:"id"`           // stringified uint64 today; opaque ordered ID for forward-compat with 5.5 multi-host
    Type       string            `json:"type"`         // e.g. "vm.started"
    Source     string            `json:"source"`       // "libvirt" | "app" | "system"
    VMID       string            `json:"vm_id,omitempty"`
    ResourceID string            `json:"resource_id,omitempty"` // generic (image/template/etc.)
    Severity   string            `json:"severity"`     // "info" | "warn" | "error"
    Message    string            `json:"message"`
    Attributes map[string]string `json:"attributes,omitempty"`
    OccurredAt time.Time         `json:"occurred_at"`
    Actor      string            `json:"actor,omitempty"` // API key alias or "system"
}
```

Outbound payloads include a top-level `schema_version: 1` so downstream consumers can detect breaking changes without sniffing fields.

**In-process event bus (new `internal/events/` package).**
- Single `EventBus` shared across the daemon. Producers call `Publish(ctx, Event)`; subscribers register via `Subscribe(filter) (<-chan Event, cancel func())`.
- Implementation: a fan-out broker with a slice of buffered subscriber channels (default 64) under `sync.RWMutex`. Slow subscribers are **dropped, not blocked** — losing a webhook target must never stall the libvirt event loop. Drops emit one `system`-source `subscriber_lagged` event with the subscriber name, throttled to 1/min.
- ID assignment is centralized in the bus using a monotonically increasing `uint64` counter, persisted in a `events_meta` bucket as 8-byte big-endian. On startup the counter is recovered as `max(persisted_next_id, last_event_id_in_bucket+1)`; persistence is best-effort, idempotent on replay.
- A single goroutine inside the bus consumes the publish channel and (1) writes to bbolt, (2) fans out to live subscribers. This guarantees the persisted log and SSE replay see events in the same order.

**Persistence (bbolt).**
- New bucket `events`, key = 8-byte big-endian `uint64` ID, value = JSON `Event`. Big-endian keys make BoltDB cursor iteration return events in chronological order — what every consumer wants for free.
- `events_meta` bucket stores `next_id` and the timestamp of the last retention sweep.
- No secondary indexes at v1: `?vm_id=` / `?type=` filters scan reverse-chronologically and short-circuit on `limit`. With caps below this is sub-millisecond. Add an `events_by_vm` (`{vm_id}/{id_be}` → empty) index later if profiling shows scan cost.
- Retention loop runs every 60s in a single Update tx: drop oldest until `count ≤ daemon.events.max_records` (default 50_000) and `age ≤ daemon.events.max_age` (default 720h / 30 days). Retention deletes are batched, capped at 5000/sweep so a backlog can't stall the writer.

**SSE protocol (`GET /api/v1/events/stream`).**
- Headers: `Content-Type: text/event-stream`, `Cache-Control: no-cache, no-transform`, `Connection: keep-alive`, `X-Accel-Buffering: no` (defeats nginx response buffering).
- Frame format: `id: <event_id>\nevent: <type>\ndata: <json>\n\n`. Always emit the JSON event body so frontends can ignore `event:` if they want a single handler.
- **Replay.** Honor `Last-Event-ID` request header; fall back to `?since=<id>` query param (browser `EventSource` cannot set custom headers). Replay is capped at `daemon.events.sse_replay_limit` (default 1000). If the client is further behind, return `410 Gone` with code `event_stream_replay_window_exceeded` so the client falls back to paginated REST.
- **Heartbeat.** 30s `: keepalive\n\n` comment frames defeat idle proxies and let the server detect dead connections via write failure.
- **Backpressure.** Per-connection buffered channel (64). On overflow: send a final `event: stream_lagged\n` frame and close. Silently dropping events is worse than terminating — clients can reconnect with the latest received `id`.
- **Lifecycle.** Track active connections in the SSE hub; expose count in `GET /api/v1/host/stats`. On `BeginShutdown`, send `event: shutdown` to all active connections and close.
- **Auth.** Default path uses the same `Authorization: Bearer` middleware as the rest of `/api/v1/*`. EventSource cannot send custom headers, so accept `?api_key=` as a same-origin fallback for the embedded GUI; rate-limit it, never log it, and document the tradeoff. Long-term: short-lived signed cookie issued at login (deferred to 3.1.5).

**Webhook delivery (new `internal/webhooks/`).**
- Configured per webhook (new `webhooks` bucket): `id`, `url`, `secret`, `event_types` (glob list, default `*`), `severity_floor`, `enabled`.
- Dispatcher subscribes to the bus, matches each event against enabled webhooks, and queues delivery in a bounded in-memory queue (1000). Overflow drops oldest and emits `webhook.delivery_failed` (system) with reason `queue_overflow`.
- **Retry policy.** 5 attempts with exponential backoff + jitter: 1s, 5s, 30s, 2m, 10m. Final failure emits a `webhook.delivery_failed` system event with the receiver's last status code and error string — visible in the events stream itself, so operators don't need a separate UI to debug.
- **Signing.** `X-VMSmith-Signature: sha256=<hex>` over the raw request body using the webhook secret (HMAC-SHA256). Also send `X-VMSmith-Event-Id`, `X-VMSmith-Event-Type`, `X-VMSmith-Timestamp` (Unix seconds) so receivers can route quickly and reject replays.
- **SSRF protection.** `daemon.webhooks.allowed_hosts` allowlist (domain or CIDR). Default deny-list always applied: `127.0.0.0/8`, `169.254.0.0/16`, `::1`, `fc00::/7`, `fe80::/10`, and the `192.168.100.0/24` VM NAT range. DNS resolution happens once per attempt and the resolved IP is checked against the deny-list before connecting (prevents DNS-rebinding round-trips).
- **Concurrency.** 4-worker pool. `http.Client` with 10s timeout; connections are not reused across webhooks (per-target `Transport`) to keep one slow receiver from starving the rest.

**Failure modes.**
| Failure | Behavior |
|---|---|
| Bolt write error in persister | log at `error`, increment `events_dropped_total`, do **not** block the producer. The libvirt event loop must keep running. |
| Subscriber channel full (slow consumer) | drop event for that subscriber only; emit throttled `subscriber_lagged` |
| SSE replay window exceeded | 410 with `event_stream_replay_window_exceeded`; client falls back to REST pagination |
| Webhook receiver down | retry per policy; persist final failure as a `webhook.delivery_failed` event |
| Daemon crash mid-publish | producer holds a copy until `Publish` returns; on graceful shutdown, drain the publish channel within `daemon.events.shutdown_timeout` (default 5s) before closing the store |

#### 4.2.1 Task list

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 4.2.1 | Subscribe to libvirt domain lifecycle events | M | ✅ Done — `internal/vm/events.go` registers libvirt lifecycle callbacks and now feeds the event bus-backed flow |
| 4.2.2 | Extend `pkg/types/event.go` to the full record above (`Source`, `Severity`, `Attributes`, `Actor`, `OccurredAt`, `ResourceID`) and add a top-level `schema_version` constant for outbound payloads | S | ✅ Done — `pkg/types/event.go` now carries the full record with additive `omitempty` JSON tags so existing events round-trip unchanged, plus an `EventSchemaVersion` constant |
| 4.2.3 | Create `internal/events/` package: `EventBus` with `Publish`/`Subscribe`/`Close`, ring-buffered fan-out, slow-subscriber drop with throttled `subscriber_lagged` warning, monotonic `uint64` ID assignment | M | ✅ Done — `internal/events/` now owns the bus, fan-out, ID assignment, and concurrency coverage |
| 4.2.4 | Refactor `internal/vm/events.go` to call `bus.Publish` for each lifecycle callback; move state mutation into a separate `persistVMState` consumer subscribed to `libvirt` events. Removes the direct `store.PutVM` call from the libvirt goroutine. | S | ✅ Done — `internal/vm/events.go` now publishes lifecycle events onto the bus without touching bbolt inline, and `internal/vm/state_consumer.go` persists `state` transitions asynchronously via `VMStatePersister`; coverage in `internal/vm/lifecycle_event_test.go` and `internal/vm/state_consumer_test.go` |
| 4.2.5 | Promote the existing `events` bucket to the indexed scheme: add `events_meta` (next_id), key events by big-endian `uint64` ID, expose `Store.ListEvents(filter, limit)`, `Store.EventByID(id)`, `Store.PruneEvents(maxRecords, maxAge)` | M | ✅ Done — `internal/store/bolt.go` now keys events by monotonic uint64 IDs, supports filtered listing / lookup, and implements both count-based and age-based pruning via `PruneEvents` and `PruneEventsByAge`, with store coverage in `internal/store/bolt_test.go` |
| 4.2.6 | Emit `app`-source events from API handlers: VM create / clone / update / delete, snapshot create / restore / delete, image upload / delete, port forward add / remove. Provide an `events.PublishAppEvent(ctx, type, vmID, attrs)` helper that pulls the actor from the auth middleware context | M | ✅ Done — mutating API handlers publish app events via the shared helper |
| 4.2.7 | Emit `system`-source events for daemon start / shutdown, quota exceeded, DHCP exhaustion, port-forward restore failure | S | ✅ Done — `daemon.started` (with listen + tls attrs) and `daemon.shutdown` are published from `internal/daemon/daemon.go`. Retention sweeps emit `events.retention_pruned`. `quota.exceeded` is emitted from the API quota validator with `field` / `attempted` / `limit` attrs whenever a create/update breaches a configured cap. `dhcp.exhausted` is emitted from `LibvirtManager` (create + clone paths) when the NAT DHCP range cannot satisfy a static-IP reservation. `port_forward.restore_failed` is emitted from `daemon.New()` when `PortForwarder.RestoreAll()` returns an error during startup |
| 4.2.8 | Retention loop (every 60s) honoring `daemon.events.max_records` (default 50_000) and `daemon.events.max_age` (default 720h); cap deletes per sweep at 5000; emit `system` event when retention drops events | S | ✅ Done — `internal/events/retention.go` now drives both `Store.PruneEvents(maxRecords)` and `Store.PruneEventsByAge(maxAge)` on every tick (default 30 days via `daemon.events.max_age_seconds`), each capped at 5000 deletes per sweep, and a single `events.retention_pruned` system event reports per-phase counts |
| 4.2.9 | Extend `GET /api/v1/events` with `?type=`, `?source=`, `?severity=`, `?until=<id>` filters (in addition to the existing `?vm_id=`, `?since=`, page / per_page), align replies to use `since`/`until` IDs alongside the current `since` timestamp, ensure same auth + rate-limit as other API routes | M | ✅ Done — event listing now supports the additional filters and `until` cursor alongside pagination |
| 4.2.10 | `GET /api/v1/events/stream` SSE endpoint: `Last-Event-ID` header support, `?since=` query fallback, 30s heartbeat, `daemon.events.sse_replay_limit` (default 1000), 410 with `event_stream_replay_window_exceeded` on overflow, `?api_key=` query auth fallback for browser EventSource. New `internal/api/sse.go` helper for headers / heartbeat / `http.Flusher` plumbing. Track active connection count and surface it in `host_stats` | L | ✅ Done — SSE replay, heartbeat, overflow handling, and EventSource auth fallback all live in `internal/api/sse.go` + `handlers_events.go`. The handler increments an atomic per-Server connection counter on entry / decrements on exit, and `GET /api/v1/host/stats` surfaces it as `event_stream_connections` (also exposed on the dashboard next to the LiveIndicator). Fixed: `responseRecorder` middleware now forwards `http.Flusher` so SSE works through the request-logger chain |
| 4.2.11 | Frontend: `web/src/hooks/useEventStream.js` — opens `EventSource`, handles reconnect with `Last-Event-ID`, falls back to polling on 410 / network error for 30s, exposes connection state. Replace polling on Dashboard and VMList with the hook; add a small "live" indicator | M | ✅ Done — `web/src/hooks/useEventStream.js` now drives the Dashboard and VM list live refresh path, reconnects with the last seen event ID, degrades to 10s polling for a bounded 30s window after repeated SSE failures, and surfaces the shared live-status pill. The Playwright suite covers the live indicator plus 410 fallback behavior in `tests/web/gui.spec.js`. |
| 4.2.12 | Frontend: "Activity" tab on VMDetail showing reverse-chronological event timeline filtered by `vm_id`, infinite scroll via `until=<id>` | M | ✅ Done — `web/src/pages/VMDetail.jsx` adds an Activity tab that embeds the timeline with `vm_id` pre-filtered, and Playwright coverage now exercises both populated and empty embedded states. Pagination is page-based (Prev/Next + per-page selector); `until=<id>` infinite scroll deferred until 4.2.10 ships |
| 4.2.13 | Frontend: top-level "Activity" page with filter chips (type / severity / source) and date-range picker, deep links via query params | M | ✅ Done — `web/src/pages/Activity.jsx` lists events with VM / source / severity / type filters; filters are mirrored to URL search params for deep links |
| 4.2.14 | CLI: `vmsmith events list [--vm <id>] [--type <t>] [--source <s>] [--severity <sev>] [--since <duration|id>] [--limit <n>]` (one-shot REST query) and `vmsmith events follow [--vm <id>] [--type <t>]` (SSE, prints events as they arrive, exits on Ctrl-C) | M | ✅ Done — `vmsmith events list` and `vmsmith events follow` are both shipped in `internal/cli/events.go`. `events follow` opens an SSE stream against `/api/v1/events/stream`, applies `--vm/--type/--source/--severity` filters client-side, reconnects with `Last-Event-ID` + `?since=` on transient errors, exits cleanly on Ctrl-C, and treats 401/410 as fatal so the user gets a clear error |
| 4.2.15 | Webhook subsystem: `webhooks` bbolt bucket; `POST/GET/DELETE /api/v1/webhooks` endpoints; HMAC-SHA256 signing; exponential backoff with jitter (1s/5s/30s/2m/10m); bounded queue (1000) with `queue_overflow` system event; SSRF protection via `daemon.webhooks.allowed_hosts` allowlist plus default deny-list of loopback / link-local / metadata / VM-NAT ranges; DNS resolution checked against deny-list before connect | L | ✅ Done — `internal/webhooks/` ships the manager, HMAC-SHA256 signer, SSRF deny-list (loopback / link-local / 169.254.169.254 metadata / 192.168.100.0/24 NAT) with `daemon.webhooks.allowed_hosts` opt-in, exponential backoff (1 s / 5 s / 30 s / 2 m / 10 m), bounded 1000-event per-webhook queue with `webhook.queue_overflow` on drop, and a `webhook.delivery_failed` system event when retries exhaust. CRUD lives at `POST/GET/DELETE /api/v1/webhooks`; matching CLI is `vmsmith webhook add/list/delete`. Secrets are redacted on read; coverage in `internal/webhooks/*_test.go` and `internal/api/handlers_webhook_test.go` |
| 4.2.16 | Webhook UI: list / create / delete in Settings page; "send test event" button (synthesizes a `system.webhook_test` event for that webhook only); show last delivery status, response code, and most recent failure reason | M | ✅ Done — new Settings page (`web/src/pages/Settings.jsx`) lists registered webhooks with their persisted `last_delivery_at` / `last_status` / `last_error`, lets operators add and delete hooks, and exposes a per-row "Test" button that calls a new `POST /api/v1/webhooks/{id}/test` endpoint. The endpoint synthesises a `system.webhook_test` event, delivers it once (no retries — UI wants a quick verdict), records the outcome on the webhook's `LastDelivery*` fields, and returns a `WebhookTestResult` with success / status / duration / error so the UI can surface success vs failure inline. Coverage: `internal/webhooks/manager_test.go` (success / failure / not-found / SSRF), `internal/api/handlers_webhook_test.go` (handler success / 404 / 503-when-no-tester), mock-server stubs in `tests/web/mock-server.js`, Playwright specs in `tests/web/gui.spec.js` + `tests/web/run-gui-tests.js` |
| 4.2.17 | Tests: unit (`EventBus` fan-out / slow-drop / ID monotonicity, Bolt persister round-trip, retention sweep, webhook signing + SSRF deny), integration (HTTP `/events` filtering + pagination, SSE replay + heartbeat + 410 + shutdown frame, webhook end-to-end with mock receiver covering retry + final failure event), E2E (start a real VM and assert `vm.started` arrives on the live SSE stream) | L | `httptest` SSE harness; mock HMAC verification example included so the test doubles as documentation. ✅ SSE replay-overflow 410 path is now correctly covered (`TestStreamEvents_ReplayOverflowReturnsGone` + `TestStreamEvents_ReplayOverflow_…ContentTypeIsJSON`) and live-after-replay delivery is exercised (`TestStreamEvents_LiveDeliveryAfterReplay`); webhook + retention unit + E2E real-VM bits still pending |
| 4.2.18 | Docs: new `docs/ARCHITECTURE.md` "Event System" section covering bus, persistence layout, SSE protocol (frame format + replay rules), webhook contract (payload shape + signature verification with a 6-line bash example) | S | ✅ Done — `docs/ARCHITECTURE.md` Section 5 ("Event System") covers the bus design, bbolt persistence layout, retention loop, REST + SSE protocol with replay/heartbeat/410 rules, and the webhook contract incl. an HMAC verification bash example; cross-linked from the REST API table, the data-model bucket table, and CLAUDE.md/README |
| 4.2.19 | Editable webhook configuration: `PATCH /api/v1/webhooks/{id}` + `vmsmith webhook edit` CLI + GUI edit modal on the Settings page. Mirrors the 2.2.10 / 2.2.11 / 2.2.12 metadata-edit pattern but for webhook receivers: URL, secret rotation, event-type filter list, and the active flag are all editable in place. Without this slice, operators had to delete and re-create a webhook to rotate a leaked secret, change a typo'd URL, or pause delivery during an incident — losing the `id`, the persisted delivery history, and any external bookkeeping pinned to that id | S | ✅ Done — `pkg/types/webhook.go` adds `WebhookUpdateSpec{URL,Secret,EventTypes,Active *…}` with pointer semantics (nil = leave unchanged; `event_types: []` clears the filter so the webhook fires on every event; empty secret rejected — secrets can be rotated but never cleared). `internal/api/handlers_webhook.go` ships `UpdateWebhook` which validates each field, persists the merged record, and bounces the in-memory delivery worker (Unregister + Register) so the next event is delivered with the new config. CLI ships `vmsmith webhook edit <id> [--url --secret --event-types --clear-event-types --active]` with `--event-types` / `--clear-event-types` mutually exclusive, and a "no fields to update" guard. The Settings page adds a Pencil-icon Edit button alongside Test / Delete, opening an `EditWebhookModal` that pre-fills the current URL + event-type filter, lets operators rotate the secret (blank = keep), toggle the "subscribe to every event" checkbox, and flip the active flag. Coverage: API integration (`TestUpdateWebhook_SetsURL`, `_RotatesSecret`, `_SetsEventTypes`, `_ClearsEventTypes`, `_TogglesActive`, `_RejectsEmptyBody`, `_RejectsInvalidURL`, `_RejectsEmptySecret`, `_NotFound`, `_NoOpWhenValueEqualsCurrent`, `_RejectsBadJSON`, `_ReactivatesStoppedWorker`); CLI (`TestCLI_WebhookEdit_SetsURL`, `_RotatesSecret`, `_TogglesActive`, `_SetsEventTypes`, `_ClearsEventTypes`, `_RejectsConflictingEventTypeFlags`, `_RejectsNoFlags`, `_PropagatesDaemonError`); mock-server PATCH route mirroring the same validation; Playwright (`edit webhook URL and event-type filter via PATCH`, `edit webhook can clear filter to subscribe-all`); OpenAPI documents the new path + `WebhookUpdateSpec` schema |
| 4.2.20 | Free-text `?search=` filter on `GET /api/v1/events` mirrored through the CLI (`vmsmith events list --search`, `events follow --search`) and the Activity page (debounced search box, URL round-trip). Case-insensitive substring match across `message`, `type`, `source`, `severity`, `actor`, `vm_id`, `resource_id`, and every value in `attributes`; the numeric event ID is intentionally excluded to avoid noisy matches on short numeric queries. Filters compose additively with the existing `vm_id` / `type` / `source` / `severity` / `since` / `until` query params. Rounds out the symmetric "list + filter" surface across VMs (`2.2.13`), images (`5.4.5`), snapshots (`5.4.6`), and templates (`5.4.7`). | S | ✅ Done — `pkg/types/event_search.go` exposes `EventMatchesSearch(evt, lowercased)`; `store.EventFilter.Search` runs the predicate inside the existing cursor walk; `internal/api/handlers_events.go` lowercases + trims the needle once before delegating; `internal/cli/events.go` adds `--search` to both `events list` and `events follow`; `web/src/pages/Activity.jsx` adds a 250 ms-debounced search input with an `X` clear control and URL round-trip via `?search=`. Coverage: unit (`TestEventMatchesSearch_*` × 8 — empty / nil / message / type / attribute / actor+vm_id+resource_id / caller-lowercases contract / no-match), API integration (`TestListEvents_FilterBySearch_*` × 7 — message / attribute value / actor / case-insensitive / whitespace-trim / no-match / composes with `?source=` / composes with `?vm_id=`), CLI (`TestCLI_EventsList_FilterBySearch_*` × 5 — message / attribute / case-insensitive / no-match / composes with `--source`), and Playwright (`search input filters the activity timeline and round-trips through the URL`, `search input matches no events when query has no hits`, `search input matches against attribute values`). Mock-server (`tests/web/mock-server.js`) mirrors the same predicate. `docs/openapi.yaml` documents the new `search` query parameter. CLAUDE.md REST quick reference gains a `GET /events` row. |
| 4.2.21 | `vmsmith webhook test <id>` CLI — thin HTTP client for `POST /api/v1/webhooks/{id}/test` so operators can verify receiver connectivity, HMAC signing, and 2xx-ness from a shell or cron job without scraping the Settings page's per-row Test button. Closes the API↔CLI parity gap on the webhook surface: every other webhook endpoint (`POST/GET/PATCH/DELETE /webhooks`, `POST /webhooks/bulk_delete`) is already reachable from `vmsmith webhook add/list/edit/delete`; `webhook test` was the last verb only the GUI could reach. Exits non-zero on delivery failure so it composes with shell pipelines (`vmsmith webhook test wh-1234 \|\| alert-oncall "Slack webhook down"`); `--json` for raw `WebhookTestResult` pass-through (matches the `host stats` / `host quotas` flag shape). Reuses the persistent `--api-url` / `--api-key` (and `$VMSMITH_API_KEY`) pattern established by `host stats` (3.3.5), `logs list` (5.4.13), and `webhook list` (5.4.14) | S | ✅ Done — `internal/cli/webhook.go` adds `webhookTestCmd` (`cobra.ExactArgs(1)`, POSTs to `/api/v1/webhooks/{id}/test`, unmarshals `WebhookTestResult`, prints an `OK …` / `FAIL …` table with status / duration / attempted / event / error, returns `fmt.Errorf("test delivery failed")` when `result.Success == false`). Coverage: `internal/cli/commands_test.go` × 7 (`TestCLI_WebhookTest_Success`, `_FailureExitsNonZero`, `_JSONFlagEmitsRaw`, `_ForwardsAuthHeader`, `_NotFoundPropagatesDaemonError`, `_ServiceUnavailablePropagates`, `_RequiresID`). CLAUDE.md REST quick reference for `POST /webhooks/{id}/test` cross-links to the new CLI command |
| 4.2.22 | Event details surface (actor + resource_id + attributes) across CLI and GUI. The events API has always returned these structured-detail fields, but `vmsmith events list`/`events follow` and the Activity table in the web UI only rendered the 6-column TIME/SEVERITY/SOURCE/TYPE/VM/MESSAGE shape — operators triaging a `vm.create_failed` event had to read raw JSON from `curl` to see the `error=DHCP exhausted` or the API-key alias that initiated it. CLI: opt-in `--show-actor` and `--show-attrs` flags on both `events list` and `events follow` add an ACTOR column (between TYPE and VM) and a trailing ATTRIBUTES column (folds in `resource_id=<id>` + sorted `key=value` pairs). Default-off so existing scripted callers grepping for the legacy header keep working. GUI: each Activity row gets a chevron toggle that reveals a details sub-row showing actor, resource_id, and the attributes key/value table. The toggle is suppressed for events with no structured detail so the table stays compact for noisy system-emitted events. No backend changes — the API surface was already complete. | S | ✅ Done — `internal/cli/events.go` adds `--show-actor` / `--show-attrs` to both `eventsListCmd` and `eventsFollowCmd` (renamed from the bool flags' initial `--actor` / `--attrs` names by 4.2.23 to free up `--actor` for the new value-flag filter — symmetric with every other filter flag like `--vm` / `--source` / `--severity`). Shared `writeEventsHeader` + `writeEventRow` + `formatEventAttributes` helpers keep list and follow output identical so a tee'd capture survives the swap from `list` to `follow`. The attributes column folds in `resource_id` (when non-empty) ahead of alphabetised `key=value` tokens so a single column carries every structured field. `web/src/pages/Activity.jsx` extracts the row into a new `ActivityRow` component that owns its own `expanded` state, renders a `ChevronRight`/`ChevronDown` toggle when there's something to disclose, and emits dedicated `activity-row-toggle-<id>` / `activity-details-<id>` / `activity-detail-actor-<id>` / `activity-detail-attrs-<id>` test ids for stable Playwright assertions. The details row is suppressed entirely for events with no actor/resource_id/attributes so the table stays compact. Coverage: CLI (`TestCLI_EventsList_DefaultHidesActorAndAttrsColumns`, `_ShowActorFlagAddsActorColumn`, `_ShowAttrsFlagAddsAttributesColumn`, `_ShowActorAndShowAttrsBothEnabled` for column order, `_AttrsDashOnEmptyAttributes`, `TestFormatEventAttributes` × 5 cases for the folding helper, `TestCLI_EventsFollow_AttrsAndActorFlagsAddColumns` exercising the SSE path); Playwright (`expand reveals actor + resource_id + attributes for events that have them`, `events with no structured details do not render a disclosure toggle`, `row click toggles the details row when details are available`). |
| 4.2.23 | Per-actor event filter — extends `GET /api/v1/events` with `?actor=<name>` exact-match against the event's structured `Actor` field, mirrored through the CLI (`vmsmith events list --actor <name>` and `events follow --actor <name>`) and a new debounced "Filter by actor" input on the Activity page (URL round-trip via `?actor=`). Actor values are case-sensitive (mirrors `?vm_id=`'s contract for the same reason — operators reference exact alias strings they registered, and case-insensitive matching is the job of `?search=`); whitespace is trimmed before comparison; empty disables the filter. Composes additively with the existing `vm_id` / `type` / `source` / `severity` / `since` / `until` / `search` query params so `X-Total-Count` reflects the post-filter population. Closes the "show me only the events `system` emitted" / "what did the deploy bot do in the last hour" operator query that the existing free-text `?search=` filter (4.2.20) covers fuzzily but not precisely — operators tailing one human / bot don't want the search box to also match every event whose message or attribute happens to mention the alias. As part of this slice the CLI column-toggle bool flags from 4.2.22 are renamed `--actor` → `--show-actor` and `--attrs` → `--show-attrs` so the natural value-flag name `--actor <name>` can take the filter role (matches `--vm` / `--source` / `--severity` / `--type`). | S | ✅ Done — `internal/store/bolt.go` adds `Actor string` to `EventFilter` and a single-line predicate inside the existing cursor walk; `internal/api/handlers_events.go` reads `?actor=` once (trimmed but not lowercased so the case-sensitive contract is preserved) and forwards to `store.EventFilter.Actor`; `internal/cli/events.go` adds `--actor` to both `eventsListCmd` and `eventsFollowCmd` and a corresponding `actor` field on the shared `eventFilter` struct so both the local-store list path and the SSE follow path apply the predicate identically. `web/src/pages/Activity.jsx` adds an `actorInput` / `actorFilter` 250 ms-debounced text input alongside the existing VM-ID input with a clear `X` button and URL round-trip via `?actor=`; `web/src/api/client.ts` extends `events.list({ actor })` to forward the param. Coverage: 6 `ListEventsFiltered` unit cases (`TestListEventsFiltered_Actor_ExactMatch`, `_CaseSensitive`, `_EmptyDisablesFilter`, `_NoMatchReturnsEmpty`, `_ComposesWithSourceAndSeverity`, `_MatchesEmptyActor`); 6 API integration cases (`TestListEvents_FilterByActor_ExactMatch`, `_CaseSensitive`, `_WhitespaceTrimmed`, `_NoMatchReturnsEmpty`, `_EmptyParamIsNoOp`, `_ComposesWithSearch`, `_TotalCountReflectsFiltered`); 4 CLI cases (`TestCLI_EventsList_FilterByActor_ExactMatch`, `_CaseSensitive`, `_TrimmedAndEmpty`, `_ComposesWithSource`); 1 SSE-follow case (`TestCLI_EventsFollow_FiltersByActor`); Playwright (`actor input filters the activity timeline and round-trips through the URL`, `actor input narrows further when combined with the source dropdown`, `actor clear button restores the unfiltered view`). Mock-server (`tests/web/mock-server.js`) mirrors the exact-match predicate. `docs/openapi.yaml` documents the new `actor` query parameter; CLAUDE.md REST quick reference for `GET /events` updated accordingly. |
| 4.2.24 | Per-resource_id exact-match filter on events. Extends `GET /api/v1/events` with `?resource_id=<id>` exact-match against the event's structured `ResourceID` field, mirrored through `vmsmith events list --resource-id` / `events follow --resource-id` and a new debounced "Filter by resource ID" input on the Activity page (URL round-trip via `?resource_id=`). Whitespace is trimmed before comparison; matching is case-sensitive — mirrors the `?vm_id=` / `?actor=` contracts because operators reference opaque server-issued IDs (e.g. `snap-1747...`, `img-1747...`, `wh-...`) verbatim, and case-insensitive matching is the job of `?search=`. Composes additively with the existing `vm_id` / `type` / `source` / `severity` / `actor` / `since` / `until` / `search` query params so `X-Total-Count` reflects the post-filter population. Closes the "show me everything the platform did to snap-1747591234" / "what's the lifecycle of img-rocky9" operator query that the existing free-text `?search=` filter (4.2.20) covers fuzzily but not precisely — operators tailing one resource don't want the search box to also match every event whose message or attribute happens to mention a short numeric substring of the ID. | S | ✅ Done — `internal/store/bolt.go` adds `EventFilter.ResourceID string` plus a single-line predicate inside the existing cursor walk (right after the `Severity` filter so the predicates stay grouped). `internal/api/handlers_events.go` reads `?resource_id=` once (trimmed but not lowercased so the case-sensitive contract is preserved) and forwards it on the filter struct. `internal/cli/events.go` adds `--resource-id` to both `eventsListCmd` and `eventsFollowCmd` and a corresponding `resourceID` field on the shared `eventFilter` struct so both the local-store list path and the SSE follow path apply the predicate identically. `web/src/pages/Activity.jsx` adds an `resourceIdInput` / `resourceIdFilter` 250 ms-debounced text input alongside the existing VM-ID input with a clear `X` button and URL round-trip via `?resource_id=`. `web/src/api/client.ts` extends `events.list({ resourceId })` to forward the param; `tests/web/mock-server.js` mirrors the exact-match predicate and seeds two events with `resource_id` so Playwright can validate the filter. `docs/openapi.yaml` documents the new `resource_id` query parameter on `GET /events`. Coverage: store unit (`TestListEventsFiltered_ResourceID_ExactMatch`, `_CaseSensitive`, `_EmptyDisablesFilter`, `_NoMatchReturnsEmpty`, `_ComposesWithSeverity`, `_MatchesEmptyResourceIDOnly`); API integration (`TestListEvents_FilterByResourceID_ExactMatch`, `_CaseSensitive`, `_WhitespaceTrimmed`, `_NoMatchReturnsEmpty`, `_EmptyParamIsNoOp`, `_ComposesWithSeverity`, `_TotalCountReflectsFiltered`); CLI list (`TestCLI_EventsList_FilterByResourceID_ExactMatch`, `_CaseSensitive`, `_TrimmedAndEmpty`, `_ComposesWithSource`); CLI follow (`TestCLI_EventsFollow_FiltersByResourceID`); Playwright (`resource_id input filters the activity timeline and round-trips through the URL`, `resource_id filter matches no events when target is unknown`, `resource_id filter narrows further when combined with the source dropdown`). |
| 4.2.25 | Per-type-prefix event filter — extends `GET /api/v1/events` with `?type_prefix=<prefix>` doing a case-insensitive prefix match on the event's `Type` field, mirrored through the CLI (`vmsmith events list --type-prefix`, `events follow --type-prefix`) and a new debounced "Type prefix" input on the Activity page (URL round-trip via `?type_prefix=`). Lets operators slice an entire event family — `?type_prefix=snapshot.` matches every `snapshot.*` subtype, `?type_prefix=vm.` matches every `vm.*` subtype, `?type_prefix=webhook.` matches every webhook lifecycle event — without listing each fully-qualified type name through repeated `?type=` exact-match queries. Whitespace is trimmed and the prefix is lowercased before comparison; empty value disables the filter. Composes additively with every other event filter (`vm_id` / `type` / `source` / `severity` / `since` / `until` / `search`) so `X-Total-Count` reflects the post-filter population. Closes the "show me everything platform did to snapshots" / "tail every webhook delivery attempt" operator query that the existing free-text `?search=` filter (4.2.20) covers fuzzily but not precisely — operators tailing one resource class don't want the search box to also match every event whose message or attribute happens to mention the family name. | S | ✅ Done — `internal/store/bolt.go` adds `EventFilter.TypePrefix` + a single-line predicate inside the existing cursor walk (right after the `Severity` filter so the predicates stay grouped). `internal/api/handlers_events.go` reads `?type_prefix=` once (trimmed + lowercased so the caller-lowercases contract documented on the store struct is honoured) and forwards on the filter struct. `internal/cli/events.go` adds `--type-prefix` to both `eventsListCmd` and `eventsFollowCmd`, plus a `typePrefix` field on the shared `eventFilter` struct so the local-store list path and the SSE follow path apply the predicate identically. `web/src/pages/Activity.jsx` adds a 250 ms-debounced "Type prefix" input alongside the existing VM-ID input with an `X` clear button and URL round-trip via `?type_prefix=`. `web/src/api/client.ts` extends `events.list({ typePrefix })` to forward the param. `tests/web/mock-server.js` mirrors the same case-insensitive prefix predicate. `docs/openapi.yaml` documents the new `type_prefix` query parameter. Coverage: 6 store unit cases (`TestListEventsFiltered_TypePrefix_ExactClassMatch`, `_CaseInsensitive`, `_EmptyDisablesFilter`, `_NoMatchReturnsEmpty`, `_ComposesWithSource`, `_FullTypeStillMatchesItself`); 8 API integration cases (`TestListEvents_FilterByTypePrefix_MatchesEntireFamily`, `_CaseInsensitive`, `_WhitespaceTrimmed`, `_EmptyParamIsNoOp`, `_NoMatchReturnsEmpty`, `_ComposesWithSource`, `_ComposesWithSearch`, `_TotalCountReflectsFiltered`); 5 CLI list cases (`TestCLI_EventsList_FilterByTypePrefix_MatchesFamily`, `_CaseInsensitive`, `_EmptyOmitsFilter`, `_NoMatchPrintsEmpty`, `_ComposesWithSource`); 2 CLI follow cases (`TestFollow_FiltersByTypePrefix`, `_CaseInsensitive`); 4 `TestMatchesEventFilter` table cases (family match, full-type match, mismatch, case-insensitive); 3 Playwright cases (`type-prefix input filters the activity timeline and round-trips through the URL`, `type-prefix filter is case-insensitive`, `type-prefix filter matches no events when no type starts with the value`). CLAUDE.md REST quick reference for `GET /events` updated accordingly. |

#### 4.2.2 Open architectural questions

These are deliberately deferred — flag them in the PRs that touch them.

1. **Pre-shutdown drain semantics.** Does `BeginShutdown` stop emitting `app` events (rejecting state-changing requests already returns 503) or only stop accepting new HTTP requests? Recommendation: keep emitting for in-flight requests, then close the bus after the libvirt connection is closed so terminal `vm.stopped`/`daemon.shutdown` events are recorded.
2. **SSE auth for the embedded UI.** Once 3.1 auth is enabled, `EventSource` cannot send custom headers. The `?api_key=` fallback is acceptable for same-origin GUI but should be rate-limited and never written to the request log. Long-term: short-lived signed session cookie (move to 3.1.5).
3. **Webhook payload stability.** Once webhooks ship, the outbound JSON is part of the public contract. Reserve the right to add fields, but never rename or remove. `schema_version: 1` from day one.
4. **Multi-host (5.5) implications.** When events come from multiple hosts, IDs need either a central allocator or a per-host namespace (`{host_id}-{seq}`). Modeling `Event.ID` as a `string` from v1 keeps that option open even though v1 always emits stringified `uint64`.
5. **Cross-cutting actor capture.** 4.2.6's `PublishAppEvent` helper assumes auth middleware has already populated a request-scoped context value. If auth is disabled (`daemon.auth.enabled: false`), `Actor` should be `"anonymous"` — not blank — so audit queries can distinguish "no auth configured" from "system-emitted".

#### 4.2.3 Dependencies

- **3.1 (auth)** — must be in place before exposing webhook config (admin-only) and before the SSE auth fallback design is finalized. The events system itself does not block on auth, but webhook UX does.
- **3.4 (rate limiting)** — `?api_key=` SSE fallback should reuse the existing per-IP token bucket and add a separate stricter bucket for unauthenticated `/events/stream` connection attempts.
- **4.1 (metrics)** — share the in-process retention pattern. The active-SSE-connection gauge belongs in `host_stats`. Consider unifying `events_dropped_total` and `webhook_*` counters once a metrics package exists.
- **5.2 (schedules)** — `schedule.fired` and `schedule.failed` are `app`-source events; design 4.2.6's helper to be schedule-friendly (accept a synthetic actor like `"scheduler"`).

### 4.3 OpenAPI / Swagger Spec

API documentation is hand-written in ARCHITECTURE.md. Auto-generated spec enables client SDKs and API explorers.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 4.3.1 | Add OpenAPI 3.0 annotations to all handlers (or generate from types) | L | ✅ Done — added hand-written `docs/openapi.yaml` covering the implemented `/api/v1` endpoints, shared schemas, pagination headers, and bearer-auth requirements |
| 4.3.2 | Serve Swagger UI at `/api/docs` | S | ✅ Done — added embedded docs handlers that serve Swagger UI at `/api/docs` and the OpenAPI schema at `/api/openapi.yaml` |
| 4.3.3 | Generate TypeScript API client from OpenAPI spec for frontend | M | ✅ Done — `web/src/api/generated/schema.d.ts` is generated from `docs/openapi.yaml`, `web/src/api/client.ts` now uses `openapi-fetch`, and `npm run generate:api` refreshes the frontend API types |

---

## Phase 5: Advanced Features (Week 10+)

Larger features for power users and advanced use cases.

### 5.1 VNC Console Access

VNC is already configured in domain XML (`internal/vm/domain.go:60` — `<graphics type='vnc' port='-1' autoport='yes' listen='127.0.0.1'/>`) but not exposed. The work below adds a browser-based console without ever opening the VNC port to the network.

#### 5.1.0 Architectural overview

**Threat model.** The VNC server is bound to `127.0.0.1` on the host. There is no VNC password by default and the RFB protocol is not encrypted. Two non-negotiable invariants:

1. The VNC TCP port must **never** be reachable from outside the host. All access goes through the authenticated, TLS-terminated daemon.
2. A console session must be authorized to the specific VM, time-limited, and revocable.

**Proxy architecture.**
- Frontend opens `WebSocket /api/v1/vms/{id}/console?ticket=<one-time-token>` from the browser.
- Daemon's websocket handler validates the ticket, looks up the live VNC TCP port (`virsh domdisplay` or libvirt `Domain.GetXMLDesc` → parse `<graphics port='...'/>`), and pipes bytes between the websocket frame stream and `net.Dial("tcp", "127.0.0.1:<vnc_port>")`.
- Subprotocol negotiation: `Sec-WebSocket-Protocol: binary` — noVNC sends and expects raw RFB bytes inside binary websocket frames.
- Bidirectional copy uses two goroutines (`ws→tcp`, `tcp→ws`) joined on first close. Set both directions to a 30s idle write deadline; the noVNC client sends framebuffer-update requests on activity, so a long idle indicates a wedged connection.
- The handler holds a `Conn` reference in a per-VM "active console session" map so admin-initiated VM stop / delete can `Close()` the websocket cleanly (`event: console.session_terminated`).

**One-time ticket flow.** EventSource and WebSocket both have weak custom-header support in browsers, and embedding API keys in URLs leaks them into proxy logs. The flow:

1. Client `POST /api/v1/vms/{id}/console/ticket` with normal `Authorization: Bearer` auth → response `{ "ticket": "...", "expires_at": "...", "websocket_url": "wss://.../api/v1/vms/{id}/console?ticket=..." }`.
2. Tickets are 32 random bytes (base64url-encoded), single-use, valid for 60s, scoped to the VM ID and the issuing API key. Stored in an in-memory map (`map[string]ticket{vmID, expires, apiKey}`) under `sync.RWMutex` with a janitor goroutine sweeping every 30s.
3. Websocket handler consumes the ticket on connect (delete-on-read), validates VM ID match, then upgrades. After upgrade, the ticket is gone — refresh requires a new POST.

**VNC password handling (optional, per-VM).**
- New `vnc_password` field on `types.VM` (write-only via API: accepted on create/update, redacted on read; persisted in bbolt as bcrypt-hashed for storage and reversibly encrypted for the libvirt domain XML using a daemon-level key from `daemon.console.password_key`).
- When set, regenerate domain XML with `<graphics type='vnc' port='-1' autoport='yes' listen='127.0.0.1' passwd='<plain>'/>` and apply on next start.
- The proxy still requires the ticket. The VNC password is a defense-in-depth layer for the rare case where the daemon socket is mis-bound.
- The proxy does **not** transparently inject the password — the noVNC client prompts the user. (Auto-injection would defeat the second factor.)

**Frontend integration.**
- Bundle noVNC's `core/rfb.js` (~200KB minified) under `web/src/vendor/novnc/`. Pin a specific version; do not load from CDN.
- New `web/src/pages/VMConsole.jsx` opens at `/vms/{id}/console`. On mount: POST for ticket → instantiate `RFB(canvas, websocketUrl)` → handle `disconnect`/`credentialsrequired`/`securityfailure` events.
- Keyboard capture: noVNC handles this; toggle full-screen with a button (calls `rfb.focus()`); add a "send Ctrl-Alt-Del" button (`rfb.sendCtrlAltDel()`).
- Reconnect on transient failure: re-POST for a new ticket and re-instantiate `RFB`. After 3 failures within 30s, surface an error and stop auto-retrying.

**Serial console alternative.** `<console type='pty'>` already exists in domain XML. libvirt exposes the pty path via `Domain.OpenConsole()` which returns a libvirt `Stream`. Wrap that stream in a websocket the same way as VNC, but serve as `text` subprotocol (UTF-8). Frontend uses `xterm.js` (~150KB). The same ticket flow applies; tickets carry an `intent: "vnc" | "serial"` field so a single-purpose ticket can't be redirected.

**Resource limits.**
- `daemon.console.max_concurrent_sessions` (default 8): global cap to prevent a leak of file descriptors and goroutines. Excess returns HTTP 429 `console_limit_reached`.
- `daemon.console.max_session_seconds` (default 3600): hard idle timeout. After expiry the daemon sends a websocket close frame.
- `daemon.console.idle_timeout_seconds` (default 600): closes the session after 10 min of zero traffic in either direction.

**Observability.** Each session emits three `app`-source events (depends on 4.2): `console.session_started`, `console.session_ended` (with reason: `client_close` / `server_idle` / `vm_stopped` / `daemon_shutdown` / `error`), and `console.session_terminated` for admin-revoked sessions. Active session count is reported in `host_stats`.

**Security checklist (must hold for v1).**
- [x] Ticket endpoint requires `Authorization: Bearer`.
- [x] Ticket is single-use, ≤60s TTL, scoped to VM ID + API key.
- [ ] Websocket handler rejects requests without a valid ticket with HTTP 401.
- [ ] VNC port stays bound to `127.0.0.1` — verified by an integration test that asserts external connect refuses.
- [x] No ticket appears in any access log (the middleware redacts `?ticket=`).
- [ ] Sessions are forcibly closed on VM stop, VM delete, daemon shutdown, and API-key revocation.
- [ ] `wss://` is required when TLS is configured (`ws://` rejected with 403 to avoid mixed-content downgrade).

#### 5.1.1 Task list

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 5.1.1 | Add `internal/console/` package: ticket store (in-memory map + janitor), `IssueTicket(vmID, apiKey)`, `ConsumeTicket(token, vmID) (apiKey, error)`. Unit tests for single-use, expiry, VM-scope mismatch | M | ✅ Done — `internal/console/store.go` now provides an in-memory single-use ticket store with TTL + janitor cleanup, and `internal/console/store_test.go` covers single-use, expiry, VM-scope mismatch, janitor pruning, token uniqueness, and concurrent single-consumer behavior |
| 5.1.2 | Add `POST /api/v1/vms/{id}/console/ticket` endpoint returning `{ticket, expires_at, websocket_url}`. Reuse `Authorization: Bearer` middleware. Validate VM exists and is in `running` state (refuse `stopped` with 409 `vm_not_running`) | S | ✅ Done — `internal/api/handlers_console.go` ships `POST /api/v1/vms/{id}/console/ticket` returning `types.ConsoleTicket{Ticket, ExpiresAt, WebsocketURL}`. Validates VM exists (404), refuses non-running VMs with 409 `vm_not_running`, and returns 503 `service_unavailable` when the daemon was started without a console store. The handler reads the caller's bearer token via `extractAPIKey` and stores it on the ticket so the (forthcoming) websocket handler in 5.1.4 can forward it. Daemon wires `console.NewStore()` and registers cleanup on shutdown. Tests: `TestIssueConsoleTicket_RunningVM_ReturnsTicket` (round-trip + single-use), `_StoppedVM_Returns409`, `_UnknownVM_Returns404`, `_NoStoreReturns503`, `_TicketCarriesCallerAPIKey`. OpenAPI documents the new path + `ConsoleTicket` schema |
| 5.1.3 | Add `vm.Manager.GetConsoleEndpoint(ctx, id, intent) (host string, port int, err error)`: parses domain XML for `<graphics>` (VNC) or returns the pty path for serial. Implement on `LibvirtManager` and `MockManager` | M | ✅ Done — `pkg/types/console.go` defines the `ConsoleIntent` enum (`vnc`/`serial`) and a structured `ConsoleEndpoint` (Host/Port for VNC, Path for serial); `vm.Manager.GetConsoleEndpoint(ctx, id, intent)` lives on the interface; the `LibvirtManager` impl looks up the live domain, refuses with `vm_not_running` (HTTP 409 mapping) when the VM is stopped, and parses `<graphics type='vnc'>` plus `<console type='pty'>`/`<serial><source path>` via a pure-Go `parseConsoleEndpointFromXML` helper that returns a typed `console_unavailable` error when libvirt has not yet allocated the port or pty. `MockManager.GetConsoleEndpoint` returns synthetic VNC/serial defaults for running VMs, exposes `SeedConsoleEndpoint` to pin a specific endpoint, and `SeedConsoleListener` to bind a real loopback TCP listener so 5.1.4's websocket-proxy tests can dial a known target. Coverage in `internal/vm/console_endpoint_test.go`: VNC happy path + default listen, port-not-yet-assigned, no-graphics, serial-from-tty, serial-from-source-path, serial-not-yet-allocated, unknown intent, malformed XML, MockManager running/stopped/unknown/invalid-intent paths, hook propagation, and a SeedConsoleListener round-trip that dials the bound socket |
| 5.1.4 | Add `GET /api/v1/vms/{id}/console` websocket endpoint: validate ticket, dial VNC TCP socket, bidirectional copy with idle deadlines, register session in active map. Use `gorilla/websocket` (already pulled in indirectly? — confirm during 5.1.1) or `nhooyr.io/websocket`. Subprotocol `binary` | L | Handle: client close, server close, dial failure (502 `console_unreachable`), TLS-mismatch (403 `mixed_content_blocked`) |
| 5.1.5 | Wire active-session map into `vm.Manager.Stop`/`Delete` so admin actions force-close in-flight sessions. Emit `console.session_terminated` (depends on 4.2) | S | |
| 5.1.6 | Add daemon config: `daemon.console.max_concurrent_sessions` (8), `daemon.console.max_session_seconds` (3600), `daemon.console.idle_timeout_seconds` (600), `daemon.console.password_key` (random base64 secret). Document in `vmsmith.yaml.example` | S | ✅ Done — `internal/config/config.go` now carries the full `Daemon.Console` block with defaults for session cap / max session length / idle timeout plus an optional `password_key`, `internal/config/config_test.go` covers both defaults and YAML overrides, and the settings are documented in `vmsmith.yaml.example` and the README. `password_key` remains optional/empty so per-VM VNC passwords stay disabled until 5.1.8 lands |
| 5.1.7 | Vendor noVNC under `web/src/vendor/novnc/` (pinned version, license header preserved). Add `web/src/pages/VMConsole.jsx` with ticket fetch, RFB instantiation, Ctrl-Alt-Del button, fullscreen toggle, status overlay | L | Add to router as `/vms/:id/console`; "Console" button on VMDetail opens in a new tab to give a clean keyboard capture surface |
| 5.1.8 | Add VNC password support: `vnc_password` field on `VMSpec`/`VMUpdateSpec`, redact-on-read in API responses, persist as bcrypt hash + reversible-encrypted blob (AES-GCM with `daemon.console.password_key`). Regenerate domain XML on next start with `passwd='...'`. Add unit tests for round-trip and redaction | M | Reject password on update if VM is running and require restart message |
| 5.1.9 | Serial console (`?intent=serial`): `vm.Manager.OpenSerialConsole(ctx, id) (io.ReadWriteCloser, error)` wrapping `Domain.OpenConsole`. Websocket handler uses `text` subprotocol. Bundle `xterm.js` and add a "Serial" tab next to "VNC" on the VMConsole page | M | Tickets carry `intent`; ticket for VNC cannot open serial and vice versa |
| 5.1.10 | Redact `?ticket=` from request middleware logs (extend the existing logging middleware to scrub the query param). Add a unit test asserting the ticket never appears in captured log output | S | ✅ Done — `internal/api/requestLogger` now redacts both `ticket` and SSE `api_key` query params, with unit coverage ensuring raw secrets never reach structured log output |
| 5.1.11 | Tests: unit (ticket store concurrency + expiry, password encryption round-trip, redaction), integration (ticket → websocket happy path with a fake VNC echo server, ticket reuse rejected, expired ticket rejected, VM stop forces close, idle timeout), Playwright (open console page, see canvas mount, send Ctrl-Alt-Del) | L | Use `httptest.NewServer` + `gorilla/websocket` test helpers |
| 5.1.12 | Docs: new section in `docs/ARCHITECTURE.md` covering proxy design, ticket flow, security checklist; add operator note in `docs/PRODUCTION_DEPLOYMENT.md` about firewalling the host's loopback (no action needed if iptables doesn't touch `lo`) | S | ✅ Done — `docs/ARCHITECTURE.md` now documents the ticket flow, websocket proxy shape for both VNC and serial intents, active-session lifecycle, reverse-proxy expectations, and an explicit console security checklist; `docs/PRODUCTION_DEPLOYMENT.md` warns operators to keep raw VNC loopback-only and avoid firewall/proxy rules that expose it |

#### 5.1.2 Open architectural questions

1. **Per-session vs per-VM concurrency.** Should two operators be able to view the same VM's console simultaneously? RFB supports it (read-only viewers); recommendation for v1: allow one read-write session and any number of read-only viewers, gated by `read_only: true` on the ticket. Defer to PR.
2. **TLS termination behind reverse proxy.** When 3.2 (TLS) is satisfied via nginx/Caddy in front of the daemon, the websocket arrives as `ws://` on the loopback. The handler should trust `X-Forwarded-Proto` only when the source IP matches a configured `daemon.trusted_proxies` CIDR list. Co-design with 3.2.4 docs.
3. **Clipboard sync.** noVNC supports clipboard via the `cuttext`/`bell` extensions. Cross-origin clipboard in browsers is restricted; document as "best effort" and disable by default behind `daemon.console.allow_clipboard`.
4. **Audit detail.** Should keystrokes / mouse events be logged? Privacy implications are significant; recommendation: log only session boundaries (start/end + bytes-in/out totals), never payloads. Make this an explicit non-feature in docs.

#### 5.1.3 Dependencies

- **3.1 (auth)** — required. The ticket endpoint and websocket auth assume `Authorization: Bearer` middleware is in place. Without auth, anyone on the network gets full console access.
- **3.2 (TLS)** — strongly recommended before exposing this beyond localhost. RFB inside `ws://` is plaintext.
- **4.2 (events)** — `console.session_*` events are emitted via the events bus; if 4.2 hasn't shipped, fall back to `logger.Info` and revisit.
- **3.4 (rate limiting)** — apply a stricter bucket on ticket issuance (e.g., 10/min per API key) to prevent enumeration of VM IDs through ticket POSTs.

### 5.2 Scheduled Operations

Automate routine tasks like snapshots, scheduled start/stop windows, and backups. The original task list glossed over several correctness questions (catch-up after restart, concurrent runs, missed window vs daylight-saving anomalies, action retries) that determine whether operators can actually trust the scheduler with production data.

#### 5.2.0 Architectural overview

**Core design.** A single in-process scheduler goroutine drives a list of `Schedule` records loaded from bbolt. Use `github.com/robfig/cron/v3` with the `WithSeconds` and `WithLocation` options for predictable spec parsing. The scheduler does **not** spawn a goroutine per schedule for execution — instead, every fire dispatches to a bounded worker pool so a stuck snapshot can't starve the rest.

**`Schedule` record (`pkg/types/schedule.go`):**

```go
type Schedule struct {
    ID             string         `json:"id"`             // sched-<unix-nano>
    Name           string         `json:"name"`
    VMID           string         `json:"vm_id"`          // empty = applies to all VMs (admin-only schedules)
    TagSelector    []string       `json:"tag_selector,omitempty"` // alternative to VMID; OR-of-tags match
    Action         string         `json:"action"`         // "snapshot" | "start" | "stop" | "restart"
    CronSpec       string         `json:"cron_spec"`      // 6-field with seconds: "0 30 2 * * *"
    Timezone       string         `json:"timezone"`       // IANA name; empty -> daemon's TZ
    Enabled        bool           `json:"enabled"`
    CatchUpPolicy  string         `json:"catch_up_policy"` // "skip" (default) | "run_once" | "run_all"
    MaxConcurrent  int            `json:"max_concurrent"`  // default 1; serializes overlapping fires per-schedule
    RetentionCount int            `json:"retention_count,omitempty"` // for snapshot action; 0 = use quota default
    Params         map[string]any `json:"params,omitempty"` // action-specific (e.g., snapshot name template)
    CreatedAt      time.Time      `json:"created_at"`
    UpdatedAt      time.Time      `json:"updated_at"`
    LastFiredAt    *time.Time     `json:"last_fired_at,omitempty"`
    LastResult     string         `json:"last_result,omitempty"` // "success" | "error: ..."
    NextFireAt     *time.Time     `json:"next_fire_at,omitempty"` // computed; cached for UI
}
```

**Run records (`pkg/types/schedule_run.go`)** — separate bucket so the schedule definition stays small:

```go
type ScheduleRun struct {
    ID         string    `json:"id"`           // run-<unix-nano>
    ScheduleID string    `json:"schedule_id"`
    VMID       string    `json:"vm_id"`        // resolved VM (tag selectors fan out)
    StartedAt  time.Time `json:"started_at"`
    FinishedAt time.Time `json:"finished_at,omitempty"`
    Status     string    `json:"status"`       // "running" | "success" | "error" | "skipped"
    Error      string    `json:"error,omitempty"`
    SkipReason string    `json:"skip_reason,omitempty"` // "vm_not_found" | "vm_already_stopped" | "concurrent_run" | "catch_up_skipped"
}
```

**bbolt layout:**
- `schedules` — key = schedule ID, value = JSON `Schedule`.
- `schedule_runs` — key = `{schedule_id}/{run_id_be}` (big-endian timestamp suffix). Compound key keeps a single forward cursor scan per-schedule history. Retention: per-schedule cap of `daemon.schedules.max_run_history` (default 200) trimmed in the same Update tx that writes a new run.
- `schedule_meta` — key = `last_tick`, value = RFC3339 timestamp of the most recent successful tick (used for catch-up after a restart).

**Catch-up semantics (the subtle part).** When the daemon restarts, some firings may have been missed. The scheduler reads `schedule_meta/last_tick` and computes which schedules would have fired between `last_tick` and `now()`. Three policies, configurable per schedule (`catch_up_policy`):

| Policy | Behavior | Use case |
|---|---|---|
| `skip` | Ignore all missed fires. Resume normal scheduling. | Default. Idempotent actions where a missed window doesn't matter (e.g., periodic snapshot, the next one will replace it). |
| `run_once` | If any fires were missed, run the action exactly once, then resume. | Backups: missing one is OK, but you want the system to know it's behind. |
| `run_all` | Replay every missed fire in chronological order, sequentially. | Auditable schedules where every interval matters (rare; warn the operator if window > 24h). |

`schedule_meta/last_tick` is updated every minute by the scheduler tick (whether or not any schedule fires). On startup, if it's missing, treat as `now()` (no catch-up — fresh install).

**Daylight-saving and timezone handling.**
- `Schedule.Timezone` is an IANA name (`America/New_York`). robfig/cron's `WithLocation` is per-scheduler, not per-entry, so we run **one cron instance per distinct timezone** and route each schedule to the right instance.
- For ambiguous local times during DST transition (e.g., 02:30 on a fall-back day occurring twice), document that we use the wall-clock time once — robfig's behavior matches Go's `time.ParseInLocation`. Surface this in the schedule edit UI with a warning when the spec touches 02:00–03:00.

**Concurrency control.**
- Per-schedule: `MaxConcurrent` (default 1). If a fire arrives while the previous run is in progress, write a `ScheduleRun{Status: "skipped", SkipReason: "concurrent_run"}` and emit a `schedule.fire_skipped` event (depends on 4.2).
- Global: `daemon.schedules.worker_pool_size` (default 4). The dispatcher queues fires onto the worker pool. Queue overflow drops with `skip_reason: "queue_full"` and emits a `system` event.

**Action handlers.** Pluggable via a small `ScheduleActionFunc` registry:

```go
type ActionFunc func(ctx context.Context, vmID string, params map[string]any) error
```

v1 actions:

| Action | Implementation |
|---|---|
| `snapshot` | `vm.Manager.CreateSnapshot(ctx, vmID, name)`. Snapshot name template defaults to `auto-{schedule_name}-{rfc3339}`. Honors `RetentionCount`: after creation, list snapshots whose names start with `auto-{schedule_name}-` and delete oldest until count ≤ retention. |
| `start` | `vm.Manager.Start(ctx, vmID)`. Skip if already running with `skip_reason: "vm_already_running"`. |
| `stop` | `vm.Manager.Stop(ctx, vmID)`. Skip if already stopped. |
| `restart` | `vm.Manager.Restart(ctx, vmID)` — graceful `Shutdown()` (with a 30s grace period before `Destroy()` falls back) followed by `Create()`. Single run record. |

**Tag-selector fan-out.** When `TagSelector` is set (and `VMID` is empty), the scheduler resolves the matching VM list at fire time — not at schedule-create time. Each matched VM gets its own `ScheduleRun` record. New VMs picked up automatically; deleted VMs simply produce zero runs.

**Failure & retry.**
- Per-action timeout from `daemon.schedules.action_timeout` (default 5m for snapshot, 30s for start/stop).
- On transient error, retry up to `daemon.schedules.max_retries` (default 2) with 30s/2m backoff. Persistent error is recorded as `Status: "error"` with the last error string.
- Retries do **not** create new `ScheduleRun` records — they update the existing one's `Error` field with each attempt's outcome (`attempt N: <err>; attempt N+1: <err>`).

**Permissions (depends on 3.1).** Schedule CRUD requires the same auth as VM mutation. Once 3.1.5 (RBAC) lands, schedules are admin-only. v1 treats any authenticated request as authorized.

**Observability.** Emits `schedule.created`, `schedule.deleted`, `schedule.updated`, `schedule.fired`, `schedule.fire_skipped`, `schedule.fire_succeeded`, `schedule.fire_failed`, `schedule.catch_up_replayed` (with N missed count). All `app`-source via 4.2's `PublishAppEvent` helper, with `Actor: "scheduler"` for fires.

#### 5.2.1 Task list

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 5.2.1 | Define `pkg/types/schedule.go` (`Schedule`) and `pkg/types/schedule_run.go` (`ScheduleRun`) per the schemas above | S | ✅ Done — added schedule + run model types (including action/catch-up/status constants) in `pkg/types/schedule.go` and `pkg/types/schedule_run.go` |
| 5.2.2 | Add `schedules`, `schedule_runs`, `schedule_meta` bbolt buckets; `Store.{Put,Get,List,Delete}Schedule`, `Store.AppendRun(scheduleID, run)` (with retention trim in the same tx), `Store.ListRuns(scheduleID, limit)`, `Store.GetLastTick`/`SetLastTick` | M | ✅ Done — store buckets plus schedule CRUD/run-history helpers and last-tick persistence are implemented in `internal/store/bolt.go`, with retention trimming and coverage in `internal/store/bolt_test.go`; run keys use `{schedule_id}/{ts_be}` for fast per-schedule scans |
| 5.2.3 | Create `internal/scheduler/` package: per-timezone `cron.Cron` instances, schedule registration/deregistration on CRUD, bounded worker pool, action registry. Unit tests for: spec parsing, timezone routing, max-concurrent skip, queue overflow | L | ✅ Done — `internal/scheduler/engine.go` runs one `cron.New(WithSeconds, WithLocation)` per distinct timezone, registers/deregisters entries on CRUD, dispatches fires onto a bounded worker pool (queue overflow → `queue_full` skip), and serialises overlapping fires per-schedule via a `MaxConcurrent` semaphore (→ `concurrent_run` skip). Unit coverage: `TestValidateSpec` (6-field required, invalid tz), `TestRegisterUnregister`, `TestRegister_DisabledScheduleNotScheduled`, `TestFire_ConcurrentRun_Skips`, `TestEnqueue_QueueFull_RecordsSkip` |
| 5.2.4 | Implement catch-up logic on daemon startup: compare `last_tick` to `now()`, fire each schedule per its `catch_up_policy`. Tick the meta key every 60s thereafter | M | ✅ Done — `runCatchUp` (engine.go) compares the persisted `last_tick` to `now()` per timezone and replays per policy: `skip` (ignore), `run_once` (fire the latest missed once), `run_all` (replay chronologically, capped at `MaxCatchUp`=100). Fresh installs (zero `last_tick`) do not catch up. A `tickLoop` goroutine advances `last_tick` every `tick_interval_seconds` (default 60). Pure missed-fire math lives in `catchup.go` (`missedFires`) with deterministic tests (`TestMissedFires_*`); policy behaviour covered by `TestRunCatchUp_RunOnce`, `_Skip`, `_FreshInstallNoCatchUp` |
| 5.2.5 | Action registry with `snapshot`, `start`, `stop`, `restart` handlers. Snapshot honors `RetentionCount` and uses a name template scoped to the schedule. Per-action timeout + retry with backoff | M | ✅ Done — `internal/scheduler/actions.go` registers `snapshot`/`start`/`stop`/`restart` handlers returning typed skips (`vm_not_found`, `vm_already_running`, `vm_already_stopped`). Snapshot names are `auto-<schedule>-<timestamp>` and `RetentionCount` trims only that schedule's auto snapshots (operator snapshots untouched). `runOne` wraps each attempt in `action_timeout` and retries transient errors up to `MaxRetries` with backoff (skips are never retried). Coverage: `TestFire_StartAction_StartsStoppedVM`, `_SkipsRunningVM`, `TestFire_StopAction_SkipsStoppedVM`, `TestFire_VMNotFound_Skips`, `TestFire_SnapshotAction_CreatesAndTrims`, `TestRunOne_RetryThenSuccess`, `_RetryExhausted_Error` |
| 5.2.6 | Tag-selector resolution at fire time: query `Store.ListVMs` filtered by tag set; fan out to one `ScheduleRun` per matched VM, all queued onto the same worker pool | S | ✅ Done — `resolveTargets` (engine.go) resolves `TagSelector` against the live VM set at fire time (any-of match over VM tags + spec tags, case-insensitive), one run per matched VM; an empty `vm_id` AND empty selector targets all VMs. Covered by `TestFire_TagFanOut` and `TestFire_NoSelector_TargetsAllVMs` |
| 5.2.7 | API: `POST /api/v1/schedules`, `GET /api/v1/schedules` (with `?vm_id=`, `?action=`, `?enabled=`), `GET /api/v1/schedules/{id}`, `PATCH /api/v1/schedules/{id}` (toggle enabled, update spec/retention), `DELETE /api/v1/schedules/{id}`, `GET /api/v1/schedules/{id}/runs` (paginated, reverse-chronological), `POST /api/v1/schedules/{id}/run-now` (synthesize a manual fire, recorded as `Actor: "<api-key-alias>"`) | M | ✅ Done — `internal/api/handlers_schedule.go` implements `POST`/`GET`/`GET {id}`/`PATCH`/`DELETE`/`GET {id}/runs`/`POST {id}/run-now`, wired in `router.go` under `/api/v1/schedules` and decoupled from the engine via the `ScheduleController`/`ScheduleStore` interfaces (mirrors the webhook subsystem). Create/update validate cron + timezone (400 `invalid_cron_spec`/`invalid_timezone`), action, target exclusivity, and catch-up policy; list supports `vm_id`/`action`/`enabled`/`search` + `sort`/`order` + pagination; run history is paginated with `X-Total-Count`. Integration coverage: `TestCreateSchedule_Success`, `TestCreateSchedule_Validation` (8 cases), `TestListSchedules_FiltersAndSort`, `TestGetSchedule_NotFound`, `TestUpdateSchedule`, `TestDeleteSchedule`, `TestRunNowAndRuns`, `TestScheduleEndpoints_503WhenDisabled` |
| 5.2.8 | CLI: `vmsmith schedule create --vm <id|--tag <t>> --action <a> --cron "<spec>" --timezone <tz> --retention <n> --catch-up <skip|run_once|run_all>`, `schedule list [--vm <id>]`, `schedule show <id>` (definition + last 20 runs), `schedule edit <id>` (PATCH), `schedule delete <id>`, `schedule run-now <id>` | M | ✅ Done — `internal/cli/schedule.go` adds `vmsmith schedule create|list|show|edit|delete|run-now` as thin HTTP clients over the daemon. `create`/`edit` validate action + catch-up policy client-side and reject `--vm`+`--tag` / `--tag`+`--clear-tags`; `list` surfaces a NEXT_FIRE column and forwards `--vm --action --enabled --search --sort --order --limit --page`; `show` prints the definition plus the last 20 runs. Coverage: `TestCLI_ScheduleCreate_*` (4), `TestCLI_ScheduleList_*` (4), `TestCLI_ScheduleEdit_*` (3), `TestCLI_ScheduleDelete`, `TestCLI_ScheduleRunNow` |
| 5.2.9 | Frontend: `web/src/pages/Schedules.jsx` listing all schedules with enabled toggle, next-fire timestamp, last-result chip, "Run now" button. `web/src/components/ScheduleForm.jsx` for create/edit with cron-spec helper (preset chips: hourly, daily 02:00, weekly Sunday) | M | ✅ Done — `web/src/pages/Schedules.jsx` lists schedules with an inline enabled toggle, next-fire timestamp, last-result chip, and a "Run now" button; create/edit modal includes cron preset chips (Hourly / Daily 02:00 / Weekly Sun 03:00). Each row has a chevron expander that fetches and shows the last 5 runs. Filters (search / action / enabled / sort / order) round-trip through the URL. Wired into the nav (`nav-schedules`, Clock icon) and routing; `web/src/api/client.ts` gains a `schedules` group; `tests/web/mock-server.js` mocks the endpoints and `tests/web/gui.spec.js` covers the page end-to-end |
| 5.2.10 | Frontend: schedule section on VMDetail page showing schedules whose `vm_id == this.id` or whose tag selector matches this VM's tags. "Add schedule" opens the form pre-filled with this VM | S | ✅ Done — VMDetail now renders a dedicated Schedules card that shows direct VM schedules plus tag-selector matches for the current VM, including next-fire visibility and disabled-state badges. The card also adds an "Add schedule" action that opens a VM-scoped create modal with the target VM ID prefilled/read-only and a default name derived from the VM. Coverage: Playwright (`vm detail shows matching schedules and can prefill a new VM schedule`) exercises the seeded schedule rendering and end-to-end create flow from the VM detail surface. |
| 5.2.11 | Tests: unit (timezone routing across DST transitions using a fake clock, catch-up policies, max-concurrent skip, retention trim under concurrent appends, action retry/backoff), integration (CRUD endpoints, run-now synthesis, run history pagination), E2E (real schedule fires a snapshot on a real VM and the snapshot appears) | L | Partially done — unit tier (`internal/scheduler`: catch-up policies, missed-fire math, max-concurrent + queue-full skip, retention trim, action retry/backoff) and integration tier (`internal/api`: CRUD, run-now synthesis, run-history pagination) shipped, both using an injected deterministic clock (`Config.now`/`sleep`) rather than a third-party fake-clock dep, and the engine uses its own per-schedule mutex for clear skip reasons as noted. The real-QEMU E2E (a live schedule firing a snapshot on a real VM) is deferred to the `tests/e2e` suite |
| 5.2.12 | Docs: new `docs/SCHEDULES.md` covering cron-spec syntax (note the 6-field with-seconds form), timezone/DST rules, catch-up policies with worked examples, retention semantics. Cross-link from CLAUDE.md | S | ✅ Done — `docs/SCHEDULES.md` covers quick-start, the 6-field seconds-required cron syntax with examples, timezone/DST handling, the three catch-up policies with use cases, snapshot retention semantics, concurrency/retry/timeout, observability, config, and the REST surface. Cross-linked from CLAUDE.md (directory map, bbolt model, Scheduled Operations section, config fields, REST quick reference) |
| 5.2.13 | (Already done) Snapshot retention auto-delete | S | ✅ Done — `daemon.quotas.max_snapshots_per_vm` auto-deletes the oldest snapshots. The scheduler's `RetentionCount` is independent and scoped to *auto-named* snapshots from the same schedule, so manual snapshots are not affected |

#### 5.2.2 Open architectural questions

1. **Distributed schedules (future 5.5).** When multi-host lands, two daemons must not both fire the same schedule. Either elect a leader (use bbolt-on-shared-storage advisory locks, fragile), or namespace schedules per-host (`Schedule.HostID`). Recommendation: namespace per-host now (`HostID` empty = "this daemon's host"), defer leader election.
2. **Idempotency keys for actions.** If `run_all` catch-up replays 30 missed `snapshot` fires after a 30-day outage, we'd create 30 snapshots and immediately retention-trim 28. Better: pass a `IdempotencyKey: "<schedule_id>:<scheduled_time_unix>"` to the action; snapshot handler skips if a snapshot with that key already exists. Defer to follow-up; v1 just trims.
3. **Pause window override.** Operators want "don't run any schedule between 09:00–17:00 on weekdays" without editing every cron spec. Add `daemon.schedules.maintenance_window` later.
4. **Cron-spec backward compat.** robfig/cron supports both 5-field and 6-field forms via constructor options. Pick one (6-field, seconds-required) and document it as the only accepted form to avoid 5-vs-6 confusion. Reject 5-field input with a clear error pointing to the 6-field equivalent.

#### 5.2.3 Dependencies

- **3.1 (auth)** — required before exposing schedule CRUD; "run-now" can fire arbitrary VM operations.
- **4.2 (events)** — schedule lifecycle and fire events flow through the events bus. `Actor: "scheduler"` distinguishes them from operator-initiated runs in the audit log.
- **3.5 (quotas)** — snapshot retention from 3.5.1 stacks with 5.2's per-schedule `RetentionCount`. Document that the lower of the two wins.
- **5.5 (multi-host)** — informs the `HostID` namespacing question above. Add the field now even though it's unused in v1.

### 5.3 VM Import/Export (OVA/OVF)

Enable interoperability with other virtualization platforms.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 5.3.1 | Export VM as OVA (tar of OVF descriptor + qcow2→vmdk converted disk) | L | Use `qemu-img convert -O vmdk` |
| 5.3.2 | Import VM from OVA/OVF: extract, convert disk to qcow2, create VM with matching specs | L | Parse OVF XML for CPU/RAM/disk specs |
| 5.3.3 | Add export/import endpoints and CLI commands | M | |

### 5.4 Pagination & Filtering for Large Deployments

Current list endpoints return all records. This won't scale beyond ~1000 VMs.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 5.4.1 | Add `?page=&per_page=` query params to `GET /vms`, `GET /images`, `GET /logs` | M | ✅ Done — list/log endpoints now accept `page` + `per_page` (with `limit` kept as an alias on logs) and return `X-Total-Count` for the full matching result set |
| 5.4.2 | Add `?status=running&sort=created_at&order=desc` filtering to `GET /vms` | M | ✅ Done — `GET /api/v1/vms` accepts case-insensitive `status=<state>` plus whitelisted `sort=<id\|name\|created_at\|state>` and `order=<asc\|desc>`. Unknown values return 400 `invalid_sort` / `invalid_order`; all comparators tiebreak on `id` so paginated requests are deterministic across backends. Mirrored in `vmsmith vm list --sort --order` and the VM-list sort dropdowns (round-tripped through URL query params). Coverage: API integration (sort by name / created_at / state, desc, page-spanning determinism, 400 paths), CLI (`TestCLI_VMList_SortByName`, `_SortByCreatedAtDesc`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`), `pkg/types.SortVMs` unit (case-insensitive name, created_at desc with id tiebreak, state ordering, unknown-field fallback, stable pagination), mock-server validation/sort, Playwright (sort dropdowns reorder list + URL captures `sort` / `order`) |
| 5.4.3 | Update frontend tables to support server-side pagination | M | ✅ Done — VM list, image list, and log viewer now use backend pagination metadata/controls instead of loading unbounded frontend tables |
| 5.4.4 | Add `--limit` and `--offset` flags to CLI list commands | S | ✅ Done — `vmsmith vm list` and `vmsmith image list` now support local `--limit` / `--offset` pagination with CLI test coverage and updated docs |
| 5.4.5 | Add `?sort=&order=` filtering to `GET /images` (mirror 5.4.2's VM sort for the image list) | S | ✅ Done — `GET /api/v1/images` accepts whitelisted `sort=<id\|name\|size\|created_at>` and `order=<asc\|desc>`. Unknown values return 400 `invalid_sort` / `invalid_order`; all comparators tiebreak on `id` so paginated requests are deterministic across backends. Mirrored in `vmsmith image list --sort --order` and the ImageList sort dropdowns (round-tripped through URL query params). Coverage: API integration (sort by name / size desc / created_at desc, combines with `?tag=`, page-spanning determinism, 400 paths), CLI (`TestCLI_ImageList_SortByName`, `_SortBySizeDesc`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`), `pkg/types.SortImages` unit (case-insensitive name, size desc with id tiebreak, created_at desc, unknown-field fallback, stable pagination), mock-server validation/sort, Playwright (sort dropdowns reorder list + URL captures `sort` / `order`) |
| 5.4.6 | Add `?sort=&order=` filtering to `GET /vms/{id}/snapshots` (mirror 5.4.2's VM sort for the snapshot list) | S | ✅ Done — `GET /api/v1/vms/{id}/snapshots` accepts whitelisted `sort=<id\|name\|created_at>` and `order=<asc\|desc>`. Unknown values return 400 `invalid_sort` / `invalid_order`; within a VM the snapshot ID is `<vmID>/<name>` so id-asc is identical to name-asc, and all comparators tiebreak on `name` so paginated requests are deterministic across backends. Mirrored in `vmsmith snapshot list <vm-id> --sort --order` and the VMDetail snapshot card sort dropdowns. Coverage: API integration (sort by name asc/desc, created_at desc, page-spanning determinism, 400 paths), CLI (`TestCLI_SnapshotList_SortByName`, `_SortByCreatedAtDesc`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`), `pkg/types.SortSnapshots` unit (case-insensitive name, created_at desc with name tiebreak, unknown-field fallback, stable pagination), mock-server validation/sort, Playwright (`sort controls reorder the snapshot list`) |
| 5.4.7 | Sortable template list across API, CLI, and GUI: `GET /api/v1/templates?sort=<id\|name\|created_at>&order=<asc\|desc>`, mirrored as `vmsmith template list --sort --order`, with the Create-VM modal's template selector defaulting to `sort=name` so the dropdown is alphabetical regardless of insertion order. Unknown values return 400 `invalid_sort` / `invalid_order`; all comparators tiebreak on `id` so paginated requests are deterministic across two independent fetches | S | ✅ Done — `pkg/types/template_sort.go` defines `SortTemplates` with whitelisted constants `TemplateSortID` / `TemplateSortName` / `TemplateSortCreatedAt` plus an id tiebreak; `internal/api/template_sort.go` adds `parseTemplateSort` mirroring `parseVMSort` (400 `invalid_sort` listing supported fields, 400 `invalid_order` for anything other than asc/desc, lowercase + trim before validation); `ListTemplates` applies `?tag=` filter first, then sort, then pagination — same control flow shape as `ListVMs` / `ListImages` / `ListSnapshots`. CLI `vmsmith template list --sort --order` validates client-side before reaching the storage manager. The frontend `templates.list({ sort, order })` typed against the regenerated OpenAPI schema; VMList's Create-VM modal calls `templatesApi.list({ sort: 'name' })` so the dropdown is alphabetical out of the box. Coverage: unit (`pkg/types/template_sort_test.go` — id-asc, name case-insensitive, created_at desc with id tiebreak, created_at asc with id tiebreak, unknown-field fallback, stable pagination on equal-name input), API integration (`TestListTemplates_SortByName`, `_SortByNameDesc`, `_SortByCreatedAtDesc`, `_SortCombinesWithTagFilter`, `_SortPaginationDeterministic`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`), CLI (`TestCLI_TemplateList_SortByName`, `_SortByCreatedAtDesc`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`), mock-server validation/sort, Playwright (`template selector lists templates alphabetically`) |
| 5.4.8 | Sortable port-forward list — extends `GET /vms/{vmID}/ports` with `?sort=<id\|host_port\|guest_port\|protocol\|description>&order=<asc\|desc>` and matching `vmsmith port list <vm-id> --sort --order` + sort dropdowns on the VMDetail port-forward card | S | ✅ Done — `pkg/types/portforward_sort.go` adds `SortPortForwards` with the whitelisted field set and an id tiebreak; `internal/api/portforward_sort.go` mirrors `parseVMSort` shape (400 `invalid_sort` / `invalid_order`); `internal/api/handlers_network.go` parses + applies sort in `ListPorts`; `internal/cli/network.go` adds `--sort` / `--order` to `port list` with the same validation; `web/src/pages/VMDetail.jsx` adds two `<select>` dropdowns (`port-sort-field` / `port-sort-order`) that round-trip through `?port_sort=` / `?port_order=` URL params and re-fetch via `useFetch` dependency on `[id, portSort, portOrder]`; `web/src/api/client.ts` extends `ports.list` to forward the new query params; `docs/openapi.yaml` documents the new params and 400 responses; the mock server mirrors the predicate so Playwright can validate ordering. Coverage: 7 `SortPortForwards` unit cases (id-asc, host-port-desc, guest-port-tiebreak, protocol-tiebreak, description-case-insensitive, unknown-field fallback, stable-equal-keys), 7 API integration cases (`TestListPorts_SortDefaultIsIDAsc`, `_SortByHostPortAsc`, `_SortByHostPortDesc`, `_SortByGuestPort`, `_SortByProtocol`, `_SortByDescription_CaseInsensitive`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`), 5 CLI cases (`TestCLI_PortList_SortByHostPort`, `_SortByHostPortDesc`, `_SortByDescription`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`), mock-server sort validation, and Playwright (`port forward sort dropdowns reorder the list and round-trip through the URL`, `port sort by description orders alphabetically (case-insensitive)`). Completes the 5.4 sortable-list quintet alongside VMs (5.4.2), images (5.4.5), snapshots (5.4.6), and templates (5.4.7) |
| 5.4.9 | Free-text `?search=` filter on `GET /api/v1/images` mirrored through the CLI (`vmsmith image list --search`) and the Images page (debounced search box, URL round-trip). Case-insensitive substring match across `name`, `description`, and `tags`; image ID is intentionally excluded so opaque `img-<unix-nano>` strings don't produce noisy numeric false positives. Filters compose additively with the existing `?tag=`, `?sort=`, `?order=`, and pagination params. Mirrors the symmetric "list + filter" surface VMs (`2.2.13`) and events (`4.2.20`) ship. | S | ✅ Done — `pkg/types/image_search.go` exposes `ImageMatchesSearch(img, lowercased)` with a unit suite (empty / nil / name / description / tag / caller-lowercases / empty-description / no-match); `internal/api/handlers_image.go` lowercases + trims `?search=` and runs the predicate in-place after the `?tag=` filter and before sort/pagination so `X-Total-Count` reflects the post-search population; `internal/cli/image.go` adds `--search` to `image list`; `web/src/pages/ImageList.jsx` adds a 250 ms-debounced search input with an `X` clear control and URL round-trip via `?search=`; `web/src/api/client.ts` extends `images.list` to forward the param; `docs/openapi.yaml` documents the new param. Coverage: unit (`TestImageMatchesSearch_*` × 8), API integration (`TestListImages_FilterBySearch_*` × 8 — name / description / tag / case-insensitive / whitespace-trim / no-match / composes with `?tag=` / composes with `?sort=`), CLI (`TestCLI_ImageList_FilterBySearch_*` × 5 — name / description / tag / no-match / composes with `--tag`), mock-server search predicate, Playwright (`search input filters the image list and round-trips through the URL`, `search input matches no images when query has no hits`) |
| 5.4.10 | Free-text `?search=` filter on `GET /api/v1/vms/{vmID}/snapshots` mirrored through the CLI (`vmsmith snapshot list <vm-id> --search`) and the VMDetail snapshot card (debounced search box, URL round-trip via `?snap_search=`). Case-insensitive substring match across `name` and `description`; the snapshot ID (`<vmID>/<name>`) and `vm_id` are intentionally excluded from the haystack to avoid noisy false positives on short numeric queries. Filters compose additively with the existing `?sort=` / `?order=` / pagination. Rounds out the symmetric "list + search" surface alongside VMs (2.2.13), events (4.2.20), and images (5.4.9). | S | ✅ Done — `pkg/types/snapshot_search.go` exposes `SnapshotMatchesSearch(snap, lowercased)`; `internal/api/handlers_snapshot.go` `ListSnapshots` lowercases + trims the needle once and applies the predicate before sort + pagination so `X-Total-Count` reflects the post-search population; `internal/cli/snapshot.go` adds `--search` to `snapshot list` with the same trim + lowercase + predicate pipeline; `web/src/pages/VMDetail.jsx` adds a 250 ms-debounced search input above the snapshot table with a `Search` icon and `X` clear button, round-tripped through the URL via `?snap_search=`. Coverage: unit (`pkg/types/snapshot_search_test.go` × 7 — empty / nil / name / description / caller-lowercases contract / empty-description / no-match / ID-VMID-not-in-haystack), API integration (`TestListSnapshots_FilterBySearch_*` × 6 — name / description / case-insensitive / whitespace-trim / no-match / composes-with-`sort`), CLI (`TestCLI_SnapshotList_Search*` × 5 — name / description / case-insensitive / no-match / composes-with-`--sort`), and Playwright (`snapshot search input filters the snapshot list and round-trips through the URL`, `snapshot search input matches the description field`, `snapshot search input shows empty state when no snapshots match`). Mock-server (`tests/web/mock-server.js`) mirrors the same predicate. `docs/openapi.yaml` documents the new `search` query parameter; CLAUDE.md REST quick reference for `GET /vms/{id}/snapshots` extended accordingly. |
| 5.4.11 | Free-text `?search=` filter on `GET /api/v1/vms/{vmID}/ports` mirrored through the CLI (`vmsmith port list <vm-id> --search`) and the VMDetail port-forward card (debounced search box, URL round-trip via `?port_search=`). Case-insensitive substring match across `description`, `protocol`, `host_port`, `guest_port`, and `guest_ip`; the rule ID (`{vmID}/{host}`) and `vm_id` are intentionally excluded from the haystack — the ID embeds the host_port (already covered) and `vm_id` is the URL scope, not a useful operator query. Filters compose additively with the existing `?sort=` / `?order=`. Rounds out the symmetric "list + search" surface alongside VMs (2.2.13), events (4.2.20), images (5.4.9), and snapshots (5.4.10). | S | ✅ Done — `pkg/types/portforward_search.go` exposes `PortForwardMatchesSearch(pf, lowercased)`; `internal/api/handlers_network.go` `ListPorts` lowercases + trims the needle once and applies the predicate after fetch and before sort; `internal/cli/network.go` adds `--search` to `port list` with the same trim + lowercase + predicate pipeline; `web/src/pages/VMDetail.jsx` adds a 250 ms-debounced search input above the port-forward table with a `Search` icon and `X` clear button, round-tripped through the URL via `?port_search=`; `web/src/api/client.ts` extends `ports.list({ search })` to forward the param. Coverage: unit (`pkg/types/portforward_search_test.go` × 10 — empty / nil / description / protocol / host_port / guest_port / guest_ip / caller-lowercases contract / empty-description / no-match / ID-VMID-not-in-haystack), API integration (`TestListPorts_FilterBySearch_*` × 9 — description / protocol / host_port / guest_ip / case-insensitive / whitespace-trim / no-match / composes-with-`sort` / ID-VMID-not-in-haystack), CLI (`TestCLI_PortList_FilterBySearch_*` × 6 — description / protocol / host_port / case-insensitive / no-match / composes-with-`--sort`), and Playwright (`port forward search input filters the list and round-trips through the URL`, `port forward search matches by host port`, `port forward search shows empty state when no rules match`). Mock-server (`tests/web/mock-server.js`) mirrors the same predicate. `docs/openapi.yaml` documents the new `search` query parameter; CLAUDE.md REST quick reference for `GET /vms/{id}/ports` extended accordingly. |
| 5.4.12 | Free-text `?search=` filter on `GET /api/v1/templates` mirrored through the CLI (`vmsmith template list --search`) and the Create-VM modal's template selector (debounced search box above the dropdown). Case-insensitive substring match across `name`, `description`, and `tags`; ID, image, default_user, and network attachments are intentionally excluded from the haystack — they describe what the template produces, not the template itself, and including them produces noisy false positives (e.g. an `image=rocky9.qcow2` template matching every search containing "rocky"). Filters compose additively with the existing `?tag=`, `?sort=`, `?order=`, and pagination params. Rounds out the symmetric "list + search" surface across VMs (2.2.13), events (4.2.20), images (5.4.9), snapshots (5.4.10), and port forwards (5.4.11). | S | ✅ Done — `pkg/types/template_search.go` exposes `TemplateMatchesSearch(tpl, lowercased)`; `internal/api/handlers_template.go` `ListTemplates` lowercases + trims the needle once and applies the predicate after `?tag=` and before sort + pagination so `X-Total-Count` reflects the post-search population; `internal/cli/template.go` adds `--search` to `template list` with the same trim + lowercase + predicate pipeline; `web/src/pages/VMList.jsx`'s Create-VM modal adds a 250 ms-debounced search input above the template selector dropdown with a `Search` icon and `X` clear button, driving `templatesApi.list({ search })`. Coverage: unit (`pkg/types/template_search_test.go` × 9 — empty / nil / name / description / tag / caller-lowercases contract / empty-description / no-match / image-default_user-not-in-haystack / ID-not-in-haystack), API integration (`TestListTemplates_FilterBySearch_*` × 9 — name / description / tag / case-insensitive / whitespace-trim / no-match / composes-with-tag / composes-with-sort / ID-not-in-haystack), CLI (`TestCLI_TemplateList_FilterBySearch_*` × 6 — name / description / tag / case-insensitive / no-match / composes-with-tag), and Playwright (`template search filter narrows the template dropdown`, `template search filter is case-insensitive and matches description`, `template search shows empty-state hint when no templates match`). Mock-server (`tests/web/mock-server.js`) mirrors the same predicate. `docs/openapi.yaml` documents the new `search` query parameter; CLAUDE.md REST quick reference for `GET /templates` extended accordingly. |
| 5.4.13 | Free-text `?search=` filter on `GET /api/v1/logs` mirrored through the LogViewer page (debounced search box above the table, URL round-trip via `?search=`). Case-insensitive substring match across each entry's `message`, `source`, `level`, and every *value* in the structured fields map; field **keys** and timestamps are intentionally excluded from the haystack — keys are a small, repeating vocabulary (`vm_id`, `method`, `error`, ...) and would produce noisy matches against operator-supplied values, while timestamps are already scoped via the existing `since` filter. Filters compose additively with `level` / `source` / `since` / pagination so `X-Total-Count` reflects the post-search population. CLI follow-up shipped: ✅ `vmsmith logs list` (`internal/cli/logs.go`) is a thin HTTP client for `GET /api/v1/logs` that mirrors the API filter surface one-to-one — `--level`, `--source`, `--since`, `--search`, `--limit`, `--page`, plus an opt-in `--fields` column for the structured map. Connectivity reuses the shared `--api-url` / `--api-key` (and `$VMSMITH_API_KEY`) pattern. Coverage: `internal/cli/logs_test.go` × 13 (happy path, fields column, filter forwarding, search trim+lowercase, empty-flag omission, since-duration → RFC3339 conversion, since invalid rejected, level whitelist, warning→warn canonicalisation, negative-limit rejected, no-entries message, daemon-error propagation, Authorization header). Closes out the symmetric "list + search" surface alongside VMs (2.2.13), events (4.2.20), images (5.4.9), snapshots (5.4.10), port forwards (5.4.11), and templates (5.4.12). | S | ✅ Done — `internal/logger/search.go` exposes `EntryMatchesSearch(entry, lowercased)` next to `Entry` (the type lives in `internal/logger`, not `pkg/types`, so the predicate stays in the same package); `internal/api/handlers_logs.go` lowercases + trims the needle once and applies the predicate after the `source` filter and before pagination; `web/src/pages/LogViewer.jsx` adds a 250 ms-debounced search input above the log table with a `Search` icon and `X` clear button, round-tripped through the URL via `?search=`; `web/src/api/client.ts` extends `logs.list({ search })` to forward the param; `docs/openapi.yaml` documents the new `search` query parameter. Coverage: unit (`internal/logger/search_test.go` × 9 — empty / message / source / level / field-value / field-keys-excluded / caller-lowercases contract / no-match / empty-fields-handled), API integration (`TestGetLogs_FilterBySearch_*` × 7 — message / case-insensitive / whitespace-trim / no-match / field-value-matches / composes-with-level / total-count-reflects-filtered), Playwright (`search input filters the log table and round-trips through the URL`, `search input matches against structured field values`, `search shows empty-state hint when no entries match`, `search clear button restores the unfiltered view`). Mock-server (`tests/web/mock-server.js`) mirrors the same predicate, including the field-key exclusion. CLAUDE.md REST quick reference for `GET /logs` extended accordingly. |
| 5.4.14 | Free-text `?search=` filter on `GET /api/v1/webhooks` mirrored through the CLI (`vmsmith webhook list --search`) and the Settings page (debounced search box, URL round-trip via `?search=`). Case-insensitive substring match across each webhook's `url` and `event_types`; `id`, `secret`, and `last_error` are intentionally excluded from the haystack — secrets must never appear in response bodies (defense-in-depth at the predicate layer), `id` is an opaque `wh-<unix-nano>` string nobody types from memory, and `last_error` is per-attempt operator-noise surfaced via the status badge instead. Whitespace is trimmed before matching. Closes out the symmetric "list + search" surface across every list endpoint alongside VMs (2.2.13), events (4.2.20), images (5.4.9), snapshots (5.4.10), port forwards (5.4.11), templates (5.4.12), and logs (5.4.13). | S | ✅ Done — `pkg/types/webhook_search.go` exposes `WebhookMatchesSearch(wh, lowercased)`; `internal/api/handlers_webhook.go` `ListWebhooks` lowercases + trims the needle once and applies the predicate while iterating the sorted set; `internal/cli/webhook.go` adds `--search` to `webhook list` with the same trim + lowercase + URL-encode pipeline; `web/src/pages/Settings.jsx` adds a 250 ms-debounced search input above the webhook table with a `Search` icon and `X` clear button, round-tripped through the URL via `?search=`; `web/src/api/client.ts` extends `webhooks.list({ search })` to forward the param; `docs/openapi.yaml` documents the new `search` query parameter. Coverage: unit (`pkg/types/webhook_search_test.go` × 9 — empty / nil / url / event-type / caller-lowercases contract / no-match / secret-not-in-haystack / id-not-in-haystack / last_error-not-in-haystack / empty-event-types-handled), API integration (`TestListWebhooks_FilterBySearch_*` × 7 — url / event-type / case-insensitive / whitespace-trim / empty-returns-all / no-match / secret-and-last_error-not-in-haystack), CLI (`TestCLI_WebhookList_*` × 4 — forwards-search-param / trims-and-lowercases / empty-omits-param / no-match-empty-message), mock-server (`tests/web/mock-server.js`) mirrors the same predicate, Playwright (`search input filters the webhook list and round-trips through the URL`, `search shows empty state when no webhooks match`) and JSDOM (`webhook search input filters the list and matches URL or event types`). |
| 5.4.15 | Sortable webhook list — extends `GET /api/v1/webhooks` with `?sort=<id\|url\|created_at\|last_delivery_at>&order=<asc\|desc>` and matching `vmsmith webhook list --sort --order` + sort dropdowns on the Settings page. `url` matches case-insensitively; `last_delivery_at` sorts never-delivered webhooks (zero timestamp) at the tail of the ascending list and the head of the descending list. All comparators tiebreak on `id` so paginated responses are deterministic. Completes the 5.4 sortable-list surface alongside VMs (5.4.2), images (5.4.5), snapshots (5.4.6), templates (5.4.7), and port forwards (5.4.8). | S | ✅ Done — `pkg/types/webhook_sort.go` defines `SortWebhooks` with whitelisted constants `WebhookSortID` / `WebhookSortURL` / `WebhookSortCreatedAt` / `WebhookSortLastDelivery` plus an id tiebreak; `internal/api/webhook_sort.go` adds `parseWebhookSort` mirroring `parsePortForwardSort` (400 `invalid_sort` listing supported fields, 400 `invalid_order` for anything other than asc/desc, lowercase + trim before validation); `internal/api/handlers_webhook.go` `ListWebhooks` applies search filter first, then sort, so the response is deterministic regardless of insertion order. `internal/cli/webhook.go` adds `--sort` / `--order` to `webhook list` with the same client-side validation. `web/src/api/client.ts` extends `webhooks.list({ sort, order })` typed against the OpenAPI schema; `web/src/pages/Settings.jsx` adds two `<select>` dropdowns (`webhook-list-sort-field` / `webhook-list-sort-order`) that round-trip through `?sort=` / `?order=` URL params and re-fetch via the existing `useFetch` dependency chain. `docs/openapi.yaml` documents the new params and 400 responses; the mock server mirrors the predicate so Playwright can validate ordering. Coverage: 7 `SortWebhooks` unit cases (id-asc, url-case-insensitive, url-tiebreak-on-id, created_at-desc, last_delivery_at-zero-handling, unknown-field fallback, stable-equal-keys), 8 API integration cases (`TestListWebhooks_SortDefaultIsIDAsc`, `_SortByURL_CaseInsensitive`, `_SortByURLDesc`, `_SortByCreatedAtDesc`, `_SortByLastDelivery_NeverDeliveredSortsLastAsc`, `_SortByLastDelivery_NeverDeliveredSortsFirstDesc`, `_SortComposesWithSearch`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`), 5 CLI cases (`TestCLI_WebhookList_ForwardsSortAndOrder`, `_NormalisesSortAndOrderCase`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`, `_EmptySortAndOrderOmitParams`). |
| 5.4.17 | Sortable logs list — extends `GET /api/v1/logs` with `?sort=<timestamp\|level\|source>&order=<asc\|desc>` and matching `vmsmith logs list --sort --order` + sort dropdowns on the LogViewer page. Default is `timestamp`-asc so the long-standing oldest-first contract the GUI's auto-scroll relies on stays intact. `level` orders by severity rank (debug < info < warn < error) not alphabetically, so `?sort=level&order=desc` is the operator triage view ("errors at the top"). `source` matches case-insensitively. All comparators tiebreak on timestamp+source so paginated responses are deterministic. Unknown values return 400 `invalid_sort` / `invalid_order`. Closes out the cross-cutting sortable-list surface that VMs (5.4.2), images (5.4.5), snapshots (5.4.6), templates (5.4.7), port forwards (5.4.8), webhooks (5.4.15), and events (5.4.16) already ship. | S | ✅ Done — `internal/logger/sort.go` defines `SortEntries` with whitelisted constants `EntrySortTimestamp` / `EntrySortLevel` / `EntrySortSource` plus a `levelRank` helper that maps `debug` → 0, `info` → 1, `warn`/`warning` → 2, `error` → 3 so severity sort produces operator-meaningful ordering instead of alphabetical noise. `internal/api/log_sort.go` adds `parseLogSort` mirroring `parseEventSort` (400 `invalid_sort` listing supported fields, 400 `invalid_order` for anything other than asc/desc, lowercase + trim before validation, defaults timestamp+asc to preserve the legacy oldest-first contract). `internal/api/handlers_logs.go` validates sort first (400 fail-fast), then applies it after the level/source/search filters and before pagination so `X-Total-Count` still reflects the post-filter / pre-pagination count. `internal/cli/logs.go` adds `--sort` / `--order` to `logs list` with client-side validation via `validateLogSortFlags`; empty flags omit the query params so the daemon's defaults apply. `web/src/api/client.ts` extends `logs.list({ sort, order })` typed against the regenerated OpenAPI schema; `web/src/pages/LogViewer.jsx` adds two `<select>` dropdowns (`log-sort-field` / `log-sort-order`) above the table that round-trip through `?sort=` / `?order=` URL params, reset page=1 on change, and hydrate the dropdowns from the URL on load. `docs/openapi.yaml` documents the new params + 400 responses; mock server mirrors the same predicate so Playwright can validate ordering. Coverage: 9 `SortEntries` unit cases (default-is-timestamp-asc, timestamp-desc, level-asc-by-severity, level-desc-puts-errors-first, level-tiebreaks-on-timestamp, source-case-insensitive, source-tiebreaks-on-timestamp, unknown-field-falls-back, stable-on-equal-keys, levelRank mapping), 8 API integration cases (`TestGetLogs_SortDefaultIsTimestampAsc`, `_SortByTimestampDesc`, `_SortByLevel_DescPutsErrorsFirst`, `_SortByLevel_AscOrderedBySeverity`, `_SortBySource`, `_SortComposesWithSearch`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`), 5 CLI cases (`TestCLI_LogsList_ForwardsSortAndOrder`, `_NormalisesSortAndOrderCase`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`, `_EmptySortAndOrderOmitParams`), and Playwright (`sort dropdowns reorder entries and round-trip through the URL`, `sort by source orders alphabetically`, `sort URL parameters hydrate the dropdowns on page load`). |
| 5.4.16 | Sortable events list — extends `GET /api/v1/events` with `?sort=<id\|occurred_at\|type\|source\|severity>&order=<asc\|desc>` and matching `vmsmith events list --sort --order` + sort dropdowns on the Activity page. Defaults to `id`-desc so the long-standing "newest first" contract is preserved (sequence IDs are appended in publish order). `type`, `source`, and `severity` match case-insensitively; `occurred_at` falls back to `created_at` for legacy events. All comparators tiebreak on `id` so paginated responses are deterministic. Closes out the cross-cutting sortable-list surface that VMs (5.4.2), images (5.4.5), snapshots (5.4.6), templates (5.4.7), port forwards (5.4.8), and webhooks (5.4.15) already ship. | S | ✅ Done — `pkg/types/event_sort.go` defines `SortEvents` with whitelisted constants `EventSortID` / `EventSortOccurredAt` / `EventSortType` / `EventSortSource` / `EventSortSeverity` plus an id tiebreak (and a CreatedAt fallback for legacy events with zero OccurredAt); `internal/api/event_sort.go` adds `parseEventSort` (400 `invalid_sort` listing supported fields, 400 `invalid_order` for anything other than asc/desc, lowercase + trim before validation, defaults id+desc to preserve the legacy newest-first contract); `internal/api/handlers_events.go` `ListEvents` validates sort first (400 fail-fast), then short-circuits to the legacy store-paginated path when sort=id+order=desc (the default), otherwise pulls the full filtered set and applies `SortEvents` + pagination so `X-Total-Count` still reflects the post-filter / pre-pagination count. `internal/cli/events.go` adds `--sort` / `--order` to `events list` with client-side validation; an empty `--sort` preserves the local "newest by timestamp" ordering so the long-standing CLI behaviour is unchanged. `web/src/api/client.ts` extends `events.list({ sort, order })` typed against the regenerated OpenAPI schema; `web/src/pages/Activity.jsx` adds two `<select>` dropdowns (`activity-sort-field` / `activity-sort-order`) that round-trip through `?sort=` / `?order=` URL params, reset page=1 on change, and clear via the existing "Clear" button. `docs/openapi.yaml` documents the new params + 400 response; mock server mirrors the predicate so Playwright can validate ordering. Coverage: 9 `SortEvents` unit cases (id-asc, occurred_at desc-is-newest-first, occurred_at falls-back-to-CreatedAt, type case-insensitive, type tiebreak-on-id, source, severity case-insensitive, unknown-field fallback, stable equal-keys), 8 API integration cases (`TestListEvents_SortDefaultIsIDDesc`, `_SortByOccurredAtAsc`, `_SortByType_CaseInsensitive`, `_SortBySource`, `_SortBySeverity`, `_SortComposesWithSearch`, `_SortPaginationDeterministic`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`), 5 CLI cases (`TestCLI_EventsList_SortByType_CaseInsensitive`, `_SortByOccurredAtAsc`, `_RejectsInvalidSort`, `_RejectsInvalidOrder`, `_NoSortFlagPreservesLegacyNewestFirst`), and Playwright (`sort dropdowns reorder the activity timeline and round-trip through the URL`). |
| 5.4.18 | Per-VM log filter — extends `GET /api/v1/logs` with `?vm_id=<id>` exact-match against the entry's structured `vm_id` field, mirrored through the CLI (`vmsmith logs list --vm-id <id>`) and a new VM-ID input on the LogViewer page (debounced, URL round-trip via `?vm_id=`). VM IDs are opaque `vm-<unix-nano>` strings so the match is case-sensitive and exact — `vm-1` deliberately does NOT swallow `vm-12345`. Whitespace is trimmed before comparison; empty value disables the filter. Composes additively with `level`, `source`, `since`, `search`, and pagination so `X-Total-Count` reflects the post-filter population. Closes the "show me only this VM's noise" operator query that the existing free-text `?search=` filter (5.4.13) covers fuzzily but not precisely — operators tailing a single VM's start-up sequence don't want the search box to also match every entry whose message happens to contain the VM's numeric suffix. | S | ✅ Done — `internal/logger/search.go` adds `EntryMatchesVMID(entry, target)` next to the existing `EntryMatchesSearch` predicate (exact-match contract documented inline; an empty target short-circuits at the handler layer rather than being treated as "match all" in the loop, so the filter and no-filter codepaths stay symmetric). `internal/api/handlers_logs.go` trims the `?vm_id=` value once and applies the predicate after the `source` filter and before `?search=` so the post-filter / pre-pagination `X-Total-Count` is correct. `internal/cli/logs.go` adds the `--vm-id` flag, forwarded as `vm_id=` (whitespace-only is dropped) so the CLI and GUI surface match the daemon's wire form exactly. `web/src/pages/LogViewer.jsx` adds a 250 ms-debounced text input alongside the existing search box, with a clear `X` button, URL round-trip via `?vm_id=`, and a tailored empty-state message ("No log entries for VM `<id>`"). `web/src/api/client.ts` extends `logs.list({ vmId })` to forward the param; `tests/web/mock-server.js` mirrors the same exact-match predicate (case-sensitive). `docs/openapi.yaml` documents the new `vm_id` query parameter. Coverage: 7 `EntryMatchesVMID` unit cases (empty-target-matches-all, exact-match, prefix-does-not-match, case-sensitive, nil-fields-misses-non-empty-target, missing-vm_id-field-misses, other-fields-ignored), 7 API integration cases (`TestGetLogs_FilterByVMID_ExactMatch`, `_WhitespaceTrimmed`, `_PrefixDoesNotMatch`, `_NoMatchReturnsEmpty`, `_ComposesWithSourceAndSearch`, `_TotalCountReflectsFiltered`, `_EmptyParamIsNoOp`), 4 CLI cases (`TestCLI_LogsList_ForwardsVMIDFilter`, `_VMIDFilterIsTrimmed`, `_EmptyVMIDOmitsParam`, `_VMIDFilterComposesWithOtherFilters`). CLAUDE.md REST quick reference for `GET /logs` updated accordingly. |
| 5.4.19 | Paginated webhook list — extends `GET /api/v1/webhooks` with `?page=<n>&per_page=<n>` (or `?limit=<n>` as a synonym for `per_page`), mirrored through the CLI (`vmsmith webhook list --limit --page`) and the Settings page (full `PaginationControls` widget with per-page selector + Previous/Next buttons). Pagination is applied after filter + sort so the `X-Total-Count` header reflects the post-filter / pre-pagination population — operators registering 20+ webhooks (Slack notifiers, PagerDuty escalations, audit log forwarders) can now scroll through page indicators like "Showing 26-50 of 137" the same way every other list page already does. Closes the last cross-cutting parity gap on the pagination surface that VMs (5.4.2), images (5.4.5), templates (2.2.10), snapshots, events (4.2.17), and logs (5.4.13) already ship. | S | ✅ Done — `internal/api/handlers_webhook.go` `ListWebhooks` now calls `parsePagination` + `paginateSlice` + `setTotalCountHeader` after the sort step so the per-filter / pre-pagination total is exposed via the standard `X-Total-Count` header. `internal/cli/webhook.go` adds `--limit` / `--page` flags to `webhook list`; `--limit` is forwarded as `per_page`, `--page` is only forwarded when paired with `--limit` (forwarding `page=1` alone would be misleading and surfaces no useful information). `web/src/api/client.ts` `webhooks.list({ page, perPage })` now returns the response through `withMeta: true` so the caller can read `meta.totalCount`. `web/src/pages/Settings.jsx` adds the shared `PaginationControls` widget (per-page selector + Previous/Next), URL round-trip via `?page=` / `?per_page=`, and a page-reset side effect that snaps back to page 1 whenever the filter / sort changes (so the user never lands beyond the post-filter page count). `tests/web/mock-server.js` mirrors the `?page=&per_page=&limit=` parsing and sets `X-Total-Count` for the JSDOM / Playwright suites. `docs/openapi.yaml` documents the new `page` / `per_page` query parameters and the `X-Total-Count` response header. Coverage: 5 API integration cases (`TestListWebhooks_Pagination_PerPagePage`, `_PageBeyondEndReturnsEmpty`, `_NoParamsReturnsAll`, `_TotalCountReflectsFilter`, `_LimitAlias`, `_CapsAtMaxPerPage`); 4 CLI cases (`TestCLI_WebhookList_ForwardsLimit`, `_ForwardsLimitAndPage`, `_PageWithoutLimitOmitsBothParams`, `_LimitComposesWithFilters`); 1 JSDOM mock-GUI case (`settings webhook list shows pagination controls with X-Total-Count total`). CLAUDE.md REST quick reference for `GET /webhooks` updated accordingly. |
| 5.4.26 | Explicit-membership `?event_type=` filter on `GET /api/v1/webhooks` mirrored through the CLI (`vmsmith webhook list --event-type <type>`) and a debounced "Filter by event type…" input on the Settings page (URL round-trip via `?event_type=`). Case-insensitive exact-match against entries in the webhook's `event_types` filter list (matches when any entry equals the value); whitespace-trimmed. Catch-all webhooks (empty `event_types`) are NOT matched — mirrors the bulk_delete `event_type` selector semantics so the list and bulk-action surfaces agree on "explicit-membership" semantics: this is "which webhooks listen for vm.created", not "which webhooks will fire for this event". Composes additively with the existing `?tag=` / `?search=` / `?sort=` / `?order=` / pagination params so `X-Total-Count` reflects the post-filter population. Closes the operator query *"audit every webhook subscribed to vm.deleted before retiring the event"* that the existing `?search=` filter (5.4.14) would also fuzzy-match against a description that happens to mention the event name. | S | ✅ Done — `internal/api/handlers_webhook.go`: `ListWebhooks` parses `?event_type=` (trim + lowercase) and applies a new `webhookSubscribedToEventType` predicate between `?tag=` and `?search=` so the post-filter / pre-pagination `X-Total-Count` stays correct; the helper is case-insensitive (unlike the bulk_delete-specific `webhookExplicitlySubscribed`) but shares the explicit-membership / catch-all-excluded semantics. `internal/cli/webhook.go`: adds `--event-type` to `webhook list` with the same trim + lowercase + URL-encode pipeline. `web/src/pages/Settings.jsx`: adds a debounced 250 ms text input next to the existing search box with an `X` clear button and URL round-trip via `?event_type=`; the page resets to page 1 whenever the filter changes; empty-state copy branched for the new filter ("No webhooks explicitly subscribe to …"). `web/src/api/client.ts`: extends `webhooks.list({ eventType })` to forward the new query parameter against the regenerated OpenAPI schema. `docs/openapi.yaml`: documents the new `event_type` query parameter. `tests/web/mock-server.js`: mirrors the predicate including the catch-all exclusion. Coverage: 9 API integration cases (`TestListWebhooks_FilterByEventType_ExactMatch`, `_CaseInsensitive`, `_WhitespaceTrimmed`, `_EmptyIsNoOp`, `_NoMatch`, `_CatchAllExcluded`, `_ComposesWithTag`, `_ComposesWithSearch`, `_TotalCountReflectsFiltered`); 4 CLI cases (`TestCLI_WebhookList_ForwardsEventTypeFilter`, `_TrimsAndLowercasesEventType`, `_EmptyEventTypeOmitsParam`, `_EventTypeComposesWithSearch`); 1 Playwright case (`event-type filter narrows the webhook list and round-trips through the URL`). |
| 5.4.20 | Paginated port-forward list — extends `GET /api/v1/vms/{vmID}/ports` with `?page=<n>&per_page=<n>` (or `?limit=<n>` as a synonym for `per_page`), mirrored through the CLI (`vmsmith port list <vm-id> --limit --offset`) and the VMDetail "Port Forwards" card (full `PaginationControls` widget with per-page selector + Previous/Next buttons). Pagination is applied after filter + sort so the `X-Total-Count` header reflects the post-filter / pre-pagination population — operators wiring up VMs with 30+ DNAT rules (Slack notifiers, mail relays, microservice fan-outs) can now scroll through page indicators like "Showing 26-30 of 30 rules" the same way every other list surface already does. Closes the last cross-cutting parity gap on the pagination surface — VMs (5.4.2), images (5.4.5), templates (2.2.10), snapshots, events (4.2.17), logs (5.4.13), and webhooks (5.4.19) all ship pagination; port forwards were the only missing list endpoint. | S | ✅ Done — `internal/api/handlers_network.go` `ListPorts` now calls `parsePagination` + `paginateSlice` + `setTotalCountHeader` after the sort step so the per-filter / pre-pagination total is exposed via the standard `X-Total-Count` header. `internal/cli/network.go` adds `--limit` / `--offset` flags to `port list` (matching the in-process VM/image CLI convention, not the over-HTTP webhook CLI), guarded by `normalizeLimitOffset` so negative values are rejected with `--limit must be >= 0` / `--offset must be >= 0`. `web/src/api/client.ts` `ports.list({ page, perPage })` now wraps the call through `withMeta: true` so the caller can read `meta.totalCount`. `web/src/pages/VMDetail.jsx` adds the shared `PaginationControls` widget under the Port Forwards card, URL round-trip via `?port_page=` / `?port_per_page=`, and a page-reset side effect that snaps back to page 1 whenever the sort / search / per-page changes. `tests/web/mock-server.js` mirrors the `?page=&per_page=&limit=` parsing and sets `X-Total-Count` for the Playwright suite. `docs/openapi.yaml` documents the new `page` / `per_page` query parameters and the `X-Total-Count` response header. Coverage: 6 API integration cases (`TestListPorts_Pagination_PerPagePage`, `_PageBeyondEndReturnsEmpty`, `_NoParamsReturnsAll`, `_TotalCountReflectsFilter`, `_LimitAlias`, `_ComposesWithSortAndTag`); 7 CLI cases (`TestCLI_PortList_LimitTruncates`, `_OffsetSkipsFromStart`, `_LimitAndOffsetTogether`, `_OffsetBeyondEnd`, `_RejectsNegativeLimit`, `_RejectsNegativeOffset`, `_PaginationComposesWithFilter`); 1 Playwright case (`port forward pagination controls reflect X-Total-Count and step pages`). CLAUDE.md REST quick reference for `GET /vms/{id}/ports` updated accordingly. |
| 5.4.21 | Per-flag tristate filters on `GET /api/v1/vms` for the daemon-managed lifecycle flags — extends the VM list with `?auto_start=true|false` and `?locked=true|false` exact-match filters, mirrored through the CLI (`vmsmith vm list --auto-start --locked`) and two new "Auto-start" / "Locked" Any/Yes/No dropdowns on the VMList page (URL round-trip via `?auto_start=` / `?locked=`). Each filter is tristate: absent / whitespace-only disables the filter; `true` / `false` (case-insensitive, plus `1` / `0` aliases) drive an exact-match predicate against the corresponding `Spec.AutoStart` / `Spec.Locked` field; anything else returns 400 `invalid_auto_start` / `invalid_locked`. Composes additively with the existing `?tag=` / `?status=` / `?search=` filters and the sort + pagination surface so `X-Total-Count` reflects the post-filter population. Closes the operator query "which VMs auto-start at daemon boot" and "which VMs are delete-protected" that the existing `?status=` filter (5.4.2) and `?tag=` filter cannot answer without round-tripping every VM through `GET /vms/{id}` — the per-flag dropdowns also serve as a discoverability surface for the 2.5.1 / 2.5.2 features they expose. | S | ✅ Done — `internal/api/parse_tristate.go` adds the shared `parseTristateBoolParam` helper (whitespace-trim + lowercase + `true`/`false`/`1`/`0` accepted, anything else returns the typed `*types.APIError` so the handler can pass it straight through `writeAPIError`); `internal/api/handlers_vm.go` `ListVMs` parses both params up-front (400 fail-fast) and threads `(value, set)` predicates into the existing filter loop alongside `status` / `tag` / `search` so the post-filter `X-Total-Count` stays correct. `internal/cli/parse_tristate.go` mirrors the parser shape for the CLI (`parseCLITristateBool` returns a usage error that name-checks the flag for the offending value), `internal/cli/vm.go` adds `--auto-start` / `--locked` flags to `vmListCmd` with the same predicates inside the in-process filter loop. `web/src/api/client.ts` `vms.list({ autoStart, locked })` forwards the typed query params against the regenerated OpenAPI schema; `web/src/pages/VMList.jsx` adds two `<select>` controls (`vm-list-auto-start-filter` / `vm-list-locked-filter`) next to the sort dropdowns, snaps page back to 1 on change, and round-trips via `?auto_start=` / `?locked=` URL params with hydration on load. `docs/openapi.yaml` adds `AutoStartFilter` / `LockedFilter` parameter components; mock server mirrors the predicates plus the 400 paths so Playwright can validate every branch. Coverage: 13 `parseTristateBoolParam` unit cases (empty / whitespace / true / TRUE / TrUe / padded / false / FALSE / numeric-one / numeric-zero / invalid-word / invalid-number / invalid-mixed); 11 API integration cases (`TestListVMs_FilterByAutoStart_True`, `_False`, `_CaseInsensitive`, `_WhitespaceTrimmed`, `_EmptyIsNoOp`, `_InvalidValueReturns400`, `_AcceptsNumericAliases`, `_ComposesWithStatus`, `TestListVMs_FilterByLocked_True`, `_False`, `_InvalidValueReturns400`, `_TotalCountReflectsFiltered`, `TestListVMs_FilterByAutoStartAndLocked`); 8 CLI cases (`TestCLI_VMList_FilterByAutoStart_True`, `_False`, `_CaseInsensitive`, `_EmptyOmitsFilter`, `TestCLI_VMList_FilterByLocked_True`, `TestCLI_VMList_RejectsInvalidAutoStart`, `_RejectsInvalidLocked`, `TestCLI_VMList_FilterByAutoStartAndLocked`); 3 Playwright cases (`auto-start filter narrows the VM list and round-trips through the URL`, `locked filter narrows the VM list`, `auto-start and locked filter values hydrate from the URL on load`). CLAUDE.md REST quick reference for `GET /vms` extended accordingly. |
| 5.4.22 | Per-base-image filter for the VM list — extends `GET /api/v1/vms` with `?image=<name>` case-insensitive exact-match on `spec.image`, mirrored through the CLI (`vmsmith vm list --image <name>`) and a new debounced "Filter by image…" input on the VMList page (URL round-trip via `?image=`). Whitespace is trimmed before comparison; empty value disables the filter. Composes additively with the existing `?tag=` / `?status=` / `?search=` filters and the sort + pagination params so `X-Total-Count` reflects the post-filter population. Closes the operator query "show me every VM built from `rocky9.qcow2`" that the existing free-text `?search=` filter would also match fuzzily against any VM whose description or tag happens to contain the substring, and that `?tag=` cannot answer without pre-tagging every VM by its base image. | S | ✅ Done — `internal/api/handlers_vm.go` `ListVMs` trims + lowercases the `?image=` value once and applies `strings.EqualFold(vm.Spec.Image, imageFilter)` inside the existing filter loop alongside `status` / `tag` / `search` so the post-filter / pre-pagination `X-Total-Count` stays correct. `internal/cli/vm.go` adds `--image` to `vmListCmd` with the same trim + lowercase + predicate pipeline so the in-process CLI path matches the daemon exactly. `web/src/pages/VMList.jsx` adds a 250 ms-debounced text input alongside the existing search box with an `X` clear button and URL round-trip via `?image=`; the page also resets to page 1 whenever the image filter changes so the operator never lands beyond the post-filter page count. `web/src/api/client.ts` extends `vms.list({ image })` to forward the new query parameter against the regenerated OpenAPI schema. `docs/openapi.yaml` adds the new `ImageFilter` parameter component and references it on `GET /vms`. `tests/web/mock-server.js` mirrors the predicate so Playwright can validate every branch. Coverage: 7 API integration cases (`TestListVMs_FilterByImage_ExactMatch`, `_IsCaseInsensitive`, `_TrimsWhitespace`, `_EmptyIsNoOp`, `_NoMatch`, `_ComposesWithStatus`, `_ComposesWithSearch`, `_TotalCountReflectsFiltered`); 5 CLI cases (`TestCLI_VMList_FilterByImage_ExactMatch`, `_CaseInsensitive`, `_EmptyOmitsFilter`, `_NoMatch`, `_ComposesWithStatus`); 3 Playwright cases (`image filter narrows the VM list to a single base image and round-trips through the URL`, `image filter is case-insensitive`, `image filter matches no VMs when query has no hits`). CLAUDE.md REST quick reference for `GET /vms` extended accordingly. |
| 5.4.23 | Exact-match `?default_user=` filter on `GET /api/v1/vms` mirrored through the CLI (`vmsmith vm list --default-user`) and the VMList page (debounced "Filter by default user…" input with URL round-trip via `?default_user=`). Case-insensitive exact-match against `Spec.DefaultUser`; whitespace is trimmed before comparison; empty filter value disables. An empty `Spec.DefaultUser` is treated as `root` to mirror `lifecycle.go`'s runtime semantics ("VMs use `root` by default; override per-VM with `VMSpec.DefaultUser` / `--default-user`") — so `?default_user=root` matches every VM that SSHes in as root, regardless of whether the field was explicitly set or left blank at create time. Closes the "which VMs SSH in as `ubuntu` vs `root` vs `ec2-user`" operator query — the existing `?search=` filter cannot answer this without producing false positives against any VM whose name, description, or tag substring happens to mention the username. Composes additively with the existing `?tag=` / `?status=` / `?search=` filters and the sort + pagination params so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_vm.go` `ListVMs` trims + lowercases the `?default_user=` value once and applies a predicate that normalises empty `Spec.DefaultUser` to `root` before the case-insensitive compare. `internal/cli/vm.go` adds `--default-user` to `vmListCmd` with the same trim + lowercase + normalised-root predicate pipeline. `web/src/pages/VMList.jsx` adds a 250 ms-debounced text input alongside the existing search box with an `X` clear button and URL round-trip via `?default_user=`. `web/src/api/client.ts` extends `vms.list({ defaultUser })` to forward the param. `tests/web/mock-server.js` mirrors the predicate, including the empty-`spec.default_user`-matches-`root` branch. `docs/openapi.yaml` adds a `DefaultUserFilter` parameter component. CLAUDE.md REST quick reference for `GET /vms` extended. Coverage: API integration (`TestListVMs_FilterByDefaultUser_ExactMatch`, `_IsCaseInsensitive`, `_TrimsWhitespace`, `_EmptyIsNoOp`, `_NoMatch`, `_RootMatchesEmptyDefaultUser`, `_ComposesWithStatus`, `_TotalCountReflectsFiltered`); CLI (`TestCLI_VMList_FilterByDefaultUser_ExactMatch`, `_IsCaseInsensitive`, `_EmptyOmitsFilter`, `_NoMatch`, `_RootMatchesEmptyDefaultUser`, `_ComposesWithStatus`). |
| 5.4.24 | Per-template `?image=` filter — extends `GET /api/v1/templates` with `?image=<value>` doing a case-insensitive exact-match against the template's `image` field, mirrored through the CLI (`vmsmith template list --image`) and a debounced "Filter by image…" input on the TemplateList page (URL round-trip via `?image=`). Whitespace is trimmed before comparison; empty value disables the filter. Closes the operator query *"show me every template built from `rocky9.qcow2`"* that `?search=` (5.4.12) would match fuzzily across name / description / tags, and that `?tag=` cannot answer without pre-tagging every template by its base image. Composes additively with the existing `?tag=` / `?search=` filters and the sort + pagination params so `X-Total-Count` reflects the post-filter population. Mirrors the VMs `?image=` filter (5.4.22) on the templates surface. | S | ✅ Done — `internal/api/handlers_template.go` `ListTemplates` trims + lowercases the `?image=` value once and applies `strings.EqualFold(tpl.Image, imageFilter)` inside a new filter pass between `?tag=` and `?search=` so the post-filter / pre-pagination `X-Total-Count` stays correct. `internal/cli/template.go` adds `--image` to `template list` with the same trim + lowercase + predicate pipeline so the in-process CLI path matches the daemon exactly. `web/src/pages/TemplateList.jsx` adds a 250 ms-debounced text input next to the existing search box with an `X` clear button and URL round-trip via `?image=`; the page resets to page 1 whenever the filter changes. `web/src/api/client.ts` extends `templates.list({ image })` to forward the new query parameter against the regenerated OpenAPI schema. `tests/web/mock-server.js` mirrors the predicate. `docs/openapi.yaml` documents the new `image` query parameter on `GET /templates`. Coverage: 7 API integration cases (`TestListTemplates_FilterByImage_ExactMatch`, `_IsCaseInsensitive`, `_TrimsWhitespace`, `_EmptyIsNoOp`, `_NoMatch`, `_ComposesWithTag`, `_ComposesWithSearch`, `_TotalCountReflectsFiltered`), 5 CLI cases (`TestCLI_TemplateList_FilterByImage_ExactMatch`, `_CaseInsensitive`, `_EmptyOmitsFilter`, `_NoMatch`, `_ComposesWithTag`), 3 Playwright cases (`image filter narrows the template list and round-trips through the URL`, `image filter is case-insensitive`, `image filter matches no templates when query has no hits`). |
| 5.4.25 | Protocol filter on port-forward list — extends `GET /api/v1/vms/{vmID}/ports` with `?protocol=<tcp\|udp>` case-insensitive exact match against the rule's transport protocol, mirrored through the CLI (`vmsmith port list <vm-id> --protocol <tcp\|udp>`) and a new "Any protocol / TCP / UDP" dropdown on the VMDetail port-forwards card (URL round-trip via `?port_protocol=`). Whitespace is trimmed before comparison; empty value disables the filter; anything other than tcp/udp returns 400 `invalid_protocol`. Mirrors the bulk_delete `protocol` selector that already lets operators wipe every rule of a given protocol so the list and bulk-action surfaces agree. Closes the operator query *"show me every UDP rule on this VM"* / *"audit the TCP listeners on the bastion"* that the existing `?search=` filter would cover fuzzily (substring-matching `tcp` would also match a `tcp-proxy` description on a UDP rule). Composes additively with the existing `?tag=` / `?search=` / `?sort=` / `?order=` and pagination params so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_network.go` `ListPorts` parses `?protocol=` once (trimmed + lowercased), validates against `types.ProtocolTCP` / `types.ProtocolUDP`, and applies the predicate between `?tag=` and `?search=` so the post-filter / pre-pagination `X-Total-Count` stays correct; invalid values fail fast with 400 `invalid_protocol`. `internal/cli/network.go` adds `--protocol` to `port list` with the same trim + lowercase + whitelist pipeline (`invalid --protocol "<value>": must be 'tcp' or 'udp'`). `web/src/pages/VMDetail.jsx` adds a third `<select>` (`port-protocol-filter`) next to the existing sort dropdowns with "Any protocol / TCP / UDP" options, round-tripped through the URL via `?port_protocol=`; the page snaps back to page 1 whenever the protocol filter changes. `web/src/api/client.ts` extends `ports.list({ protocol })` to forward the new query param. `docs/openapi.yaml` documents the new `protocol` query parameter and the 400 response. `tests/web/mock-server.js` mirrors the predicate plus the 400 path so Playwright can validate every branch. Coverage: 7 API integration cases (`TestListPorts_FilterByProtocol_TCP`, `_UDP`, `_CaseInsensitive`, `_WhitespaceTrimmed`, `_EmptyIsNoOp`, `_InvalidReturns400`, `_ComposesWithTag`, `_TotalCountReflectsFiltered`); 6 CLI cases (`TestCLI_PortList_FilterByProtocol_TCP`, `_UDP`, `_CaseInsensitive`, `_EmptyOmitsFilter`, `_RejectsInvalidProtocol`, `_ComposesWithTag`); 3 Playwright cases (`protocol filter narrows the port-forward list and round-trips through the URL`, `protocol filter is case-insensitive`, `protocol filter "Any protocol" returns every rule`). CLAUDE.md REST quick reference for `GET /vms/{id}/ports` updated accordingly. |
| 5.4.27 | `?source_vm=<vm-id>` filter on `GET /api/v1/images` — case-insensitive exact-match against the image's `source_vm` field (the VM ID the image was exported from), mirrored through `vmsmith image list --source-vm <id>` and a debounced "Filter by source VM…" input on the ImageList page (URL round-trip via `?source_vm=`). Whitespace is trimmed before comparison; empty value disables the filter. Closes the operator query *"show me every image exported from VM `vm-1741…`"* / *"audit the export history for the bastion VM"* that the existing `?search=` filter (5.4.9) covers fuzzily but not precisely — `?search=vm-1741` would also match any image whose name or description happens to contain the numeric suffix, and `?tag=` cannot answer it without pre-tagging every export. The `source_vm` field is already exposed on the `Image` type (set by `internal/storage/image.go` when `POST /images` is invoked from a VM); the filter wires it into the list surface. Composes additively with the existing `?tag=` / `?search=` filters and the sort + pagination params so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_image.go` `ListImages` trims + lowercases the `?source_vm=` value once and applies `strings.EqualFold(img.SourceVM, sourceVMFilter)` inside a new filter pass between `?tag=` and `?search=` so the post-filter / pre-pagination `X-Total-Count` stays correct. `internal/cli/image.go` adds `--source-vm` to `image list` with the same trim + lowercase + predicate pipeline so the in-process CLI path matches the daemon exactly. `web/src/pages/ImageList.jsx` adds a 250 ms-debounced text input next to the existing search box with an `X` clear button and URL round-trip via `?source_vm=`; the page resets to page 1 whenever the filter changes; empty-state branched for the new filter. `web/src/api/client.ts` extends `images.list({ sourceVM })` to forward the new query parameter. `docs/openapi.yaml` documents the new `source_vm` query parameter on `GET /images`. `CLAUDE.md` REST quick reference for `GET /images` extended. `tests/web/mock-server.js` mirrors the predicate so the JSDOM/Playwright suites can exercise the new query path. Coverage: 8 API integration cases (`TestListImages_FilterBySourceVM_ExactMatch`, `_IsCaseInsensitive`, `_TrimsWhitespace`, `_EmptyIsNoOp`, `_NoMatch`, `_IgnoresImagesWithEmptySourceVM`, `_ComposesWithTag`, `_ComposesWithSearch`, `_TotalCountReflectsFiltered`); 5 CLI cases (`TestCLI_ImageList_FilterBySourceVM_ExactMatch`, `_IsCaseInsensitive`, `_EmptyOmitsFilter`, `_NoMatch`, `_ComposesWithTag`); 1 JSDOM/mock-GUI case (`image source-vm filter narrows the image list and round-trips through the URL`). |
| 5.4.28 | Time-range filter on snapshot list — extends `GET /api/v1/vms/{vmID}/snapshots` with `?since=<rfc3339>` and `?until=<rfc3339>` inclusive bounds on `created_at`, mirrored through the CLI (`vmsmith snapshot list <vm-id> --since --until`). Whitespace is trimmed before parsing; empty disables the bound; invalid values return 400 `invalid_since` / `invalid_until`. A snapshot with a zero `created_at` is filtered OUT whenever any bound is set — operators querying a time window don't want unbounded entries silently included. Composes additively with the existing `?tag=` / `?search=` filters and the sort + pagination params so `X-Total-Count` reflects the post-filter population. Closes the "show me every snapshot taken before the bad deploy" / "audit nightly snapshots from last week" operator queries that the existing `?search=` filter cannot answer (timestamps don't appear in the search haystack) and that name-prefix matching only addresses fuzzily (only if the snapshot naming convention encodes the timestamp). | S | ✅ Done — `internal/api/parse_time_range.go` adds the shared `parseTimeRangeParam` helper (whitespace-trim + RFC3339 / RFC3339Nano accepted, anything else returns the typed `*types.APIError` so the handler can pass it straight through `writeAPIError`) plus the `snapshotInTimeRange` predicate (inclusive bounds, zero-time filtered out when any bound set). `internal/api/handlers_snapshot.go` `ListSnapshots` parses both params up-front (400 fail-fast) and applies the predicate between `?tag=` and `?search=` so the post-filter `X-Total-Count` stays correct. `internal/cli/parse_time_range.go` mirrors the parser shape for the CLI (`parseCLITimeRange` returns a usage error that name-checks the offending flag); `internal/cli/snapshot.go` adds `--since` / `--until` flags to `snap list` with the same predicates inside the in-process filter loop. `docs/openapi.yaml` documents both `since` / `until` parameters with their RFC3339 / inclusive / zero-time semantics. CLAUDE.md REST quick reference for `GET /vms/{id}/snapshots` extended accordingly. Coverage: 9 unit cases on `parseTimeRangeParam` / `snapshotInTimeRange` (empty / whitespace / RFC3339 / RFC3339Nano / trimmed / invalid-text / invalid-date / invalid-epoch; predicate covers no-bounds / in-range / before / after / equal-endpoints / zero-with-bounds-excluded / zero-without-bounds-included); 8 API integration cases (`TestListSnapshots_FilterBySince`, `_FilterByUntil`, `_FilterBySinceAndUntil`, `_FilterBySince_Inclusive`, `_FilterByInvalidSince`, `_FilterByInvalidUntil`, `_FilterBySince_EmptyIsNoOp`, `_FilterByTimeRange_ExcludesZeroCreatedAt`, `_FilterBySince_ComposesWithTagAndSearch`); 6 CLI cases (`TestCLI_SnapshotList_FilterBySince`, `_FilterByUntil`, `_FilterBySinceAndUntil`, `_RejectsInvalidSince`, `_RejectsInvalidUntil`, `_EmptySinceOmitsFilter`). |
| 5.4.29 | Time-range filter on image list — extends `GET /api/v1/images` with `?since=<rfc3339>` and `?until=<rfc3339>` inclusive bounds on `created_at`, mirrored through the CLI (`vmsmith image list --since --until`) and two datetime-local inputs on the ImageList page (URL round-trip via `?since=` / `?until=`). Whitespace is trimmed before parsing; empty disables the bound; invalid values return 400 `invalid_since` / `invalid_until`. An image with a zero `created_at` is filtered OUT whenever any bound is set — operators querying a time window don't want unbounded entries silently included. Composes additively with the existing `?tag=` / `?source_vm=` / `?search=` filters and the sort + pagination params so `X-Total-Count` reflects the post-filter population. Closes the "show me every image uploaded last week" / "audit qcow2 imports from before the bad deploy" operator queries that `?search=` cannot answer (timestamps don't appear in the search haystack) and that `?tag=<release-cohort>` only answers when operators pre-tag releases by date. Mirrors the 5.4.28 snapshot time-range filter on the image surface. | S | ✅ Done — `internal/api/handlers_image.go` `ListImages` parses both params via the shared `parseTimeRangeParam` helper (introduced in 5.4.28) and applies the `snapshotInTimeRange` predicate between `?source_vm=` and `?search=` so the post-filter / pre-pagination `X-Total-Count` stays correct; 400 `invalid_since` / `invalid_until` on garbage. `internal/cli/image.go` adds `--since` / `--until` flags to `image list` using the same `parseCLITimeRange` + `snapshotInCLITimeRange` helpers so the in-process CLI path matches the daemon byte-for-byte. `web/src/pages/ImageList.jsx` adds two datetime-local inputs with a "Clear range" button and URL round-trip via `?since=` / `?until=`; the page resets to page 1 whenever the range changes; empty-state branched with a tailored message. `web/src/api/client.ts` extends `images.list({ since, until })` to forward the new query parameters. `tests/web/mock-server.js` mirrors the predicate (including the 400 path and the zero-`created_at` exclusion) so the JSDOM/Playwright suites can exercise the new query path; the seed images carry fixed 2026-05-05 / 2026-05-12 timestamps so the boundary tests are deterministic. `docs/openapi.yaml` documents the new `since` / `until` query parameters on `GET /images`. `CLAUDE.md` REST quick reference for `GET /images` extended. Coverage: 8 API integration cases (`TestListImages_FilterBySince`, `_FilterByUntil`, `_FilterBySinceAndUntil`, `_FilterBySince_Inclusive`, `_FilterByInvalidSince`, `_FilterByInvalidUntil`, `_FilterBySince_EmptyIsNoOp`, `_FilterByTimeRange_ExcludesZeroCreatedAt`, `_FilterBySince_ComposesWithTagAndSearch`); 6 CLI cases (`TestCLI_ImageList_FilterBySince`, `_FilterByUntil`, `_FilterBySinceAndUntil`, `_RejectsInvalidSince`, `_RejectsInvalidUntil`, `_EmptySinceOmitsFilter`); 3 Playwright/mock-GUI cases (`?since= filter narrows the image list by created_at and round-trips through the URL`, `?until= filter narrows the image list by created_at upper bound`, `time-range filter empty-state surfaces a tailored message`). |
| 5.4.30 | Time-range filter on VM list — extends `GET /api/v1/vms` with `?since=<rfc3339>` and `?until=<rfc3339>` inclusive bounds on `created_at`, mirrored through `vmsmith vm list --since --until` and a pair of `datetime-local` "Created since … until" inputs on the VMList page (URL round-trip via `?since=` / `?until=`, with a "Clear range" button that drops both at once). Reuses the shared `parseTimeRangeParam` / `snapshotInTimeRange` helpers introduced in 5.4.28 — those primitives are time-only (`time.Time` in, `bool` out) so the snapshot-derived name is only a naming-history artefact, not a coupling. Closes the *"show me every VM created last week"* / *"audit which machines were spun up before the bad deploy"* operator queries that `?search=` cannot answer (timestamps don't appear in the search haystack) and that `?tag=` only answers when operators pre-tag fleets by date. Mirrors the snapshot (5.4.28) and image (5.4.29) time-range filters on the third large list surface. Semantics match 5.4.28 / 5.4.29 exactly: whitespace-trimmed; empty disables the bound; invalid values return 400 `invalid_since` / `invalid_until`; a VM with a zero `created_at` is filtered OUT whenever any bound is set. Composes additively with `?tag=` / `?status=` / `?search=` / `?image=` / `?default_user=` / `?auto_start=` / `?locked=` / sort / pagination so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_vm.go` `ListVMs` parses both params up-front (400 fail-fast) via the shared helper and adds a single `snapshotInTimeRange(vm.CreatedAt, …)` call inside the existing filter loop between the tristate-bool checks and `?search=` so the post-filter `X-Total-Count` stays correct. `internal/cli/vm.go` `vm list` mirrors the parse + predicate via `parseCLITimeRange` / `snapshotInCLITimeRange` (no extra dependencies; the in-process CLI path produces identical matching to the over-HTTP path). `web/src/pages/VMList.jsx` adds two `<input type="datetime-local">` controls plus a contextual "Clear range" button next to the existing sort / auto-start / locked dropdowns; URL round-trip via `?since=` / `?until=`; the page snaps back to page 1 whenever either bound changes; a tiny `datetimeLocalToISO` helper converts the browser's naive local-time string to RFC3339-UTC before forwarding. `web/src/api/client.ts` extends `vms.list({ since, until })` to forward the new query params. `tests/web/mock-server.js` mirrors the predicate (whitespace-trim + invalid → 400 + zero-time-excluded) AND pins the seeded VM `created_at` to 2026-05-05 / 2026-05-15 so the Playwright boundary check at 2026-05-10 cleanly splits them. `docs/openapi.yaml` documents both `since` / `until` parameters on `GET /vms`. `CLAUDE.md` REST quick reference for `GET /vms` extended. Coverage: 9 API integration cases (`TestListVMs_FilterBySince`, `_FilterByUntil`, `_FilterBySinceAndUntil`, `_FilterBySince_Inclusive`, `_FilterByInvalidSince`, `_FilterByInvalidUntil`, `_FilterBySince_EmptyIsNoOp`, `_FilterByTimeRange_ExcludesZeroCreatedAt`, `_FilterBySince_ComposesWithTagAndSearch`); 6 CLI cases (`TestCLI_VMList_FilterBySince`, `_FilterByUntil`, `_FilterBySinceAndUntil`, `_RejectsInvalidSince`, `_RejectsInvalidUntil`, `_EmptySinceOmitsFilter`); 3 Playwright cases (`created-since filter narrows the VM list and round-trips through the URL`, `created-until filter narrows the VM list by upper bound`, `created-at range empty-state when both bounds exclude every VM`). |
| 5.4.32 | Time-range filter on webhook list — extends `GET /api/v1/webhooks` with `?since=<rfc3339>` and `?until=<rfc3339>` inclusive bounds on `created_at`, mirrored through the CLI (`vmsmith webhook list --since --until`). Reuses the shared `parseTimeRangeParam` / `snapshotInTimeRange` helpers from 5.4.28 so semantics are byte-identical across snapshots (5.4.28), images (5.4.29), VMs (5.4.30), templates (5.4.31), and now webhooks. Closes the "show me every webhook registered last quarter" / "audit which delivery targets predate the receiver migration" operator queries that `?search=` cannot answer (timestamps don't appear in the search haystack) and that `?tag=` only answers when operators pre-tag webhooks by date. Whitespace trimmed; empty disables the bound; invalid values return 400 `invalid_since` / `invalid_until`; a webhook with a zero `created_at` is filtered OUT whenever any bound is set. Composes additively with `?tag=` / `?event_type=` / `?search=` / sort / pagination so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_webhook.go` `ListWebhooks` parses both params up-front via the shared helper (400 fail-fast) and applies the predicate between `?event_type=` and `?search=` so the post-filter / pre-pagination `X-Total-Count` stays correct. `internal/cli/webhook.go` `webhook list` validates the flags client-side via `parseCLITimeRange` (with the same usage-error name-checks) and forwards `?since=` / `?until=` verbatim to the daemon. `docs/openapi.yaml` documents both `since` / `until` parameters on `GET /webhooks`. `CLAUDE.md` REST quick reference for `GET /webhooks` extended. Coverage: 8 API integration cases (`TestListWebhooks_FilterBySince`, `_FilterByUntil`, `_FilterBySinceAndUntil`, `_FilterBySince_Inclusive`, `_FilterByInvalidSince`, `_FilterByInvalidUntil`, `_FilterBySince_EmptyIsNoOp`, `_FilterByTimeRange_ExcludesZeroCreatedAt`, `_FilterBySince_ComposesWithTagAndSearch`); 6 CLI cases (`TestCLI_WebhookList_ForwardsSince`, `_ForwardsUntil`, `_ForwardsSinceAndUntil`, `_RejectsInvalidSince`, `_RejectsInvalidUntil`, `_EmptySinceOmitsParam`). |
| 5.4.31 | Time-range filter on template list — extends `GET /api/v1/templates` with `?since=<rfc3339>` and `?until=<rfc3339>` inclusive bounds on `created_at`, mirrored through the CLI (`vmsmith template list --since --until`) and two `datetime-local` inputs on the TemplateList page (URL round-trip via `?since=` / `?until=`, with a "Clear range" button that drops both at once). Reuses the shared `parseTimeRangeParam` / `snapshotInTimeRange` helpers from 5.4.28 so semantics are byte-identical across snapshots (5.4.28), images (5.4.29), VMs (5.4.30), and now templates. Closes the "show me every template authored last quarter" / "audit which templates predate the rocky10 base image refresh" operator queries that `?search=` cannot answer (timestamps don't appear in the search haystack) and that `?tag=` only answers when operators pre-tag templates by date. Whitespace trimmed; empty disables the bound; invalid values return 400 `invalid_since` / `invalid_until`; a template with a zero `created_at` is filtered OUT whenever any bound is set. Composes additively with `?tag=` / `?image=` / `?search=` / sort / pagination so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_template.go` `ListTemplates` parses both params up-front via the shared helper (400 fail-fast) and adds a single `snapshotInTimeRange(tpl.CreatedAt, …)` call inside the filter loop between the `?image=` exact-match and `?search=` substring filters so the post-filter `X-Total-Count` stays correct. `internal/cli/template.go` `template list` mirrors the parse + predicate via `parseCLITimeRange` / `snapshotInCLITimeRange` and adds `--since` / `--until` flags. `web/src/pages/TemplateList.jsx` adds two `<input type="datetime-local">` controls plus a contextual "Clear range" button next to the existing search + sort controls; URL round-trip via `?since=` / `?until=`; the page snaps back to page 1 whenever either bound changes; reuses the shared `datetimeLocalToISO` helper to convert the browser's naive local-time string to RFC3339-UTC before forwarding. `web/src/api/client.ts` extends `templates.list({ since, until })` to forward the new query params. `tests/web/mock-server.js` mirrors the predicate (whitespace-trim + invalid → 400 + zero-time-excluded) and pins seeded template `created_at` so Playwright boundary checks are deterministic. `docs/openapi.yaml` documents both `since` / `until` parameters on `GET /templates`. `CLAUDE.md` REST quick reference for `GET /templates` extended. Coverage: 9 API integration cases (`TestListTemplates_FilterBySince`, `_FilterByUntil`, `_FilterBySinceAndUntil`, `_FilterBySince_Inclusive`, `_FilterByInvalidSince`, `_FilterByInvalidUntil`, `_FilterBySince_EmptyIsNoOp`, `_FilterByTimeRange_ExcludesZeroCreatedAt`, `_FilterBySince_ComposesWithTagAndSearch`); 6 CLI cases (`TestCLI_TemplateList_FilterBySince`, `_FilterByUntil`, `_FilterBySinceAndUntil`, `_RejectsInvalidSince`, `_RejectsInvalidUntil`, `_EmptySinceOmitsFilter`); 3 Playwright/mock-GUI cases (`?since= filter narrows the template list by created_at and round-trips through the URL`, `?until= filter narrows the template list by created_at upper bound`, `time-range filter empty-state surfaces a tailored message`). |
| 5.4.33 | Server-side filters on the SSE event stream — extends `GET /api/v1/events/stream` with `?vm_id=`, `?type=`, `?type_prefix=`, `?source=`, `?severity=`, `?actor=`, `?resource_id=`, and `?search=` so the daemon drops non-matching events on its side instead of forcing every consumer to discard cross-tenant noise client-side. Filters apply to both the replay window after `Last-Event-ID` / `?since=<seq>` AND to live events arriving on the bus — the SSE stream and `GET /events` now agree on membership byte-for-byte. Mirrors the predicate set already shipped on the paginated list endpoint; semantics are case-sensitive exact-match on VMID / Type / Source / Severity / Actor / ResourceID, case-insensitive prefix on `type_prefix`, and case-insensitive substring on `search` (delegated to the shared `EventMatchesSearch` haystack). The CLI (`vmsmith events follow --vm --type --type-prefix --source --severity --actor --resource-id --search`) forwards filters as query params while keeping the client-side predicate as defense-in-depth; the `useEventStream` React hook accepts an optional `filter` object that is appended to the EventSource URL verbatim. Closes the operator query "tail just vm-1's events" / "stream every snapshot.* event family" that previously required the consumer to pay for every other event the bus produces. The replay-overflow 410 short-circuit and `?since=<seq>` cursor are unchanged — the filter set is purely additive. | S | ✅ Done — `pkg/types/event_filter.go` introduces `EventStreamFilter` + `EventMatchesStreamFilter` (case-sensitive exact-match on `VMID`/`Type`/`Source`/`Severity`/`Actor`/`ResourceID`, case-insensitive prefix on `TypePrefix`, delegation to `EventMatchesSearch` on `Search`; nil event → false, empty filter → true; `HasAny()` short-circuits the no-filter fast path). `internal/api/handlers_events.go` `StreamEvents` parses the same set of trimmed/lowercased query params as `ListEvents` once at the top of the handler and gates both the replay loop and the live bus loop on the new predicate — non-matching events are silently skipped before they reach the SSE writer. `internal/cli/events.go` `streamEventsOnce` collects the filter into a single `url.Values` (alongside the existing `since=` replay cursor) so the daemon sees every set field; the existing client-side `filterEvents` predicate stays in place as a defense-in-depth net. `web/src/hooks/useEventStream.js` accepts an optional `filter` prop, stashes it behind a ref so the EventSource URL always reflects the latest value on reconnect, and extends `buildStreamUrl` to append the supported keys (`vm_id`, `type`, `type_prefix`, `source`, `severity`, `actor`, `resource_id`, `search`). `CLAUDE.md` REST quick reference adds a dedicated `GET /events/stream` row documenting the new filter set and reiterating the 410 / replay-cursor contract. Coverage: 8 `EventMatchesStreamFilter` unit cases (empty-matches / nil-never-matches / VMID-case-sensitive / per-field exact-match {Type,Source,Severity,Actor,ResourceID} / TypePrefix-case-insensitive / Search-delegation / all-fields-must-match / HasAny zero+per-field); 4 SSE API integration cases (`TestStreamEvents_FilterReplay_VMID`, `_TypePrefix`, `_Search`, `TestStreamEvents_FilterLive_VMID`); 2 CLI cases (`TestCLI_EventsFollow_ForwardsFiltersAsQueryParams`, `_EmptyFilterOmitsQueryParams`). |
| 5.4.34 | Time-range filter on logs list — extends `GET /api/v1/logs` with `?until=<rfc3339>` inclusive upper bound on the entry's `timestamp`, mirrored through `vmsmith logs list --until` (accepts Go-duration shorthand like `5m` or RFC3339) and a pair of `datetime-local` "Since … Until" inputs on the LogViewer page (URL round-trip via `?since=` / `?until=`). Also tightens the pre-existing `?since=` to fail-fast with `400 invalid_since` on garbage timestamps (previously silently ignored) so both endpoints participate in the 5.4.28-5.4.32 time-range family contract. Semantic note: `since` retains its strict-after semantics inherited from `logger.Entries` (entries strictly AFTER the bound) while `until` is INCLUSIVE (at-or-before), matching the snapshot / image / VM / template / webhook family — both contracts are documented explicitly in CLAUDE.md and the OpenAPI description. Closes the "show me what was happening between 2:00 and 2:30 AM during the incident" / "give me logs from before the bad deploy" operator queries that `?search=` cannot answer (timestamps are not in the haystack) and that `?since=` alone could only answer if the caller polled and post-filtered client-side. Composes additively with the existing `level` / `source` / `vm_id` / `search` / `sort` / `order` / pagination so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_logs.go` `GetLogs` now parses both `since` and `until` via the shared `parseTimeRangeParam` helper (400 fail-fast); `since` still flows into `logger.Get().Entries(level, since, 0)` for strict-after semantics, `until` is applied as an inclusive post-filter loop before the source/vm_id/search filters so the post-filter / pre-pagination `X-Total-Count` stays correct. `internal/cli/logs.go` adds `--until` next to `--since` and reuses a new `parseUntilFlag` helper in `internal/cli/events.go` that mirrors `parseSinceFlag` (Go-duration shorthand or RFC3339; the duration variant resolves to "<now> minus <duration>", so `--until 1m` reads as "everything at-or-before one minute ago"). `web/src/pages/LogViewer.jsx` adds two `<input type="datetime-local">` controls plus a contextual "Clear range" button next to the existing search / VM-ID inputs; URL round-trip via `?since=` / `?until=`; the page snaps back to page 1 whenever either bound changes; the empty-state copy distinguishes the time-range branch ("No log entries in the selected time range"). `web/src/api/client.ts` extends `logs.list({ since, until })` to forward the new query params. `tests/web/mock-server.js` mirrors the predicate (whitespace-trim + invalid → 400 + strict-after `since` + inclusive `until`) so the Playwright suite can exercise every branch. `docs/openapi.yaml` documents both `since` / `until` parameters on `GET /logs`. `CLAUDE.md` REST quick reference for `GET /logs` extended. Coverage: 7 API integration cases (`TestGetLogs_FilterByUntil`, `_FilterByUntil_Inclusive`, `_FilterBySinceAndUntil`, `_FilterByInvalidSince`, `_FilterByInvalidUntil`, `_FilterByUntil_EmptyIsNoOp`, `_FilterByUntil_ComposesWithLevel`, `_FilterByUntil_TotalCountReflectsFiltered`); 5 CLI cases (`TestCLI_LogsList_ForwardsUntil`, `_UntilDurationBecomesRFC3339`, `_UntilInvalidIsRejected`, `_ForwardsSinceAndUntil`, `_EmptyUntilOmitsParam`). |
| 5.4.35 | Delivery-status filter on webhook list — extends `GET /api/v1/webhooks` with `?delivery_status=<never\|healthy\|failing>`, a categorical filter on the webhook's most-recent delivery classification. The three values form a complete partition of every persisted webhook: `never` = `last_delivery_at` is zero (no attempt yet); `healthy` = last attempt returned 2xx and `last_error` is empty; `failing` = an attempt existed but did not meet the healthy contract (transport error, 4xx, 5xx, 3xx, or 2xx with a stale `last_error`). Closes the operator queries *"show me failing webhooks"* / *"show me healthy webhooks"* / *"show me webhooks I registered but that haven't fired yet"* that `?search=` cannot answer (delivery state doesn't appear in the haystack) and that the existing `?tag=` / `?event_type=` filters can't answer (those slice by definition, not by runtime health). Mirrored through `vmsmith webhook list --delivery-status <s>` and a Settings page dropdown that round-trips through `?delivery_status=` and renders a dedicated empty-state for each value. Case-insensitive; whitespace-trimmed; empty disables; unknown values return 400 `invalid_delivery_status`. Composes additively with `?tag=` / `?event_type=` / `?since=` / `?until=` / `?search=` / sort / pagination so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `pkg/types/webhook_delivery_status.go` introduces `WebhookDeliveryStatus(*Webhook) string` (nil → `never`; `LastDeliveryAt.IsZero()` takes precedence; `LastStatus` in 200..299 with empty `LastError` → `healthy`; anything else → `failing`) plus `IsValidWebhookDeliveryStatus(s)` for input validation. `internal/api/handlers_webhook.go` `ListWebhooks` parses + validates the param up-front (400 `invalid_delivery_status` on garbage) and applies the predicate between the time-range filter and the search filter so `X-Total-Count` reflects the post-filter population. `internal/cli/webhook.go` adds `--delivery-status` with client-side validation (rejecting unknown values before contacting the daemon) and forwards the normalised lowercase value verbatim. `web/src/pages/Settings.jsx` adds a `<select>` dropdown next to the existing sort / order / time-range controls with four options (All / Never delivered / Healthy / Failing), URL round-trip via `?delivery_status=`, page reset on filter change, and a dedicated empty-state for each bucket; the stale-link path falls back to `''` so a typo in the URL can't wedge the page. `web/src/api/client.ts` extends `webhooks.list({ deliveryStatus })` to forward the param. `tests/web/mock-server.js` mirrors the predicate (400 path + the three-bucket classification logic). `docs/openapi.yaml` documents the new param on `GET /webhooks`. `CLAUDE.md` REST quick reference extended. Coverage: 11 unit cases (`TestWebhookDeliveryStatus_Nil`, `_Never_ZeroLastDeliveryAt`, `_Never_PrecedesLegacyStatus`, `_Healthy_200`, `_Healthy_204`, `_Failing_TransportError`, `_Failing_5xx`, `_Failing_4xx`, `_Failing_3xx`, `_Failing_200WithError`, `TestIsValidWebhookDeliveryStatus`); 8 API integration cases (`TestListWebhooks_FilterByDeliveryStatus_Never`, `_Healthy`, `_Failing`, `_CaseInsensitive`, `_TrimsWhitespace`, `_EmptyIsNoOp`, `_Invalid`, `_ComposesWithTag`); 4 CLI cases (`TestCLI_WebhookList_ForwardsDeliveryStatus`, `_DeliveryStatus_NormalisesCaseAndWhitespace`, `_RejectsInvalidDeliveryStatus`, `_EmptyDeliveryStatusOmitsParam`); 1 Playwright case (`delivery-status dropdown filters the webhook list and round-trips through the URL`) exercising the three-bucket classification end-to-end via the mock `/test` endpoint. |
| 5.4.36 | Per-network filter on the VM list — extends `GET /api/v1/vms` with `?network=<name>`, a case-insensitive exact-match (any-of) against the name of any of the VM's additional network attachments (`spec.networks[].name`), mirrored through the CLI (`vmsmith vm list --network`) and a debounced "Filter by network" input on the VMList page that round-trips through `?network=` in the URL. Closes the operator query *"show me every VM attached to `data-net`"* — operators name extra networks (`data-net`, `storage-net`) for organisational/team grouping and want to slice the fleet by network membership the same way `?image=` (5.4.22) slices by base image and `?default_user=` (5.4.23) slices by SSH user. The implicit primary NAT network is intentionally NOT matched: it is the default every VM carries and is not represented in `spec.networks`, so the filter only ever scopes to the explicitly-attached extra networks. Composes additively with every existing VM list filter so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `pkg/types/vm_network_match.go` adds `VMMatchesNetwork(vm, network)` (any-of case-insensitive exact-match over `spec.networks[].name`; empty query short-circuits to match-all; nil VM never matches), shared verbatim between the API handler and the CLI so the case-insensitive contract can't regress in one surface. `internal/api/handlers_vm.go` `ListVMs` reads + trims + lowercases `?network=` and applies the predicate inside the existing filter loop (between the auto_start/locked flags and the time-range / search filters) so it composes additively and `X-Total-Count` stays correct. `internal/cli/vm.go` adds the `--network` flag to `vm list`, routing through the same `types.VMMatchesNetwork`. `web/src/api/client.ts` extends `vms.list({ network })`; `web/src/pages/VMList.jsx` adds a 250 ms-debounced "Filter by network" input (with clear button) next to the image / default-user inputs, URL round-trip via `?network=`, and page-reset on change. `tests/web/mock-server.js` mirrors the predicate and seeds `web-server` on `data-net` + `db-server` on `storage-net`. `docs/openapi.yaml` adds the `NetworkFilter` parameter under `/vms`; `CLAUDE.md` REST quick reference extended. Coverage: unit (`pkg/types/vm_network_match_test.go` × 7 — nil, empty-matches-all, exact, case-insensitive, no-match, no-networks, partial-is-not-a-match); API integration (`TestListVMs_FilterByNetwork_ExactMatch`, `_IsCaseInsensitive`, `_TrimsWhitespace`, `_EmptyIsNoOp`, `_NoMatch`, `_ComposesWithStatus`, `_TotalCountReflectsFiltered`); CLI (`TestCLI_VMList_FilterByNetwork_ExactMatch`, `_IsCaseInsensitive`, `_NoMatch`, `_ComposesWithStatus`); Playwright (`network filter narrows the VM list to a single network and round-trips through the URL`, `network filter is case-insensitive`, `network filter matches no VMs when query has no hits`). |
| 5.4.37 | Active filter on webhook list — extends `GET /api/v1/webhooks` with `?active=true|false`, a tristate boolean exact-match on the webhook's `active` flag, mirrored through `vmsmith webhook list --active` and an Active dropdown on the Settings page that round-trips through `?active=`. Closes the operator queries *"show me only disabled webhooks"* / *"show me only live webhooks"* that `?delivery_status=` (runtime health) and `?event_type=` (subscription) cannot answer — `active` is the config toggle that PATCH can flip but the list could not filter by. Case-insensitive `true`/`false` with `1`/`0` aliases; whitespace-trimmed; empty disables; anything else returns 400 `invalid_active`. Composes additively with `?tag=` / `?event_type=` / `?since=` / `?until=` / `?delivery_status=` / `?search=` / sort / pagination so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_webhook.go` `ListWebhooks` parses the param up-front via the shared `parseTristateBoolParam` helper (400 `invalid_active` on garbage) and applies the predicate between the delivery-status filter and the search filter so `X-Total-Count` stays post-filter correct. `internal/cli/webhook.go` adds `--active` with client-side validation via the shared `parseCLITristateBool` helper and forwards the normalised `true`/`false` verbatim. `web/src/pages/Settings.jsx` adds an Active `<select>` (All / Active / Inactive) next to the delivery-status dropdown, URL round-trip via `?active=`, page reset on change, and a dedicated empty-state for each bucket; the stale-link path falls back to `''`. `web/src/api/client.ts` extends `webhooks.list({ active })`. `tests/web/mock-server.js` mirrors the tristate parse (400 path + boolean filter). `docs/openapi.yaml` documents the new param on `GET /webhooks` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference extended. Coverage: 8 API integration cases (`TestListWebhooks_FilterByActive_True`, `_False`, `_NumericAliases`, `_CaseInsensitive`, `_TrimsWhitespace`, `_EmptyIsNoOp`, `_Invalid`, `_ComposesWithTag`); 5 CLI cases (`TestCLI_WebhookList_ForwardsActiveTrue`, `_ForwardsActiveFalse`, `_Active_NormalisesAliasesAndCase`, `_RejectsInvalidActive`, `_EmptyActiveOmitsParam`); 1 Playwright case (`active dropdown filters the webhook list and round-trips through the URL`). |
| 5.4.38 | Default-user filter on the template list — extends `GET /api/v1/templates` with `?default_user=<value>`, a case-insensitive exact-match against the template's `default_user` field, mirrored through `vmsmith template list --default-user` and a debounced "Filter by default user" input on the TemplateList page that round-trips through `?default_user=` in the URL. Closes the operator query *"show me every template that provisions the `deploy` SSH user"* — templates carry a `default_user` (2.4.1) that VMs already filter by (5.4.23), but the template list could only slice by `tag` / `image` / `search`. Unlike the VM filter there is intentionally NO empty-means-root fallback: a template's empty `default_user` means "use the image's built-in user", so an empty stored value never matches a non-empty query. Whitespace-trimmed; empty disables; composes additively with `?tag=` / `?image=` / `?since=` / `?until=` / `?search=` / sort / pagination so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_template.go` `ListTemplates` reads + trims + lowercases `?default_user=` and applies the case-insensitive exact-match predicate between the `?image=` and `?since=`/`?until=` filters so the post-filter `X-Total-Count` stays correct. `internal/cli/template.go` adds the `--default-user` flag to `template list`, applying the identical in-process predicate. `web/src/api/client.ts` extends `templates.list({ defaultUser })`; `web/src/pages/TemplateList.jsx` adds a 250 ms-debounced "Filter by default user" input (with clear button) next to the image filter, URL round-trip via `?default_user=`, page-reset on change, and a tailored empty-state. `tests/web/mock-server.js` mirrors the filter and the seeded templates carry distinct users (`ubuntu` / `root`). `docs/openapi.yaml` documents the new `default_user` query parameter under `/templates` (regenerated `schema.d.ts`); `CLAUDE.md` REST quick reference extended. Coverage: API integration (`TestListTemplates_FilterByDefaultUser_ExactMatch`, `_IsCaseInsensitive`, `_TrimsWhitespace`, `_EmptyIsNoOp`, `_EmptyStoredNeverMatches`, `_NoMatch`, `_ComposesWithTag`, `_TotalCountReflectsFiltered`); CLI (`TestCLI_TemplateList_FilterByDefaultUser_ExactMatch`, `_CaseInsensitive`, `_EmptyOmitsFilter`, `_NoMatch`); Playwright (`default-user filter narrows the template list and round-trips through the URL`, `default-user filter is case-insensitive`, `default-user filter matches no templates when query has no hits`). |
| 5.4.39 | Time-range filter on schedule list — extends `GET /api/v1/schedules` with `?since=<rfc3339>` and `?until=<rfc3339>` inclusive bounds on `created_at`, mirrored through the CLI (`vmsmith schedule list --since --until`) and a pair of `datetime-local` "Created since … until" inputs on the Schedules page (URL round-trip via `?since=` / `?until=`, with a "Clear range" button that drops both at once). Reuses the shared `parseTimeRangeParam` / `snapshotInTimeRange` helpers so semantics are byte-identical across snapshots (5.4.28), images (5.4.29), VMs (5.4.30), templates (5.4.31), and webhooks (5.4.32) — and now schedules, the last major list surface still missing the family. Closes the *"show me every schedule authored last quarter"* / *"audit which automation predates the cron migration"* operator queries that `?search=` cannot answer (timestamps don't appear in the haystack) and that `?vm_id=` / `?action=` cannot answer at all. Whitespace trimmed; empty disables the bound; invalid values return 400 `invalid_since` / `invalid_until`; a schedule with a zero `created_at` is filtered OUT whenever any bound is set. Composes additively with `?vm_id=` / `?action=` / `?enabled=` / `?search=` / sort / pagination so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_schedule.go` `ListSchedules` parses both bounds via the shared `parseTimeRangeParam` helper and applies the predicate between the `enabled` filter and the `search` filter so `X-Total-Count` stays post-filter correct. `internal/cli/schedule.go` adds `--since` / `--until` with client-side validation via the shared `parseCLITimeRange` helper and forwards the values verbatim. `web/src/pages/Schedules.jsx` adds the `datetime-local` inputs + Clear-range button, URL round-trip, page reset on change, and an extended empty-state. `web/src/api/client.ts` extends `schedules.list({ since, until })`. `tests/web/mock-server.js` mirrors the parse (400 paths + time-range filter). `docs/openapi.yaml` documents both params on `GET /schedules` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference extended. Coverage: 1 API integration case (`TestListSchedules_FilterByTimeRange` — inclusive lower/upper bounds, both-bound window, zero-time exclusion, action compose, 400 paths); 3 CLI cases (`TestCLI_ScheduleList_ForwardsTimeRange`, `_RejectsInvalidSince`, `_RejectsInvalidUntil`); 3 Playwright cases (since narrows + URL round-trip + clear, until narrows, range empty-state). |
| 5.4.40 | Size-range filter on image list — extends `GET /api/v1/images` with `?min_size=<bytes>` and `?max_size=<bytes>`, inclusive bounds on the image's `size_bytes`, mirrored through `vmsmith image list --min-size --max-size` and a pair of "Min bytes / Max bytes" inputs on the Images page that round-trip through `?min_size=` / `?max_size=`. Closes the capacity-management operator queries *"show me every image larger than 10 GiB"* / *"which images would fit on this 2 GiB partition"* that `?search=` cannot answer (sizes aren't in the haystack) and `?tag=` / `?source_vm=` cannot answer at all. Whitespace-trimmed; empty disables the bound; non-numeric or negative values return 400 `invalid_min_size` / `invalid_max_size`. Unlike the time-range filters there is no zero-value exclusion — a zero-byte size is a legitimate, fully-known value that matches whenever it falls within the window. Applied between the `source_vm` and time-range filters so it composes additively with `?tag=` / `?source_vm=` / `?since=` / `?until=` / `?search=` / sort / pagination and `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_image.go` `ListImages` parses both bounds up-front via the new `parseSizeRangeParam` helper (`internal/api/parse_size_range.go`; 400 `invalid_min_size` / `invalid_max_size` on garbage) and applies the `imageInSizeRange` predicate between the `source_vm` and time-range filters so `X-Total-Count` stays post-filter correct. `internal/cli/image.go` adds `--min-size` / `--max-size` with client-side validation via the mirrored `parseCLISizeRange` helper (`internal/cli/parse_size_range.go`). `web/src/pages/ImageList.jsx` adds debounced "Min bytes / Max bytes" number inputs with a "Clear sizes" button, URL round-trip via `?min_size=` / `?max_size=`, page reset on change, and a dedicated empty-state. `web/src/api/client.ts` extends `images.list({ minSize, maxSize })`. `tests/web/mock-server.js` mirrors the byte-range parse (400 path + filter). `docs/openapi.yaml` documents the two params on `GET /images` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference extended. Coverage: 9 API integration cases (`TestListImages_FilterByMinSize`, `_FilterByMaxSize`, `_FilterByMinAndMaxSize`, `_FilterByMinSize_Inclusive`, `_FilterByInvalidMinSize`, `_FilterByInvalidMaxSize`, `_FilterByMinSize_EmptyIsNoOp`, `_FilterByMinSize_RejectsNegative`, `_FilterBySize_ComposesWithTagAndSearch`); 5 CLI cases (`TestCLI_ImageList_FilterByMinSize`, `_FilterByMaxSize`, `_FilterBySizeRange`, `_RejectsInvalidMinSize`, `_RejectsNegativeMaxSize`); 2 Playwright cases (size range narrows + URL round-trip + clear; size range empty-state). |
| 5.4.41 | Severity-floor filter on the event list + SSE stream — extends `GET /api/v1/events` and `GET /api/v1/events/stream` with `?min_severity=<info\|warn\|error>`, returning events ranked at-or-above the value (info < warn < error), mirrored through the CLI (`vmsmith events list --min-severity`, `events follow --min-severity`) and a "Min severity" dropdown on the Activity page that round-trips through `?min_severity=`. Closes the *"show me everything that needs attention (warn + error)"* operator query that the exact-match `?severity=` filter (which returns one level only) cannot answer and that `?search=` covers fuzzily at best. Whitespace-trimmed and case-insensitive; empty disables the floor; unknown values return 400 `invalid_min_severity`. Composes additively with the exact-match `?severity=` and every other event filter so `X-Total-Count` reflects the post-filter population. Mirrors the severity-floor semantics the logs `level` filter already ships, bringing parity to the events surface. | S | ✅ Done — `pkg/types/event_severity.go` adds the shared `EventSeverityRank` (info=0/warn=1/error=2, case-insensitive, `(rank, ok)` for validation) + `EventMeetsMinSeverity` floor predicate. `internal/store/bolt.go` adds `EventFilter.MinSeverity` + a single-line predicate in the cursor walk (right after the exact-match `Severity`). `internal/api/handlers_events.go` validates `?min_severity=` once (400 `invalid_min_severity`) and wires it into both `ListEvents` (`store.EventFilter`) and `StreamEvents` (`types.EventStreamFilter`, validated before the SSE 200 is committed). `pkg/types/event_filter.go` adds `EventStreamFilter.MinSeverity` to `HasAny()` + `EventMatchesStreamFilter`. `internal/cli/events.go` adds `--min-severity` to both `events list` and `events follow` (validated via `validateMinSeverityFlag`), threads it through the shared `eventFilter` predicate (`filterEvents` + `matchesEventFilter`), and forwards it as a query param on the SSE connect. `web/src/pages/Activity.jsx` adds a "Min severity" `<select>` with URL round-trip via `?min_severity=`, page-reset on change, and clear-all wiring; `web/src/api/client.ts` extends `events.list({ minSeverity })`. `tests/web/mock-server.js` mirrors the floor + 400 path; `docs/openapi.yaml` documents the new param (regenerated `schema.d.ts`); CLAUDE.md REST quick reference for `GET /events` + `/events/stream` extended. Coverage: unit (`pkg/types/event_severity_test.go` — rank table + floor matrix + nil-event; `pkg/types/event_filter_test.go` — `_MinSeverityFloor` + `HasAny`); store (`TestListEventsFiltered_MinSeverity_Floor`, `_EmptyDisablesFilter`, `_ComposesWithSource`); API (`TestListEvents_FilterByMinSeverity_Floor`, `_CaseInsensitiveAndTrimmed`, `_EmptyIsNoOp`, `_InvalidReturns400`, `_ComposesWithSource`, `_TotalCountReflectsFiltered`); SSE (`TestStreamEvents_FilterReplay_MinSeverity`, `_InvalidMinSeverityReturns400`); CLI (`TestCLI_EventsList_FilterByMinSeverity_Floor`, `_CaseInsensitive`, `_Invalid`, `_ComposesWithSource`, `TestCLI_EventsFollow_FiltersByMinSeverity`); Playwright (`min-severity floor narrows results and round-trips through the URL`). |
| 5.4.42 | Tag-selector membership filter on the schedule list — extends `GET /api/v1/schedules` with `?tag_selector=<tag>`, a case-insensitive exact-membership filter that matches a schedule when any entry in its `tag_selector` list equals the value, mirrored through the CLI (`vmsmith schedule list --tag-selector`) and a "Tag selector" input on the Schedules page that round-trips through `?tag_selector=`. Closes the *"which schedules act on VMs tagged `prod`"* operator query: the schedule list already supports `?vm_id=` (exact, for single-VM-targeted schedules) but had no symmetric way to scope the *tag-selector-targeted* schedules — `?search=` covers it only fuzzily (substring `pro` would match `prod`/`production`). Whitespace-trimmed and case-insensitive; empty disables the filter; schedules with an empty `tag_selector` (vm_id-targeted or all-VMs) are NOT matched, mirroring the webhook `?event_type=` membership semantics. Composes additively with every other schedule filter so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_schedule.go` parses `?tag_selector=` (trim + lowercase) and adds a single membership predicate (`scheduleHasTagSelector`) in the `ListSchedules` filter loop right after the exact-match `vm_id`; schedules with an empty `tag_selector` never match. `internal/cli/schedule.go` adds `--tag-selector` to `schedule list` (trim + lowercase, forwarded as the `tag_selector` query param). `web/src/pages/Schedules.jsx` adds a debounced "Tag selector" input with URL round-trip via `?tag_selector=`, page-reset on change, and clear wiring; `web/src/api/client.ts` extends `schedules.list({ tagSelector })`. `tests/web/mock-server.js` mirrors the exact-membership filter; `docs/openapi.yaml` documents the new param (regenerated `schema.d.ts`); CLAUDE.md REST quick reference + CLI summary for `GET /schedules` extended. Coverage: API (`TestListSchedules_FilterByTagSelector` — exact membership, case-insensitive + trim, substring-is-not-a-match, empty no-op, vm_id-targeted schedule excluded, composes with action + post-filter `X-Total-Count`); CLI (`TestCLI_ScheduleList_ForwardsTagSelector` — trim + lowercase forwarding); Playwright (`tag-selector filter narrows the list and round-trips to the URL`). |
| 5.4.43 | Filtering on the schedule run-history endpoint — extends `GET /api/v1/schedules/{id}/runs` with `?status=<running\|success\|error\|skipped>`, `?since=<rfc3339>`, and `?until=<rfc3339>`, mirrored through a new `vmsmith schedule runs <id> --status --since --until --limit --page` CLI command and a per-row "Filter runs by status" dropdown on the Schedules page recent-runs expander. Closes the *"show me every failed/skipped fire for this schedule"* operator query that the run-history endpoint — which previously shipped only pagination — could not answer at all. `status` is case-insensitive exact-match (empty disables; unknown → 400 `invalid_status`); `since`/`until` are inclusive RFC3339 bounds on each run's `started_at` (whitespace-trimmed; invalid → 400 `invalid_since` / `invalid_until`; a run with a zero `started_at` is filtered OUT when any bound is set, mirroring the schedule/snapshot/image/template/webhook time-range family). Filters are applied before pagination so `X-Total-Count` reflects the post-filter population and compose additively. | S | ✅ Done — `pkg/types/schedule_run.go` adds the shared `IsValidScheduleRunStatus` validator. `internal/api/handlers_schedule.go` `ListScheduleRuns` parses + validates the three params up-front (reusing the existing `parseTimeRangeParam` + `snapshotInTimeRange` helpers) and filters the store's `ListRuns` result before pagination. `internal/cli/schedule.go` adds the new `schedule runs` subcommand (client-side `--status` validation + `parseCLITimeRange`, forwarded as query params). `web/src/api/client.ts` extends `schedules.runs({ status, since, until })`; `web/src/pages/Schedules.jsx` adds the status `<select>` to the `ScheduleRow` runs expander wired into the lazy `useFetch`. `tests/web/mock-server.js` mirrors the status/time-range filter + 400 paths and seeds an `error` run (`run-3`) so the filter is exercised. `docs/openapi.yaml` documents the three params (regenerated `schema.d.ts`); CLAUDE.md REST quick reference + CLI map extended. Coverage: unit (`pkg/types/schedule_run_test.go` — `IsValidScheduleRunStatus` table); API (`TestListScheduleRuns_Filters` — status exact/case-insensitive/trim, 400 `invalid_status`, since/until inclusive bounds, 400 invalid since/until, status+since compose, empty-array no-match, post-filter `X-Total-Count` under pagination, 404 unknown schedule); CLI (`TestCLI_ScheduleRuns_ForwardsFilters`, `_RejectsInvalidStatus`, `_RejectsInvalidSince`, `_Empty`); Playwright (`runs status filter narrows the recent-runs expander`). |
| 5.4.44 | vCPU-count range filter on the VM list — extends `GET /api/v1/vms` with `?min_cpus=<n>` and `?max_cpus=<n>`, inclusive bounds on the VM's `spec.cpus`, mirrored through `vmsmith vm list --min-cpus --max-cpus` and a pair of "Min vCPUs / Max vCPUs" number inputs on the VMList page that round-trip through `?min_cpus=` / `?max_cpus=`. Closes the capacity-audit operator queries *"show me every VM with ≥ 8 vCPUs"* / *"which VMs fit a 2-vCPU budget"* that `?search=` cannot answer (resource counts aren't in the haystack) and `?tag=` / `?image=` cannot answer at all. Whitespace-trimmed; empty disables the bound; non-numeric or negative values return 400 `invalid_min_cpus` / `invalid_max_cpus`. Like the image size-range filter there is no zero-value exclusion — a fully-known count matches whenever it falls within the window. Applied right after the `status` filter so it composes additively with `?tag=` / `?status=` / `?image=` / `?default_user=` / `?network=` / `?auto_start=` / `?locked=` / `?since=` / `?until=` / `?search=` / sort / pagination and `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_vm.go` `ListVMs` parses both bounds up-front via the new `parseCountRangeParam` helper (`internal/api/parse_count_range.go`; 400 `invalid_min_cpus` / `invalid_max_cpus` on garbage) and applies the `countInRange` predicate inside the filter loop right after the `status` check so `X-Total-Count` stays post-filter correct. `internal/cli/vm.go` adds `--min-cpus` / `--max-cpus` with client-side validation via the mirrored `parseCLICountRange` helper (`internal/cli/parse_count_range.go`) + `countInCLIRange` predicate. `web/src/pages/VMList.jsx` adds debounced "Min vCPUs / Max vCPUs" number inputs with a "Clear vCPUs" button, URL round-trip via `?min_cpus=` / `?max_cpus=`, and page reset on change. `web/src/api/client.ts` extends `vms.list({ minCpus, maxCpus })`. `tests/web/mock-server.js` mirrors the count-range parse (400 path + filter). `docs/openapi.yaml` documents the two params on `GET /vms` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference extended. Coverage: 8 API integration cases (`TestListVMs_FilterByMinCpus`, `_FilterByMaxCpus`, `_FilterByCpuRange`, `_FilterByMinCpus_Inclusive`, `_FilterByMinCpus_EmptyIsNoOp`, `_FilterByInvalidMinCpus`, `_FilterByMaxCpus_RejectsNegative`, `_FilterByCpus_ComposesWithStatusAndCount`); 5 CLI cases (`TestCLI_VMList_FilterByMinCpus`, `_FilterByMaxCpus`, `_FilterByCpuRange`, `_RejectsInvalidMinCpus`, `_RejectsNegativeMaxCpus`); 1 Playwright case (vCPU range narrows + URL round-trip + clear). |
| 5.4.45 | Per-network filter on the template list — extends `GET /api/v1/templates` with `?network=<name>`, a case-insensitive exact-match (any-of) against the name of any of the template's additional network attachments (`networks[].name`), mirrored through `vmsmith template list --network` and a debounced "Filter by network" input on the TemplateList page that round-trips through `?network=` in the URL. Closes the operator query *"show me every template that attaches `data-net`"* — templates carry network attachments that VMs already filter by (5.4.36), but the template list could only slice by `tag` / `image` / `default_user` / `search` / time-range. Mirrors the VM `?network=` filter: a template matches when any attachment name equals the value; the implicit primary NAT network is NOT represented in the template's networks list, so this only scopes to explicitly-attached extra networks operators name and group by (`data-net`, `storage-net`). Whitespace-trimmed; empty disables; applied after `?default_user=` and before `?since=`/`?until=` so it composes additively with `?tag=` / `?image=` / `?default_user=` / `?since=` / `?until=` / `?search=` / sort / pagination and `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `pkg/types/template_network_match.go` adds the shared `TemplateMatchesNetwork` predicate (mirrors `VMMatchesNetwork`; nil-safe, empty-query short-circuit, case-insensitive any-of over `networks[].name`). `internal/api/handlers_template.go` `ListTemplates` reads + trims + lowercases `?network=` and applies the predicate between the `?default_user=` and `?since=`/`?until=` filters so the post-filter `X-Total-Count` stays correct. `internal/cli/template.go` adds the `--network` flag to `template list`, applying the identical in-process predicate. `web/src/api/client.ts` extends `templates.list({ network })`; `web/src/pages/TemplateList.jsx` adds a 250 ms-debounced "Filter by network" input (with clear button) next to the default-user filter, URL round-trip via `?network=`, page-reset on change, and a tailored empty-state. `tests/web/mock-server.js` mirrors the filter and seeds `data-net` on the big-rocky template. `docs/openapi.yaml` documents the new `network` query parameter under `/templates` (regenerated `schema.d.ts`); `CLAUDE.md` REST quick reference extended. Coverage: unit (`pkg/types/template_network_match_test.go` — nil/empty/exact/case-insensitive/no-match/no-networks/partial); API integration (`TestListTemplates_FilterByNetwork_ExactMatch`, `_IsCaseInsensitive`, `_TrimsWhitespace`, `_AnyOf`, `_EmptyIsNoOp`, `_NoAttachmentsNeverMatch`, `_PartialIsNotAMatch`, `_ComposesWithTag`, `_TotalCountReflectsFiltered`); CLI (`TestCLI_TemplateList_FilterByNetwork_ExactMatch`, `_CaseInsensitive`, `_EmptyOmitsFilter`, `_NoMatch`); Playwright (`network filter narrows the template list and round-trips through the URL`, `network filter is case-insensitive`, `network filter matches no templates when query has no hits`). |
| 5.4.46 | Catch-up-policy filter on the schedule list — extends `GET /api/v1/schedules` with `?catch_up_policy=<skip\|run_once\|run_all>`, a case-insensitive exact-match on the schedule's catch-up policy, mirrored through `vmsmith schedule list --catch-up` and a "Catch-up" dropdown on the Schedules page that round-trips through `?catch_up_policy=`. Closes the post-downtime operator query *"show me every schedule that will replay missed fires (`run_once` / `run_all`) vs the ones that skip"* — the schedule list could already slice by `action` and `enabled` but had no symmetric way to scope by catch-up behaviour, even though it's an equally-important categorical field that determines blast radius after a daemon restart. Whitespace-trimmed; empty disables the filter; invalid values return 400 `invalid_catch_up_policy`. A schedule persisted with an empty policy is treated as `skip` (the engine's default) so `?catch_up_policy=skip` matches it, mirroring the VM `?default_user=root` empty-means-root semantics. Applied right after the `action` filter so it composes additively with `?vm_id=` / `?tag_selector=` / `?action=` / `?enabled=` / `?since=` / `?until=` / `?search=` / sort / pagination — `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_schedule.go` `ListSchedules` validates `?catch_up_policy=` once (400 `invalid_catch_up_policy` via the existing `types.IsValidCatchUpPolicy`) and applies the new `scheduleCatchUpPolicy` predicate (empty-stored → `skip`) in the filter loop right after `action`. `internal/cli/schedule.go` adds `schedule list --catch-up` with client-side validation, trimmed + lowercased and forwarded as the `catch_up_policy` query param. `web/src/pages/Schedules.jsx` adds a "Catch-up" `<select>` with URL round-trip via `?catch_up_policy=`, page-reset on change, and tailored empty-state; `web/src/api/client.ts` extends `schedules.list({ catchUpPolicy })`. `tests/web/mock-server.js` mirrors the filter + 400 path. `docs/openapi.yaml` documents the new param (regenerated `schema.d.ts`); CLAUDE.md REST quick reference + CLI map extended. Coverage: API (`TestListSchedules_FilterByCatchUpPolicy` — exact skip/run_once/run_all, empty-stored-treated-as-skip, case-insensitive + trim, empty no-op, 400 invalid, composes with action + post-filter `X-Total-Count`); CLI (`TestCLI_ScheduleList_ForwardsCatchUpPolicy`, `_RejectsInvalidCatchUp`); Playwright (`catch-up filter narrows the list and round-trips to the URL`). |
| 5.4.47 | Host-port range filter on the port-forward list — extends `GET /api/v1/vms/{id}/ports` with `?min_host_port=<n>` and `?max_host_port=<n>`, inclusive bounds on the rule's `host_port`, mirrored through `vmsmith port list --min-host-port --max-host-port` and a pair of "Min host port / Max host port" inputs on the VM detail port-forward panel that round-trip through `?port_min_host=` / `?port_max_host=`. Closes the port-allocation audit query *"show me every forward listening between 8000 and 8999"* that `?search=` can only answer fuzzily (a substring match on `80` also hits `8080`, `800`, …) and `?tag=` / `?protocol=` cannot answer at all. Mirrors the image size-range (5.4.40) numeric-range filter: whitespace-trimmed; empty disables the bound; non-numeric or negative values return 400 `invalid_min_host_port` / `invalid_max_host_port`; no zero-value exclusion — a fully-known port matches whenever it falls in the window. Applied after `protocol` and before `search` so it composes additively with `?tag=` / `?protocol=` / `?search=` / sort / pagination and `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/parse_port_range.go` adds `parsePortRangeParam` (400 `invalid_<name>` on garbage) + the `portInRange` predicate; `internal/api/handlers_network.go` `ListPorts` parses both bounds up-front and applies the predicate after the protocol filter. `internal/cli/parse_port_range.go` mirrors the helpers and `internal/cli/network.go` adds `port list --min-host-port --max-host-port` with client-side validation. `web/src/api/client.ts` extends `ports.list({ minHostPort, maxHostPort })`; `web/src/pages/VMDetail.jsx` adds debounced "Min/Max host port" number inputs with a "Clear ports" button, URL round-trip via `?port_min_host=` / `?port_max_host=`, and page reset on change. `tests/web/mock-server.js` mirrors the port-range parse (400 path + filter). `docs/openapi.yaml` documents both params on `GET /vms/{vmID}/ports` (regenerated `schema.d.ts`); CLAUDE.md REST quick reference extended. Coverage: 9 API integration cases (`TestListPorts_FilterByMinHostPort`, `_FilterByMaxHostPort`, `_FilterByHostPortRange`, `_FilterByMinHostPort_Inclusive`, `_FilterByMinHostPort_EmptyIsNoOp`, `_FilterByInvalidMinHostPort`, `_FilterByMaxHostPort_RejectsNegative`, `_FilterByHostPort_ComposesWithProtocol`, `_FilterByHostPort_TotalCountReflectsFiltered`); 5 CLI cases (`TestCLI_PortList_FilterByMinHostPort`, `_FilterByMaxHostPort`, `_FilterByHostPortRange`, `_RejectsInvalidMinHostPort`, `_RejectsNegativeMaxHostPort`); 1 Playwright case (`host-port range filter narrows the list and round-trips through the URL`). |
| 5.4.48 | RAM-size range filter on the VM list — extends `GET /api/v1/vms` with `?min_ram_mb=<n>` and `?max_ram_mb=<n>`, inclusive bounds on the VM's `spec.ram_mb` (RAM in MB), mirrored through `vmsmith vm list --min-ram-mb --max-ram-mb` and a pair of "Min RAM (MB) / Max RAM (MB)" number inputs on the VMList page that round-trip through `?min_ram_mb=` / `?max_ram_mb=`. Closes the capacity-audit operator queries *"show me every VM with ≥ 8 GB RAM"* / *"which VMs fit a 2 GB memory budget"* that `?search=` cannot answer (resource sizes aren't in the haystack) and `?tag=` / `?image=` cannot answer at all. The symmetric counterpart to the vCPU-count range filter (5.4.44) on the same list, reusing its `parseCountRangeParam` / `countInRange` helpers. Whitespace-trimmed; empty disables the bound; non-numeric or negative values return 400 `invalid_min_ram_mb` / `invalid_max_ram_mb`. Like the vCPU range (5.4.44) and image size-range (5.4.40) filters there is no zero-value exclusion — a fully-known size matches whenever it falls within the window. Applied right after the vCPU-range predicate so it composes additively with `?tag=` / `?status=` / `?image=` / `?default_user=` / `?network=` / `?auto_start=` / `?locked=` / `?since=` / `?until=` / `?min_cpus=` / `?max_cpus=` / `?search=` / sort / pagination and `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_vm.go` `ListVMs` parses both bounds up-front via the existing `parseCountRangeParam` helper (400 `invalid_min_ram_mb` / `invalid_max_ram_mb` on garbage) and applies the `countInRange` predicate (against `vm.Spec.RAMMB`) inside the filter loop right after the vCPU-range check so `X-Total-Count` stays post-filter correct. `internal/cli/vm.go` adds `--min-ram-mb` / `--max-ram-mb` with client-side validation via the mirrored `parseCLICountRange` helper + `countInCLIRange` predicate. `web/src/pages/VMList.jsx` adds debounced "Min RAM (MB) / Max RAM (MB)" number inputs with a "Clear RAM" button, URL round-trip via `?min_ram_mb=` / `?max_ram_mb=`, and page reset on change. `web/src/api/client.ts` extends `vms.list({ minRamMb, maxRamMb })`. `tests/web/mock-server.js` mirrors the count-range parse (400 path + filter). `docs/openapi.yaml` documents the two params on `GET /vms` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference extended. Coverage: 8 API integration cases (`TestListVMs_FilterByMinRAM`, `_FilterByMaxRAM`, `_FilterByRAMRange`, `_FilterByMinRAM_Inclusive`, `_FilterByMinRAM_EmptyIsNoOp`, `_FilterByInvalidMinRAM`, `_FilterByMaxRAM_RejectsNegative`, `_FilterByRAM_ComposesWithCpusAndCount`); 5 CLI cases (`TestCLI_VMList_FilterByMinRAM`, `_FilterByMaxRAM`, `_FilterByRAMRange`, `_RejectsInvalidMinRAM`, `_RejectsNegativeMaxRAM`); 1 Playwright case (`RAM range filter narrows the VM list and round-trips through the URL`). |
| 5.4.49 | Guest-port range filter on the port-forward list — extends `GET /api/v1/vms/{id}/ports` with `?min_guest_port=<n>` and `?max_guest_port=<n>`, inclusive bounds on the rule's `guest_port`, mirrored through `vmsmith port list --min-guest-port --max-guest-port` and a pair of "Min guest port / Max guest port" inputs on the VM detail port-forward panel that round-trip through `?port_min_guest=` / `?port_max_guest=`. The symmetric counterpart to the host-port range (5.4.47): `host_port` answers the host-side allocation audit (*"which forward listens on host :8080"*) while `guest_port` answers the guest-side one (*"which forwards target the guest's :22"*) — a query `?search=` can only answer fuzzily (a substring match on `22` also hits `2222`, `8022`, …) and `?tag=` / `?protocol=` cannot answer at all. Reuses the host-port range's `parsePortRangeParam` / `portInRange` (API) and `parseCLIPortRange` / `portInCLIRange` (CLI) helpers verbatim: whitespace-trimmed; empty disables the bound; non-numeric or negative values return 400 `invalid_min_guest_port` / `invalid_max_guest_port`; no zero-value exclusion — a fully-known port matches whenever it falls in the window. Applied right after the host-port range and before `search` so it composes additively with `?tag=` / `?protocol=` / `?min_host_port=` / `?max_host_port=` / `?search=` / sort / pagination and `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_network.go` `ListPorts` parses both bounds up-front via the existing `parsePortRangeParam` helper (400 `invalid_min_guest_port` / `invalid_max_guest_port` on garbage) and applies the `portInRange(pf.GuestPort, …)` predicate right after the host-port range filter. `internal/cli/network.go` adds `port list --min-guest-port --max-guest-port` with client-side validation via the shared `parseCLIPortRange` + `portInCLIRange`. `web/src/api/client.ts` extends `ports.list({ minGuestPort, maxGuestPort })`; `web/src/pages/VMDetail.jsx` adds debounced "Min/Max guest port" number inputs with a "Clear guest ports" button, URL round-trip via `?port_min_guest=` / `?port_max_guest=`, and page reset on change. `tests/web/mock-server.js` mirrors the guest-port-range parse (400 path + filter). `docs/openapi.yaml` documents both params on `GET /vms/{vmID}/ports` (regenerated `schema.d.ts`); CLAUDE.md REST quick reference extended. Coverage: 10 API integration cases (`TestListPorts_FilterByMinGuestPort`, `_FilterByMaxGuestPort`, `_FilterByGuestPortRange`, `_FilterByMinGuestPort_Inclusive`, `_FilterByMinGuestPort_EmptyIsNoOp`, `_FilterByInvalidMinGuestPort`, `_FilterByMaxGuestPort_RejectsNegative`, `_FilterByGuestPort_ComposesWithProtocol`, `_FilterByGuestPort_ComposesWithHostPort`, `_FilterByGuestPort_TotalCountReflectsFiltered`); 5 CLI cases (`TestCLI_PortList_FilterByMinGuestPort`, `_FilterByMaxGuestPort`, `_FilterByGuestPortRange`, `_RejectsInvalidMinGuestPort`, `_RejectsNegativeMaxGuestPort`); 1 Playwright case (`guest-port range filter narrows the list and round-trips through the URL`). |
| 5.4.51 | vCPU-count range filter on the template list — extends `GET /api/v1/templates` with `?min_cpus=<n>` and `?max_cpus=<n>`, inclusive bounds on the template's `cpus` field, mirrored through `vmsmith template list --min-cpus --max-cpus` and a pair of "Min CPUs / Max CPUs" number inputs on the TemplateList page that round-trip through `?min_cpus=` / `?max_cpus=`. Opens the capacity-audit operator query *"show me every template that provisions ≥ 8 vCPU VMs"* against the template cohort — the symmetric counterpart to the VM `?min_cpus=` / `?max_cpus=` filter (5.4.44), and the first range filter on a template surface that previously only carried `?tag=` / `?search=` / `?image=` / `?default_user=` / `?network=` / `?since=` / `?until=`. Reuses the VM-list `parseCountRangeParam` / `countInRange` (API) and `parseCLICountRange` / `countInCLIRange` (CLI) helpers verbatim: whitespace-trimmed; empty disables the bound; non-numeric or negative values return 400 `invalid_min_cpus` / `invalid_max_cpus`; no zero-value exclusion — a fully-known vCPU count matches whenever it falls within the window. Applied immediately after the time-range filter and before `?search=` so it composes additively with `?tag=` / `?image=` / `?default_user=` / `?network=` / `?since=` / `?until=` / `?search=` / sort / pagination and `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_template.go` `ListTemplates` parses both bounds up-front via the shared `parseCountRangeParam` helper (400 `invalid_min_cpus` / `invalid_max_cpus` on garbage) and applies the `countInRange` predicate (against `tpl.CPUs`) right after the since/until time-range filter so `X-Total-Count` stays post-filter correct. `internal/cli/template.go` adds `--min-cpus` / `--max-cpus` with client-side validation via the mirrored `parseCLICountRange` helper + `countInCLIRange` predicate. `web/src/pages/TemplateList.jsx` adds debounced "Min CPUs / Max CPUs" number inputs with a "Clear CPUs" button, URL round-trip via `?min_cpus=` / `?max_cpus=`, and page reset on change. `web/src/api/client.ts` extends `templates.list({ minCpus, maxCpus })`. `tests/web/mock-server.js` mirrors the count-range parse (400 path + filter). `docs/openapi.yaml` documents both params on `GET /templates`. `CLAUDE.md` REST quick reference extended. Coverage: 8 API integration cases (`TestListTemplates_FilterByMinCpus`, `_FilterByMaxCpus`, `_FilterByCpusRange`, `_FilterByMinCpus_Inclusive`, `_FilterByMinCpus_EmptyIsNoOp`, `_FilterByInvalidMinCpus`, `_FilterByMaxCpus_RejectsNegative`, `_FilterByCpus_ComposesWithTagAndSearch`); 5 CLI cases (`TestCLI_TemplateList_FilterByMinCpus`, `_FilterByMaxCpus`, `_FilterByCpusRange`, `_RejectsInvalidMinCpus`, `_RejectsNegativeMaxCpus`); 1 Playwright case (`vCPU range filter narrows the template list and round-trips through the URL`). |
| 5.4.53 | Disk-size range filter on the template list — extends `GET /api/v1/templates` with `?min_disk_gb=<n>` and `?max_disk_gb=<n>`, inclusive bounds on the template's `disk_gb` field, mirrored through `vmsmith template list --min-disk-gb --max-disk-gb` and a pair of "Min disk (GB) / Max disk (GB)" number inputs on the TemplateList page that round-trip through `?min_disk_gb=` / `?max_disk_gb=`. Opens the capacity-audit operator query *"show me every template that provisions ≥ 100 GB disk VMs"* against the template cohort — the symmetric counterpart to the VM disk-range filter (5.4.50), reusing the same `parseCountRangeParam` / `countInRange` (API) and `parseCLICountRange` / `countInCLIRange` (CLI) helpers, and completing the cpus/ram/disk capacity-audit trio on the template list alongside the template vCPU range (5.4.51) and template RAM range (5.4.52). Whitespace-trimmed; empty disables the bound; non-numeric or negative values return 400 `invalid_min_disk_gb` / `invalid_max_disk_gb`. Like the VM disk range (5.4.50) and the template CPU range (5.4.51) there is no zero-value exclusion — a fully-known disk size matches whenever it falls within the window. Applied immediately after the CPU/RAM range filters and before `?search=` so it composes additively with `?tag=` / `?image=` / `?default_user=` / `?network=` / `?since=` / `?until=` / `?min_cpus=` / `?max_cpus=` / `?min_ram_mb=` / `?max_ram_mb=` / `?search=` / sort / pagination and `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_template.go` `ListTemplates` parses both bounds up-front via the shared `parseCountRangeParam` helper (400 `invalid_min_disk_gb` / `invalid_max_disk_gb` on garbage) and applies the `countInRange` predicate (against `tpl.DiskGB`) right after the CPU-range filter so `X-Total-Count` stays post-filter correct. `internal/cli/template.go` adds `--min-disk-gb` / `--max-disk-gb` with client-side validation via the mirrored `parseCLICountRange` helper + `countInCLIRange` predicate. `web/src/pages/TemplateList.jsx` adds debounced "Min disk (GB) / Max disk (GB)" number inputs with a "Clear disk" button, URL round-trip via `?min_disk_gb=` / `?max_disk_gb=`, and page reset on change. `web/src/api/client.ts` extends `templates.list({ minDiskGb, maxDiskGb })`. `tests/web/mock-server.js` mirrors the count-range parse (400 path + filter). `docs/openapi.yaml` documents both params on `GET /templates` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference extended. Coverage: 8 API integration cases (`TestListTemplates_FilterByMinDisk`, `_FilterByMaxDisk`, `_FilterByDiskRange`, `_FilterByMinDisk_Inclusive`, `_FilterByMinDisk_EmptyIsNoOp`, `_FilterByInvalidMinDisk`, `_FilterByMaxDisk_RejectsNegative`, `_FilterByDisk_ComposesWithCpusAndSearch`); 5 CLI cases (`TestCLI_TemplateList_FilterByMinDisk`, `_FilterByMaxDisk`, `_FilterByDiskRange`, `_RejectsInvalidMinDisk`, `_RejectsNegativeMaxDisk`); 1 Playwright case (`disk range filter narrows the template list and round-trips through the URL`). |
| 5.4.50 | Disk-size range filter on the VM list — extends `GET /api/v1/vms` with `?min_disk_gb=<n>` and `?max_disk_gb=<n>`, inclusive bounds on the VM's `spec.disk_gb` (disk in GB), mirrored through `vmsmith vm list --min-disk-gb --max-disk-gb` and a pair of "Min disk (GB) / Max disk (GB)" number inputs on the VMList page that round-trip through `?min_disk_gb=` / `?max_disk_gb=`. Closes the capacity-audit operator queries *"show me every VM with ≥ 100 GB disk"* / *"which VMs fit a 20 GB volume budget"* that `?search=` cannot answer (resource sizes aren't in the haystack) and `?tag=` / `?image=` cannot answer at all. The symmetric counterpart to the vCPU-count range filter (5.4.44) and RAM-size range filter (5.4.48) on the same list, reusing their `parseCountRangeParam` / `countInRange` helpers — and completing the cpus/ram/disk capacity-audit trio on the VM list. Whitespace-trimmed; empty disables the bound; non-numeric or negative values return 400 `invalid_min_disk_gb` / `invalid_max_disk_gb`. Like the vCPU range (5.4.44), RAM range (5.4.48), and image size-range (5.4.40) filters there is no zero-value exclusion — a fully-known size matches whenever it falls within the window. Applied right after the RAM-range predicate so it composes additively with `?tag=` / `?status=` / `?image=` / `?default_user=` / `?network=` / `?auto_start=` / `?locked=` / `?since=` / `?until=` / `?min_cpus=` / `?max_cpus=` / `?min_ram_mb=` / `?max_ram_mb=` / `?search=` / sort / pagination and `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_vm.go` `ListVMs` parses both bounds up-front via the existing `parseCountRangeParam` helper (400 `invalid_min_disk_gb` / `invalid_max_disk_gb` on garbage) and applies the `countInRange` predicate (against `vm.Spec.DiskGB`) inside the filter loop right after the RAM-range check so `X-Total-Count` stays post-filter correct. `internal/cli/vm.go` adds `--min-disk-gb` / `--max-disk-gb` with client-side validation via the mirrored `parseCLICountRange` helper + `countInCLIRange` predicate. `web/src/pages/VMList.jsx` adds debounced "Min disk (GB) / Max disk (GB)" number inputs with a "Clear disk" button, URL round-trip via `?min_disk_gb=` / `?max_disk_gb=`, and page reset on change. `web/src/api/client.ts` extends `vms.list({ minDiskGb, maxDiskGb })`. `tests/web/mock-server.js` mirrors the count-range parse (400 path + filter). `docs/openapi.yaml` documents the two params on `GET /vms` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference extended. Coverage: 8 API integration cases (`TestListVMs_FilterByMinDisk`, `_FilterByMaxDisk`, `_FilterByDiskRange`, `_FilterByMinDisk_Inclusive`, `_FilterByMinDisk_EmptyIsNoOp`, `_FilterByInvalidMinDisk`, `_FilterByMaxDisk_RejectsNegative`, `_FilterByDisk_ComposesWithRAMAndCount`); 5 CLI cases (`TestCLI_VMList_FilterByMinDisk`, `_FilterByMaxDisk`, `_FilterByDiskRange`, `_RejectsInvalidMinDisk`, `_RejectsNegativeMaxDisk`); 1 Playwright case (`disk range filter narrows the VM list and round-trips through the URL`). |
| 5.4.56 | Capacity sort axes on the VM list — extends `GET /api/v1/vms` `?sort=` whitelist with three numeric capacity fields (`cpus`, `ram_mb`, `disk_gb`), in addition to the existing `id` / `name` / `created_at` / `state` axes, mirrored through `vmsmith vm list --sort cpus|ram_mb|disk_gb` and three new options ("vCPUs", "RAM (MB)", "Disk (GB)") in the VMList sort dropdown. Closes the capacity-audit operator queries *"show me the largest VMs by vCPU"* / *"order all VMs by RAM descending"* / *"which VM has the biggest disk"* that the existing `?min_cpus=` / `?max_cpus=` / `?min_ram_mb=` / `?max_ram_mb=` / `?min_disk_gb=` / `?max_disk_gb=` range filters (5.4.44, 5.4.48, 5.4.50) can narrow but not order — completing the symmetry between capacity filters and capacity sorts on the VM list. Numeric ordering on `vm.Spec.CPUs` / `RAMMB` / `DiskGB` (asc = smallest first, desc = largest first); all comparators tiebreak on `id` so paginated requests are deterministic. Unknown sort values continue to return 400 `invalid_sort` (and the rejection-test sentinel value moves from `ram_mb` to `memory` since the former is now valid). | S | ✅ Done — `pkg/types/vm_sort.go` adds `VMSortCPUs` / `VMSortRAMMB` / `VMSortDiskGB` constants and three numeric `sort.SliceStable` cases that compare `Spec.CPUs` / `Spec.RAMMB` / `Spec.DiskGB` with an `ID` tiebreak. `internal/api/vm_sort.go` extends the whitelist + 400 error message; `internal/cli/vm.go` does the same on the client side and updates the `--sort` flag description. `web/src/pages/VMList.jsx` extends the sort-field `<select>` with three new options ("vCPUs" / "RAM (MB)" / "Disk (GB)") whose values map to the new API tokens. `tests/web/mock-server.js` extends both the sort-field whitelist (so the 400 path stays correct) and the `cmp` switch (numeric subtraction with the same `id` tiebreak). `docs/openapi.yaml` documents the extended `VMSort` enum (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference updated. Coverage: 4 unit cases in `pkg/types/vm_sort_test.go` (`TestSortVMs_ByCPUs_AscTiebreaksOnID`, `_ByCPUs_Desc`, `_ByRAMMB_AscTiebreaksOnID`, `_ByDiskGB_DescTiebreaksOnID`); 4 API integration cases in `internal/api/api_test.go` (`TestListVMs_SortByCPUs`, `_SortByCPUsDesc_TiebreaksOnID`, `_SortByRAMMB`, `_SortByDiskGB`); 3 CLI cases in `internal/cli/commands_test.go` (`TestCLI_VMList_SortByCPUs`, `_SortByRAMMBDesc`, `_SortByDiskGB`) plus the `_RejectsInvalidSort` sentinel update; 1 Playwright case (`capacity sort axes (cpus / ram_mb / disk_gb) reorder the VM list and round-trip through the URL`). |
| 5.4.54 | VM-id filter on the schedule run history — extends `GET /api/v1/schedules/{id}/runs` with `?vm_id=<id>`, a case-sensitive exact-match against each run's `vm_id` field (VM IDs are opaque `vm-<unix-nano>` strings — case matters), mirrored through `vmsmith schedule runs <id> --vm <vm-id>` and a "Filter runs by VM id" text input on the Schedules page recent-runs expander (sits next to the existing status dropdown; 250ms debounce; empty disables). Closes the *"which runs of this tag-selector schedule targeted VM X"* operator query that the prior `?status=` / `?since=` / `?until=` filters cannot answer on their own — a schedule that targets `tag_selector: ["prod"]` may fire against ten VMs every night, and operators reviewing a failure want to slice the history to one VM at a time. Whitespace-trimmed; empty disables the filter (mirrors the events handler's `?vm_id=` contract). Applied right after the status check and before the time-range predicate so it composes additively with `?status=` / `?since=` / `?until=` / pagination and `X-Total-Count` reflects the post-filter / pre-pagination population. | S | ✅ Done — `internal/api/handlers_schedule.go` `ListScheduleRuns` parses `vm_id` via `strings.TrimSpace` and applies an exact-match predicate inside the filter loop right after the status check so `X-Total-Count` stays post-filter correct. `internal/cli/schedule.go` adds `--vm` to `schedule runs` with whitespace-trimmed forwarding. `web/src/pages/Schedules.jsx` adds a debounced "Filter runs by VM id" text input next to the existing status dropdown on the recent-runs expander (250ms debounce, lazy fetch via `useFetch` dependency on the trimmed debounced value). `web/src/api/client.ts` extends `schedules.runs({ vmId })`. `tests/web/mock-server.js` mirrors the exact-match filter and seeds a `vm-2` run on `sch-1` so the filter narrows meaningfully. `docs/openapi.yaml` documents the param on `GET /schedules/{scheduleID}/runs` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference extended. Coverage: 1 API integration case (`TestListScheduleRuns_FilterByVMID` — baseline / exact / whitespace-trim / case-sensitivity / empty-no-op / status compose / since compose / paginated total-count); 2 CLI cases (`TestCLI_ScheduleRuns_ForwardsVMFilter`, `TestCLI_ScheduleRuns_EmptyVMFlagSendsNothing`); 1 Playwright case (`runs vm_id filter narrows the recent-runs expander`). |
| 5.4.52 | RAM-size range filter on the template list — extends `GET /api/v1/templates` with `?min_ram_mb=<n>` and `?max_ram_mb=<n>`, inclusive bounds on the template's `ram_mb` field, mirrored through `vmsmith template list --min-ram-mb --max-ram-mb` and a pair of "Min RAM (MB) / Max RAM (MB)" number inputs on the TemplateList page that round-trip through `?min_ram_mb=` / `?max_ram_mb=`. Opens the capacity-audit operator query *"show me every template that provisions ≥ 8 GB RAM VMs"* against the template cohort — the symmetric counterpart to the VM `?min_ram_mb=` / `?max_ram_mb=` filter (5.4.48), and the second range filter on the template list after the vCPU range (5.4.51). Reuses the same `parseCountRangeParam` / `countInRange` (API) and `parseCLICountRange` / `countInCLIRange` (CLI) helpers verbatim: whitespace-trimmed; empty disables the bound; non-numeric or negative values return 400 `invalid_min_ram_mb` / `invalid_max_ram_mb`; no zero-value exclusion — a fully-known RAM value matches whenever it falls within the window. Applied immediately after the time-range filter and before `?search=` so it composes additively with `?tag=` / `?image=` / `?default_user=` / `?network=` / `?since=` / `?until=` / `?search=` / sort / pagination and `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_template.go` `ListTemplates` parses both bounds up-front via the shared `parseCountRangeParam` helper (400 `invalid_min_ram_mb` / `invalid_max_ram_mb` on garbage) and applies the `countInRange` predicate (against `tpl.RAMMB`) right after the since/until time-range filter so `X-Total-Count` stays post-filter correct. `internal/cli/template.go` adds `--min-ram-mb` / `--max-ram-mb` with client-side validation via the mirrored `parseCLICountRange` helper + `countInCLIRange` predicate. `web/src/pages/TemplateList.jsx` adds debounced "Min RAM (MB) / Max RAM (MB)" number inputs with a "Clear RAM" button, URL round-trip via `?min_ram_mb=` / `?max_ram_mb=`, and page reset on change. `web/src/api/client.ts` extends `templates.list({ minRamMb, maxRamMb })`. `tests/web/mock-server.js` mirrors the count-range parse (400 path + filter). `docs/openapi.yaml` documents both params on `GET /templates` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference extended. Coverage: 8 API integration cases (`TestListTemplates_FilterByMinRAM`, `_FilterByMaxRAM`, `_FilterByRAMRange`, `_FilterByMinRAM_Inclusive`, `_FilterByMinRAM_EmptyIsNoOp`, `_FilterByInvalidMinRAM`, `_FilterByMaxRAM_RejectsNegative`, `_FilterByRAM_ComposesWithTagAndSearch`); 5 CLI cases (`TestCLI_TemplateList_FilterByMinRAM`, `_FilterByMaxRAM`, `_FilterByRAMRange`, `_RejectsInvalidMinRAM`, `_RejectsNegativeMaxRAM`); 1 Playwright case (`RAM range filter narrows the template list and round-trips through the URL`). |
| 5.4.57 | Capacity sort axes on the template list — extends `GET /api/v1/templates` `?sort=` whitelist with three numeric capacity fields (`cpus`, `ram_mb`, `disk_gb`), in addition to the existing `id` / `name` / `created_at` axes, mirrored through `vmsmith template list --sort cpus|ram_mb|disk_gb` and three new options ("vCPUs", "RAM (MB)", "Disk (GB)") in the TemplateList sort dropdown. Closes the capacity-audit operator queries *"show me the largest templates by vCPU"* / *"order all templates by RAM descending"* / *"which template provisions the biggest disk"* against the template cohort that the existing `?min_cpus=` / `?max_cpus=` / `?min_ram_mb=` / `?max_ram_mb=` / `?min_disk_gb=` / `?max_disk_gb=` range filters (5.4.51, 5.4.52, 5.4.53) can narrow but not order — completing the symmetry between capacity filters and capacity sorts on the template list, mirroring the VM list (5.4.56). Numeric ordering on `VMTemplate.CPUs` / `RAMMB` / `DiskGB` (asc = smallest first, desc = largest first); all comparators tiebreak on `id` so paginated requests are deterministic. Unknown sort values continue to return 400 `invalid_sort` (and the rejection-test sentinel value moves from `ram_mb` to `memory` since the former is now valid). | S | ✅ Done — `pkg/types/template_sort.go` adds `TemplateSortCPUs` / `TemplateSortRAMMB` / `TemplateSortDiskGB` constants and three numeric `sort.SliceStable` cases that compare `CPUs` / `RAMMB` / `DiskGB` with an `ID` tiebreak. `internal/api/template_sort.go` extends the whitelist + 400 error message; `internal/cli/template.go` does the same on the client side and updates the `--sort` flag description. `web/src/pages/TemplateList.jsx` extends the sort-field `<select>` with three new options ("vCPUs" / "RAM (MB)" / "Disk (GB)") whose values map to the new API tokens. `tests/web/mock-server.js` extends both the sort-field whitelist (so the 400 path stays correct) and the `cmp` switch (numeric subtraction with the same `id` tiebreak). `docs/openapi.yaml` documents the extended `TemplateSort` enum (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference updated. Coverage: 4 unit cases in `pkg/types/template_sort_test.go` (`TestSortTemplates_ByCPUs_AscTiebreaksOnID`, `_ByCPUs_Desc`, `_ByRAMMB_AscTiebreaksOnID`, `_ByDiskGB_DescTiebreaksOnID`); 4 API integration cases in `internal/api/api_test.go` (`TestListTemplates_SortByCPUs`, `_SortByCPUsDesc_TiebreaksOnID`, `_SortByRAMMB`, `_SortByDiskGB`); 3 CLI cases in `internal/cli/commands_test.go` (`TestCLI_TemplateList_SortByCPUs`, `_SortByRAMMBDesc`, `_SortByDiskGB`) plus the `_RejectsInvalidSort` sentinel update; 1 Playwright case (`capacity sort axes (cpus / ram_mb / disk_gb) reorder templates and round-trip through the URL`). |
| 5.4.55 | Per-timezone filter on the schedule list — extends `GET /api/v1/schedules` with `?timezone=<IANA>`, a case-sensitive exact-match against the schedule's stored `timezone` field, mirrored through `vmsmith schedule list --timezone <tz>` and a debounced "Filter by timezone" text input on the Schedules page that round-trips through `?timezone=`. Closes the deployment-audit operator query *"show me every schedule that runs in `America/New_York`"* / *"which schedules were ever pinned to the legacy `Asia/Tokyo` zone"* — the schedule list already sliced by `?action=` / `?catch_up_policy=` / `?enabled=` (categorical) and `?vm_id=` / `?tag_selector=` (target) but had no symmetric way to scope by timezone, even though timezone determines fire-time semantics across DST transitions and across geographically-distributed operators. IANA timezone names are case-sensitive (`America/New_York` ≠ `america/new_york`), so the filter is case-sensitive — mirrors the `?vm_id=` / `?actor=` / `?resource_id=` exact-match contracts; case-insensitive matching is the job of `?search=`. Whitespace-trimmed; empty disables; no default-fallback for empty stored values since the engine's effective default is host-dependent (`time.Local`) — operators querying by timezone must supply the literal value they stored, so an empty stored timezone never matches a non-empty query. Applied right after `?catch_up_policy=` and before `?enabled=` so it composes additively with `?vm_id=` / `?tag_selector=` / `?action=` / `?catch_up_policy=` / `?enabled=` / `?since=` / `?until=` / `?search=` / sort / pagination — `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_schedule.go` `ListSchedules` reads + trims `?timezone=` and applies the exact-match predicate (`sched.Timezone != timezoneFilter`) right after the catch-up filter so `X-Total-Count` stays post-filter correct. `internal/cli/schedule.go` adds `schedule list --timezone <tz>` with whitespace-trimmed forwarding (no lowercasing — IANA case-sensitivity). `web/src/pages/Schedules.jsx` adds a 250 ms-debounced "Timezone" text input alongside the existing catch-up dropdown, URL round-trip via `?timezone=`, page-reset on change, and tailored empty-state. `web/src/api/client.ts` extends `schedules.list({ timezone })`. `tests/web/mock-server.js` mirrors the exact-match filter and updates the seed `sch-2` timezone to `America/New_York` so the filter narrows meaningfully (UTC × 2 vs America/New_York × 1). `docs/openapi.yaml` documents the new param on `GET /schedules` (regenerated `schema.d.ts`); `CLAUDE.md` REST quick reference + CLI map extended. Coverage: 1 API integration case (`TestListSchedules_FilterByTimezone` — baseline / exact / case-sensitive UTC + America/New_York / whitespace-trim / empty-no-op / no-match / compose with action / compose with catch_up_policy / paginated total-count); 2 CLI cases (`TestCLI_ScheduleList_ForwardsTimezone`, `TestCLI_ScheduleList_EmptyTimezoneOmitsParam`); 1 Playwright case (`timezone filter narrows the schedule list and round-trips through the URL`). |
| 5.4.59 | Sort axes on the schedule run history — extends `GET /api/v1/schedules/{id}/runs` with `?sort=<id|started_at|finished_at|status>` and `?order=<asc|desc>`, mirrored through `vmsmith schedule runs <id> --sort --order` and a pair of "Sort" / "Order" dropdowns on the Schedules page recent-runs expander. Closes the triage / audit operator queries *"show me the runs sorted by finish time"* / *"order all runs by status"* that the existing `?status=` / `?vm_id=` / `?since=` / `?until=` / `?search=` filters can narrow but not order — the schedule run history was the only list endpoint with NO sort customization (implicit newest-first via bolt key order). Default stays `started_at` desc when no params are supplied so the legacy newest-first contract is preserved; an explicit `?sort=` value defaults `?order=` to `asc`. The `finished_at` axis treats nil (still-running) `finished_at` as trailing in ascending order and leading in descending order, matching the existing `compareNextFire` nil-trailing semantics in `pkg/types/schedule.go`. The `status` axis matches alphabetically (`error` < `running` < `success`). All comparators tiebreak on `id` so paginated requests are deterministic. Unknown values return 400 `invalid_sort` / `invalid_order`. | S | ✅ Done — `pkg/types/schedule_run_sort.go` adds `ScheduleRunSortID` / `_StartedAt` / `_FinishedAt` / `_Status` constants, `IsValidScheduleRunSort`, and `SortScheduleRuns` (numeric `compareTime`, nil-aware `compareNextFire`, alphabetical `Status`, ID tiebreak). `internal/api/handlers_schedule.go::ListScheduleRuns` parses `?sort=` / `?order=` up-front (defaulting to `started_at` desc on bare GET; `asc` on explicit sort), validates via the whitelist, calls `types.SortScheduleRuns` between filtering and pagination so `X-Total-Count` stays post-filter correct. `internal/cli/schedule.go` adds `--sort` / `--order` flags on `schedule runs` with client-side validation against `types.IsValidScheduleRunSort` + `SortOrderAsc`/`Desc`. `web/src/api/client.ts` extends `schedulesApi.runs({ sort, order })`; `web/src/pages/Schedules.jsx` adds two `<select>` dropdowns (`schedule-runs-sort-<id>` and `schedule-runs-order-<id>`) next to the existing VM-id, search, and status filters on the recent-runs expander. `tests/web/mock-server.js` mirrors the sort whitelist + 400 contract + nil-trailing `finished_at` comparator + ID tiebreak. `docs/openapi.yaml` documents the new `sort` / `order` query parameters on `/schedules/{scheduleID}/runs` (and the generated `schema.d.ts` reflects them); `CLAUDE.md` REST quick reference + CLI map extended. Coverage: 7 unit cases in `pkg/types/schedule_run_sort_test.go` (`TestSortScheduleRuns_DefaultIDAsc`, `_ByStartedAt_AscTiebreaksOnID`, `_ByStartedAt_Desc`, `_ByFinishedAt_NilTrailingAsc`, `_ByFinishedAt_NilLeadingDesc`, `_ByStatus_TiebreaksOnID`, `_UnknownFieldFallsBackToID`, `TestIsValidScheduleRunSort`); 1 API integration case in `internal/api/handlers_schedule_test.go` (`TestListScheduleRuns_Sort` — default newest-first / explicit started_at,asc / finished_at,asc nil-trailing / finished_at,desc nil-leading / status alphabetical / id,asc / invalid_sort / invalid_order / whitespace-trim + case-insensitive / sort composes with status / pagination preserves post-filter total); 4 CLI cases (`TestCLI_ScheduleRuns_ForwardsSortAndOrder` — whitespace-trim + lowercase forwarding; `_EmptySortAndOrderOmitParams` — empty disables; `_RejectsInvalidSort`; `_RejectsInvalidOrder`). |
| 5.4.61 | Last-delivery time-range filter on the webhook list — extends `GET /api/v1/webhooks` with `?last_delivery_since=<rfc3339>` / `?last_delivery_until=<rfc3339>`, inclusive bounds on each webhook's `last_delivery_at`, mirrored through `vmsmith webhook list --last-delivery-since --last-delivery-until` and two `<input type="datetime-local">` controls on the Settings page (URL round-trip via `?last_delivery_since=` / `?last_delivery_until=`). Closes the delivery-audit operator queries *"which webhooks delivered events in the last hour"* / *"which receivers haven't fired since the maintenance window 02:00-06:00"* — the list already sliced by `?delivery_status=<never\|healthy\|failing>` (categorical health bucket) and `?since=` / `?until=` on `created_at` (when the webhook was *registered*) but had no symmetric way to scope by *when the receiver last fired*. Reuses the same `parseTimeRangeParam` helper as `?since=` / `?until=` and the same `snapshotInTimeRange` predicate so the contract matches exactly: whitespace-trimmed; empty disables; invalid values return 400 `invalid_last_delivery_since` / `invalid_last_delivery_until`. Never-delivered webhooks (zero `last_delivery_at`) are filtered OUT whenever either bound is set — mirrors the zero-time exclusion across every existing time-range family (snapshot/image/VM/template/webhook `created_at`; schedule `next_fire_at` in 5.4.60); use `?delivery_status=never` when the intent is to *find* never-delivered webhooks. Applied right after the existing `since` / `until` filter on `created_at` and before `?delivery_status=` so it composes additively with every other webhook filter; `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_webhook.go` `ListWebhooks` parses both bounds via the existing `parseTimeRangeParam` helper (400 `invalid_last_delivery_since` / `invalid_last_delivery_until` on garbage) and applies the `snapshotInTimeRange(h.LastDeliveryAt, …)` predicate inside the filter loop right after the `created_at` time-range check so `X-Total-Count` stays post-filter correct. `internal/cli/webhook.go` adds `--last-delivery-since` / `--last-delivery-until` to `webhook list` with client-side validation via the mirrored `parseCLITimeRange` helper. `web/src/api/client.ts` extends `webhooks.list({ lastDeliverySince, lastDeliveryUntil })`. `web/src/pages/Settings.jsx` adds two `<input type="datetime-local">` controls with a "Clear last delivery" button, URL round-trip via `?last_delivery_since=` / `?last_delivery_until=`, page reset on change, and a tailored empty-state that points operators at `?delivery_status=never` when the window matches no rows. `tests/web/mock-server.js` mirrors the time-range parse (400 path + filter + zero-time exclusion) and accepts `last_delivery_at` / `last_status` / `last_error` on POST /webhooks as a mock-only test affordance so the Playwright test can seed deterministic timestamps. `docs/openapi.yaml` documents both params on `GET /webhooks` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference + CLI map extended. Coverage: 11 API integration cases (`TestListWebhooks_FilterByLastDeliverySince`, `_FilterByLastDeliveryUntil`, `_FilterByLastDeliveryRange`, `_FilterByLastDeliverySince_Inclusive`, `_FilterByLastDeliveryUntil_Inclusive`, `_FilterByInvalidLastDeliverySince`, `_FilterByInvalidLastDeliveryUntil`, `_FilterByLastDeliverySince_EmptyIsNoOp`, `_FilterByLastDeliveryRange_ExcludesNeverDelivered`, `_FilterByLastDelivery_ComposesWithDeliveryStatus`, `_FilterByLastDelivery_PaginatedTotalCountReflectsFiltered`); 6 CLI cases (`TestCLI_WebhookList_ForwardsLastDeliverySince`, `_ForwardsLastDeliveryUntil`, `_ForwardsLastDeliveryRange`, `_RejectsInvalidLastDeliverySince`, `_RejectsInvalidLastDeliveryUntil`, `_EmptyLastDeliveryFlagsOmitParams`); 1 Playwright case (`last-delivery range filter narrows the webhook list and round-trips through the URL` — exercises since-only / both-bounds / wide-open with never-delivered exclusion / empty-state with nudge toward `delivery_status=never` / "Clear last delivery" button restores every row). |
| 5.4.58 | Free-text search filter on the schedule run history — extends `GET /api/v1/schedules/{id}/runs` with `?search=<q>`, a case-insensitive substring match across the run's `error` and `skip_reason` fields, mirrored through `vmsmith schedule runs <id> --search "..."` and a debounced "Search error / skip-reason" text input on the Schedules page recent-runs expander. Closes the triage operator queries *"show me every run of this nightly-snapshot schedule that timed out"* / *"which runs hit `vm_already_stopped` last week"* / *"find the runs where the libvirt connection was refused"* that the existing `?status=` (which only narrows to the four `running` / `success` / `error` / `skipped` enum buckets) and `?vm_id=` (which scopes by target VM but not by failure reason) cannot answer. The haystack intentionally excludes `id` / `schedule_id` / `vm_id` / `status` so short numeric needles don't generate noisy matches against opaque IDs — mirrors the events handler `?search=` exclusion contract for the same reason. Whitespace-trimmed; empty disables; composes additively with `?status=` / `?vm_id=` / `?since=` / `?until=` / pagination so `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_schedule.go::ListScheduleRuns` reads + trims + lowercases `?search=` and applies the substring predicate through the new `scheduleRunMatchesSearch` helper right after the time-range filter so the post-filter `X-Total-Count` stays correct. `internal/cli/schedule.go` adds the `--search` flag on `schedule runs` with whitespace-trimmed forwarding (no lowercasing — the API layer normalises) and an updated long-help description. `web/src/api/client.ts` extends `schedulesApi.runs({ search })`; `web/src/pages/Schedules.jsx` adds a 250 ms-debounced "Search error / skip-reason" input next to the existing VM-id and status filters on the recent-runs expander, with `data-testid="schedule-runs-search-filter-<id>"` so the Playwright assertions can target it deterministically. `tests/web/mock-server.js` mirrors the lowercased substring predicate against both `error` and `skip_reason` for the run-history endpoint. `docs/openapi.yaml` documents the new `search` query parameter on `/schedules/{scheduleID}/runs` (regenerated `schema.d.ts`); `CLAUDE.md` REST quick reference + CLI map extended. Coverage: 1 API integration case (`TestListScheduleRuns_FilterBySearch` — baseline / empty-no-op / error-substring / case-insensitive / whitespace-trim / skip-reason / `vm_id` excluded from haystack / `success` status string excluded from haystack / compose with status / compose with status+no-cross-bleed / paginated total-count); 2 CLI cases (`TestCLI_ScheduleRuns_ForwardsSearchFilter` covers whitespace-trim + actual query forwarding; `TestCLI_ScheduleRuns_EmptySearchFlagSendsNothing` covers the empty-disables contract); 1 Playwright case (`runs search filter narrows the recent-runs expander to matching error text` — exercises baseline / "libvirt" substring / "SNAPSHOT" case-insensitive / no-match empty-state / cleared-restores-full). |
| 5.4.60 | Next-fire time-range filter on the schedule list — extends `GET /api/v1/schedules` with `?next_fire_since=<rfc3339>` / `?next_fire_until=<rfc3339>`, inclusive bounds on each schedule's cron-computed `next_fire_at`, mirrored through `vmsmith schedule list --next-fire-since --next-fire-until` and two `<input type="datetime-local">` controls on the Schedules page that round-trip through `?next_fire_since=` / `?next_fire_until=`. Closes the deployment-audit operator query *"which schedules are about to fire in the next 4 hours?"* / *"which schedules will fire over the maintenance window 02:00–06:00?"* — the schedule list already sliced by `?vm_id=` / `?tag_selector=` / `?action=` / `?catch_up_policy=` / `?timezone=` / `?enabled=` / `?since=` (on `created_at`) and could **order** by `next_fire_at` via the existing sort axis, but had no symmetric way to **narrow** by next-fire window. Reuses the same `parseTimeRangeParam` helper as the `?since=` / `?until=` filter and the same `snapshotInTimeRange` predicate so the contract matches exactly: whitespace-trimmed; empty disables the bound; invalid values return 400 `invalid_next_fire_since` / `invalid_next_fire_until`. Schedules with a nil `next_fire_at` (disabled or otherwise stalled) are filtered OUT whenever any bound is set — mirrors the zero-`created_at` handling in the existing time-range filter and the snapshot / image / VM / template / webhook time-range families. Applied right after the existing `since` / `until` filter on `created_at` and before `?search=` so it composes additively with every other schedule filter; `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_schedule.go` `ListSchedules` parses both bounds up-front via `parseTimeRangeParam` (400 `invalid_next_fire_since` / `invalid_next_fire_until` on garbage) and applies the predicate right after the `created_at` time-range check so `X-Total-Count` stays post-filter correct. `internal/cli/schedule.go` adds `schedule list --next-fire-since` / `--next-fire-until` with client-side validation via the mirrored `parseCLITimeRange` helper. `web/src/pages/Schedules.jsx` adds two `datetime-local` "Next fire since / until" inputs and a "Clear next fire" button alongside the existing created-at range filter, URL round-trip via `?next_fire_since=` / `?next_fire_until=`, and page reset on change. `web/src/api/client.ts` extends `schedules.list({ nextFireSince, nextFireUntil })`. `tests/web/mock-server.js` mirrors the time-range parse (400 path + filter + nil-NextFireAt exclusion). `docs/openapi.yaml` documents both params on `GET /schedules` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference + CLI map extended. Coverage: 1 API integration case (`TestListSchedules_FilterByNextFire` — baseline / next_fire_since lower / next_fire_until upper / both-bounds window / nil-NextFireAt excluded under bounds / empty-no-op / whitespace-trim / compose with action / compose with created_at `since` / invalid 400 paths / paginated total-count); 4 CLI cases (`TestCLI_ScheduleList_ForwardsNextFireRange`, `_EmptyNextFireOmitsParam`, `_RejectsInvalidNextFireSince`, `_RejectsInvalidNextFireUntil`); 1 Playwright case (`next_fire range filter narrows the schedule list and round-trips through the URL`). |
| 5.4.62 | `finished_at` range filter on the schedule run history — extends `GET /api/v1/schedules/{id}/runs` with `?finished_since=<rfc3339>` / `?finished_until=<rfc3339>`, inclusive bounds on each run's nullable `finished_at`, mirrored through `vmsmith schedule runs <id> --finished-since --finished-until` and two `<input type="datetime-local">` "finished_since / finished_until" controls on the Schedules page recent-runs expander. Closes the post-incident triage operator query *"which runs of this hourly-snapshot schedule completed inside the 02:00–06:00 maintenance window"* / *"which long-running restart runs finished after 09:00 (rolling into business hours)"* that the existing `?since=` / `?until=` filter on `started_at` cannot answer when a run can start inside one window and finish outside it (e.g. a start-action that fans out across 200 tag-selected VMs and runs for several minutes). Reuses the same `parseTimeRangeParam` helper as the `?since=` / `?until=` filter and the same `snapshotInTimeRange` predicate so the contract matches exactly: whitespace-trimmed; empty disables the bound; invalid values return 400 `invalid_finished_since` / `invalid_finished_until`. Runs with a nil `finished_at` — typically still-running runs — are filtered OUT whenever either bound is set, mirroring the nil-handling for the schedule `?next_fire_since=` / `?next_fire_until=` filter (5.4.60). Applied right after the existing `started_at` `since` / `until` filter and before `?search=`, so it composes additively with every other run filter; `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_schedule.go::ListScheduleRuns` parses both bounds via `parseTimeRangeParam` (400 `invalid_finished_since` / `invalid_finished_until` on garbage) and applies the predicate right after the `started_at` time-range check; runs with `run.FinishedAt == nil` are excluded when either bound is set. `internal/cli/schedule.go` adds `schedule runs --finished-since` / `--finished-until` with client-side validation via the existing `parseCLITimeRange` helper. `web/src/pages/Schedules.jsx` adds two `datetime-local` "finished_since / finished_until" inputs (`data-testid="schedule-runs-finished-{since,until}-filter-<id>"`) alongside the existing VM-id, search, and status filters in the recent-runs expander, plus a "Clear finished" button. `web/src/api/client.ts` extends `schedulesApi.runs({ finishedSince, finishedUntil })`. `tests/web/mock-server.js` mirrors the time-range parse (400 path + filter + nil-finished_at exclusion). `docs/openapi.yaml` documents both params on `GET /schedules/{scheduleID}/runs` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference + CLI map extended. Coverage: 1 API integration case (`TestListScheduleRuns_FilterByFinishedAt` — baseline / finished_since lower bound / finished_until upper bound / both-bounds window / nil-finished_at excluded under bounds / empty-no-op / whitespace-trim / compose with status / compose with started_at `since` / invalid 400 paths / paginated total-count); 5 CLI cases (`TestCLI_ScheduleRuns_ForwardsFinishedRange`, `_EmptyFinishedFlagsOmitParams`, `_RejectsInvalidFinishedSince`, `_RejectsInvalidFinishedUntil`); 1 Playwright case (`finished_at range filter narrows the recent-runs expander to runs that finished inside the window`). |
| 5.4.64 | Duration range filter on the schedule run history — extends `GET /api/v1/schedules/{id}/runs` with `?min_duration_ms=<n>` / `?max_duration_ms=<n>`, an inclusive non-negative integer window on each run's `finished_at - started_at` duration in milliseconds, mirrored through `vmsmith schedule runs <id> --min-duration-ms --max-duration-ms` and two `<input type="number">` "min duration ms / max duration ms" controls on the Schedules page recent-runs expander. Closes the SRE triage operator queries *"show me every run that took ≥ 5 minutes during yesterday's window"* / *"every run that completed in under a second (probably a no-op)"* that the categorical `?status=` filter cannot answer (a 5s success and a 2h success share `status=success`) and the existing `?since=` / `?until=` / `?finished_since=` / `?finished_until=` time-range filters cannot answer either (they bound *when* a run started or finished, not *how long* it ran). The symmetric range counterpart to the `duration` sort axis added in 5.4.63 — the same axis can now be sorted *and* filtered to bound the visible cohort, exactly the way `?since=`/`?until=` complement the `started_at` sort axis. Reuses the existing `parseCountRangeParam` helper (non-negative integer; whitespace-trimmed; empty disables; non-numeric or negative returns 400 `invalid_min_duration_ms` / `invalid_max_duration_ms`) and the existing `countInRange` predicate so the contract matches the VM/template/image numeric range filter family exactly. Runs with a nil `finished_at` — still-running runs have no known duration — are filtered OUT whenever either bound is set, mirroring the `finished_since`/`finished_until` nil-handling so the filter family stays consistent. Applied right after the `finished_at` range filter and before `?search=`, so it composes additively with every other run filter; `X-Total-Count` reflects the post-filter population. | S | ✅ Done — `internal/api/handlers_schedule.go::ListScheduleRuns` parses both bounds via `parseCountRangeParam` (400 `invalid_min_duration_ms` / `invalid_max_duration_ms` on garbage or negative) and applies the predicate right after the `finished_at` time-range check via `countInRange`; runs with `run.FinishedAt == nil` are excluded when either bound is set; the duration is computed as `int(run.FinishedAt.Sub(run.StartedAt) / time.Millisecond)` clamped to `≥ 0` so out-of-order timestamps don't leak negative durations. `internal/cli/schedule.go` adds `schedule runs --min-duration-ms` / `--max-duration-ms` (Int flags, client-side rejection of negatives, using `cmd.Flags().Changed(...)` so an explicit `--min-duration-ms=0` is forwarded while the unset default is not). `web/src/pages/Schedules.jsx` adds two `<input type="number" min="0" step="1">` "Min duration ms / Max duration ms" inputs (`data-testid="schedule-runs-{min,max}-duration-ms-filter-<id>"`) alongside the existing finished-at inputs in the recent-runs expander, plus a "Clear duration" button; client-side `parsePositiveInt` mirrors the API contract so blank / negative / non-integer values fall through to "filter disabled" rather than firing a 400. `web/src/api/client.ts` extends `schedulesApi.runs({ minDurationMs, maxDurationMs })`. `tests/web/mock-server.js` mirrors the integer parse (400 path + filter + nil-finished_at exclusion via `Date.parse(finished) - Date.parse(started)`). `docs/openapi.yaml` documents both params on `GET /schedules/{scheduleID}/runs` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference + CLI map extended. Coverage: 1 API integration case (`TestListScheduleRuns_FilterByDuration` — baseline / min lower bound / max upper bound / both-bounds window / inclusive boundary / wide-open excludes still-running / empty-no-op / whitespace-trim / compose with status / compose with `since` / invalid 400 paths incl. negative / paginated total-count); 4 CLI cases (`TestCLI_ScheduleRuns_ForwardsDurationRange`, `_DurationRangeZeroExplicit`, `_DurationRangeUnsetOmitsParams`, `_RejectsNegativeDuration`); 1 Playwright case (`duration range filter narrows recent runs by finished_at - started_at duration` exercises min-lower-bound / both-bounds collapse-to-single / clear / max-upper-bound). |
| 5.4.63 | Duration sort axis on the schedule run history — extends `GET /api/v1/schedules/{id}/runs` `?sort=` whitelist with a fifth axis, `duration`, computed as `finished_at - started_at`, mirrored through `vmsmith schedule runs <id> --sort duration` and a new "Duration" option in the recent-runs expander sort dropdown. Closes the SRE-triage operator queries *"show me the slowest runs of this schedule"* / *"which runs took unusably long during yesterday's maintenance window"* / *"order all runs by fastest first"* that the existing `?status=` / `?vm_id=` / `?since=` / `?until=` / `?finished_since=` / `?finished_until=` / `?search=` filters can narrow but not order, and that the existing `?sort=` axes (`id`, `started_at`, `finished_at`, `status`) cannot answer at all — `finished_at` orders by *when* a run completed, not *how long it ran*, so a 30s run that finished at 02:00:30 and a 2h run that finished at 02:00:30 sort identically. The duration axis applies the same nil-trailing semantics as `finished_at`: runs with a nil `finished_at` (still-running) have no known duration and sink to the tail in ascending order / lead in descending order, mirroring the `compareNextFire` nil-trailing convention. All comparators tiebreak on `id` so paginated requests are deterministic. Unknown sort values continue to return 400 `invalid_sort` (and the rejection-test sentinel value moves from `duration` to `memory` since the former is now valid). | S | ✅ Done — `pkg/types/schedule_run_sort.go` adds the `ScheduleRunSortDuration` constant, the `compareRunDuration` helper (nil-trailing), and a new case in `SortScheduleRuns`. `internal/api/handlers_schedule.go::ListScheduleRuns` extends the 400 `invalid_sort` message to list `duration` (validation flows through `IsValidScheduleRunSort`). `internal/cli/schedule.go` extends the `--sort` flag help + client-side validation error to mention `duration`. `web/src/pages/Schedules.jsx` adds the `<option value="duration">Duration</option>` to the recent-runs sort dropdown. `web/src/api/client.ts` extends `schedulesApi.runs({ sort })`. `tests/web/mock-server.js` extends the sort-field whitelist + the `cmp` switch (`Date.parse(finished) - Date.parse(started)`, nil-trailing) + 400 message. `docs/openapi.yaml` extends the `ScheduleRunSort` enum (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference + CLI map extended. Coverage: 3 unit cases in `pkg/types/schedule_run_sort_test.go` (`TestSortScheduleRuns_ByDuration_AscNilTrailing`, `_ByDuration_DescNilLeading`, `_ByDuration_TiebreaksOnID`) plus the `TestIsValidScheduleRunSort` whitelist update; 1 API integration case in `internal/api/handlers_schedule_test.go` (`TestListScheduleRuns_SortByDuration` — duration asc / duration desc / whitespace-trim + case-insensitive / sort composes with status / pagination preserves post-filter total / invalid_sort sentinel updated to "memory") plus the existing `TestListScheduleRuns_Sort` rejection sentinel update; 1 CLI case (`TestCLI_ScheduleRuns_ForwardsDurationSort` covers whitespace-trim + lowercase forwarding) plus the existing `_RejectsInvalidSort` sentinel update; 1 Playwright case (`duration sort axis orders the recent-runs expander by finish-minus-start` exercises asc / desc / reset). |

### 5.5 Multi-Host Management (Future Vision)

Manage VMs across multiple physical hosts from a single VMSmith instance.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 5.5.1 | Design multi-host architecture: central coordinator + per-host agents, or remote libvirt URIs | XL | Architecture decision needed |
| 5.5.2 | Add `hosts` config section with libvirt URI per host | M | |
| 5.5.3 | Add host selection to VM create (`--host <name>`) | L | |
| 5.5.4 | Add host overview dashboard showing per-host resource usage | L | |

### 5.6 Windows Guest Support

Run Windows guests (workstation and server, "2020 version and up": Windows 10/11
and Windows Server 2019/2022/2025) alongside Linux, sharing the same overlay /
NAT / DHCP / snapshot / lifecycle machinery. The only divergence is the guest
OS family, which drives the libvirt domain tuning and the first-boot
provisioning datasource.

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 5.6.1 | Add an `os_type` (`linux`\|`windows`) guest-family selector to `VMSpec`, plus optional `os_variant` (windows-10/11, windows-server-2019/2022/2025) and write-only `admin_password` | S | ✅ Done — `pkg/types/vm.go` adds `OSType`/`OSVariant`/`AdminPassword` with `ResolvedOSType()` (empty ⇒ linux) + `IsWindows()` helpers and `KnownWindowsVariants`. `validateVMSpec` adds `invalid_os_type` / `invalid_os_variant` (both 400) and a Windows resource floor (≥2048 MB RAM, ≥32 GB disk, checked only when set). CLI: `vmsmith vm create --os --os-variant --admin-password`. Coverage: `pkg/types` (`ResolvedOSType`/`IsWindows`/`IsKnownWindowsVariant`), API (`TestCreateVM_Windows`, `_InvalidOSType`, `_WindowsResourceFloor`, `TestValidateVMSpec` windows cases), CLI (`TestCLI_VMCreate_Windows`) |
| 5.6.2 | Windows-tuned libvirt domain XML: SATA system disk (boots without virtio storage drivers), e1000e NIC (native Windows driver), `localtime` clock, Hyper-V enlightenments + `hypervclock`, USB tablet, QXL video; Linux path unchanged | M | ✅ Done — `internal/vm/domain.go` `DomainParamsFromSpec` sets the Windows device profile from `spec.IsWindows()`; `GenerateDomainXML` normalises empty device fields to the historical Linux defaults so direct callers/tests are unaffected. Coverage: `TestDomainParamsFromSpec_WindowsTuning`, `_LinuxDefaultsUnchanged`, `TestGenerateDomainXML_Windows`, `_NoVirtioWinWhenUnset` |
| 5.6.3 | Provision Windows via a cloudbase-init NoCloud datasource (instead of cloud-init): `cidata`-labelled ISO with `meta-data` (instance-id/hostname/admin_pass) + a `#ps1_sysnative` PowerShell `user-data` that sets the Administrator password, enables RDP, and installs/enables OpenSSH with the injected key; `admin_password` redacted from the stored record after the ISO is written | M | ✅ Done — `internal/vm/lifecycle.go` `createProvisioningISO` dispatches on OS family; `createWindowsProvisioningISO` + `buildWindowsUserData` generate the datasource; Create/Clone/Update reuse it. DHCP reservation / IP monitor are OS-agnostic. Coverage: `TestBuildWindowsUserData_*`, `TestPSSingleQuote` |
| 5.6.4 | Attach the virtio-win driver ISO as an extra cdrom for Windows guests, via `storage.virtio_win_iso` (auto-probes `/usr/share/virtio-win/virtio-win.iso`) | S | ✅ Done — `config.StorageConfig.VirtioWinISO` + `DefaultVirtioWinISOPath`; `LibvirtManager.virtioWinISOPath`/`applyVirtioWin` attach it on the `sdc` cdrom when present. Documented in `docs/WINDOWS_GUESTS.md` and `vmsmith.yaml.example` |
| 5.6.5 | Operator guide: preparing Windows base images (sysprep + cloudbase-init + virtio), driver injection, RDP/WinRM access | S | ✅ Done — `docs/WINDOWS_GUESTS.md` |

**Status:** the core create/provision/boot path (5.6.1–5.6.5) is shipped; the
items below are the follow-up backlog to make Windows a fully first-class
citizen across the surface area (GUI, templates, filtering, console, metrics)
and to support the harder guest scenarios (Windows 11's UEFI+TPM requirement,
unattended installs from a raw ISO, and a fully paravirtual guest).

#### Follow-up tasks

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 5.6.6 | **GUI surface** — OS-family selector + Windows variant / Administrator-password fields in the Create Machine modal (with the Windows RAM/disk floors enforced client-side), an OS badge (Linux/Windows) on VM rows and VMDetail, and a "Connect via RDP" hint when a 3389 port-forward exists | M | ✅ Done — the Create Machine modal now exposes `os_type`, Windows `os_variant`, and optional Administrator-password fields with Windows-specific RAM/disk floors enforced client-side; VM rows and VMDetail show the OS family surface; and VMDetail renders an RDP connection hint when a TCP 3389 port-forward exists. Coverage includes Playwright cases for the Windows create form, generated-password reveal, VMDetail OS/RDP rendering, plus the corresponding frontend wiring in `web/src/pages/VMList.jsx` and `web/src/pages/VMDetail.jsx`. |
| 5.6.7 | **Templates carry OS family** — add `os_type` / `os_variant` to `VMTemplate` + `CreateTemplateRequest`, merge them in `mergeVMSpecWithTemplate`, and expose them in the template CLI/GUI so a "Windows Server 2022" template provisions Windows VMs directly | S | ✅ Done — `pkg/types/template.go` adds `OSType` / `OSVariant` + `ResolvedOSType()` / `IsWindows()` (empty resolves to `linux`, mirroring `VMSpec.ResolvedOSType`); `internal/api/handlers_template.go` extends `createTemplateRequest` + `toTemplate()` with lowercased/trimmed `os_type` / `os_variant`; `internal/api/handlers_vm.go::mergeVMSpecWithTemplate` propagates both fields when the derived VMSpec leaves them empty (explicit per-VM `os_type` still overrides — `TestCreateVM_TemplateInheritsOSType` + `TestCreateVM_TemplateOSTypeOverride`). `validateTemplateRequest` reuses `validateOSType` so `POST /templates` returns 400 `invalid_os_type` / `invalid_os_variant` on the same alphabet as `POST /vms` (and mixed-case `Windows` / `Windows-Server-2022` is accepted + lowercased on store — `TestCreateTemplateRejectsInvalidOSType`, `_RejectsInvalidOSVariant`, `_AcceptsMixedCaseOSType`). `docs/openapi.yaml` extends `VMTemplate` + `CreateTemplateRequest` schemas with `os_type` / `os_variant`; regenerated `schema.d.ts`. The GUI surface is filter-only (see 5.6.8); the create-template modal stays Linux-only as a follow-up (5.6.6 covers the create-VM modal). |
| 5.6.8 | **`?os_type=` list filter** — add a case-insensitive exact-match `os_type` filter to `GET /vms` (empty `spec.os_type` treated as `linux`, mirroring the `?default_user=root` empty-means-root semantics) and `GET /templates`, plus the `--os-type` CLI flag and GUI facet | S | ✅ Done — `internal/api/parse_os_type.go` adds a shared `parseOSTypeFilter` helper (case-insensitive `linux`/`windows`, whitespace-trimmed, empty disables, garbage returns 400 `invalid_os_type` mirroring `validateOSType`). `internal/api/handlers_vm.go::ListVMs` and `internal/api/handlers_template.go::ListTemplates` wire `?os_type=` through the helper and filter via `Spec.ResolvedOSType()` / `tpl.ResolvedOSType()` so empty stored values match `?os_type=linux`. `internal/cli/parse_os_type.go` mirrors the contract for `vmsmith vm list --os-type` / `vmsmith template list --os-type`. `web/src/api/client.ts` extends `vms.list({ osType })` / `templates.list({ osType })`; `web/src/pages/VMList.jsx` + `web/src/pages/TemplateList.jsx` add a 3-option `<select>` (All OSes / Linux / Windows) with URL round-trip via `?os_type=`. `tests/web/mock-server.js` mirrors the filter (400 path + empty-means-linux) and seeds a Windows `win-app` VM + a `windows-2022` template so the filter narrows meaningfully (Linux × 2 vs Windows × 1 on both lists). `docs/openapi.yaml` adds a shared `OSTypeFilter` parameter referenced from `/vms` and `/templates` (regenerated `schema.d.ts`). `CLAUDE.md` REST quick reference + CLI maps extended. Coverage: 3 unit cases (`TestVMTemplateResolvedOSType`, `_IsWindows`, mixed-case variants); 16 API integration cases (`TestListVMs_FilterByOSType_*` × 8 covering exact / case-insensitive / whitespace / empty-no-op / linux-matches-empty / invalid-400 / compose-with-status / total-count, plus the mirrored `TestListTemplates_FilterByOSType_*` × 8); 11 CLI cases (`TestCLI_VMList_FilterByOSType_*` × 6 + `TestCLI_TemplateList_FilterByOSType_*` × 5); 4 Playwright cases (os-type filter narrows/round-trips and linux-matches-empty for both VMList and TemplateList). |
| 5.6.9 | **UEFI + Secure Boot + virtual TPM 2.0** — required to boot Windows 11. Add an OVMF firmware path (`<os firmware='efi'>` / explicit `<loader>`+`<nvram>`) and a `swtpm` `<tpm model='tpm-crd'>` device, gated on `os_variant` (windows-11 ⇒ on by default) or an explicit `secure_boot` / `tpm` VMSpec flag. Probe for `swtpm` + OVMF and surface a clear error when missing | L | Without this Windows 11 Setup refuses to install; Windows 10 / Server are fine on SeaBIOS |
| 5.6.10 | **QEMU guest agent (qemu-ga) for Windows** — document/automate installing `qemu-ga` from the virtio-win ISO so VMSmith can report the real in-guest IP, do guest-initiated graceful shutdown, and surface memory-balloon metrics (the metrics sampler already depends on the agent for RAM pressure). Today Windows reports IP only via the DHCP-lease ping path | M | The `<channel ...org.qemu.guest_agent.0>` is already in the domain XML; this is guest-side enablement + docs |
| 5.6.11 | **Unattended install from a raw Windows ISO** — for operators without a prepared cloudbase-init image: boot the install ISO + generate an `Autounattend.xml` provisioning ISO (disk partitioning, edition select, locale, admin password, enable RDP/WinRM, optional cloudbase-init bootstrap). Needs an `--install-iso` create path and a boot-order/empty-disk flow distinct from the overlay-from-base-image model | XL | Complements 5.6.3 (which assumes a prepared base image); this is the "bring your own ISO" path |
| 5.6.12 | **Switch-to-virtio helper** — once virtio-win drivers are installed in-guest, flip the system disk bus to virtio and the NIC to virtio-net for throughput. Add a `vmsmith vm set-virtio <id>` (or a `disk_bus`/`nic_model` override in VMUpdateSpec) that rewrites the domain XML and validates the guest can still boot | M | ✅ Done — `DiskBus` / `NICModel` added to `VMUpdateSpec` as `*string` with pointer semantics (nil = no change, pointer-to-`""` = clear the override and fall back to the OS-family default at next render). Validated at the API boundary via shared `validateDiskBus` / `validateNICModel` helpers (re-used by the create-path validator from 5.6.15) so typos return 400 `invalid_disk_bus` / `invalid_nic_model` instead of silently falling back. `LibvirtManager.Update` extends the existing redefine block — change triggers a domain XML re-render with the new bus / model, preserves the existing UUID, and restarts the VM if it was running. CLI: `vmsmith vm edit <id> --disk-bus <virtio\|sata\|""> --nic-model <virtio\|e1000e\|"">` plus the dedicated `vmsmith vm set-virtio <id>` shortcut that PATCHes both to virtio at once. GUI: a "Device Tuning" subsection in the existing Edit VM modal exposes both selectors and a "Switch to virtio →" shortcut that pre-fills the pair atomically. OpenAPI + generated TypeScript schema + mock-server validation + Playwright coverage all updated. Pairs with 5.6.15 per-VM device overrides at create time — together they cover the full lifecycle of bus / model selection. |
| 5.6.13 | **RDP console in the GUI** — extend the console-ticket/proxy subsystem with an RDP path (e.g. an embedded HTML5 RDP client or a guacd bridge) so Windows guests get a browser console comparable to the VNC serial/graphics path | L | The VNC graphical console already works (QXL+tablet); this adds native RDP |
| 5.6.14 | **Windows image catalog / prep helper** — a `vmsmith image prepare-windows` helper (or docs + script) that downloads a Windows eval ISO/qcow2, injects virtio drivers, installs cloudbase-init, syspreps, and registers the result as a base image | M | Mirrors the Linux GenericCloud download guidance; reduces the "prepare a base image" barrier |
| 5.6.15 | **Per-VM device overrides** — optional `virtio_win_iso`, `disk_bus`, `nic_model`, `machine`, and `firmware` overrides on `VMSpec` so advanced operators can tune individual Windows VMs without changing global config | S | ✅ Done — `pkg/types/vm.go` adds five optional fields (`DiskBus`, `NICModel`, `Machine`, `Firmware`, `VirtioWinISO`) with `Resolved*` helpers: `ResolvedDiskBus()` returns the OS-family default unless overridden (`virtio` for Linux / `sata` for Windows); `ResolvedNICModel()` mirrors the same fallback (`virtio` / `e1000e`); `ResolvedFirmwareAttr()` maps `uefi`/`ovmf` to libvirt's `firmware='efi'` shorthand and `bios`/empty to no attribute; `IsValidMachineType()` enforces a conservative `[A-Za-z0-9._-]+` alphabet so shell metacharacters can't reach the domain template. `internal/vm/domain.go::DomainParamsFromSpec` applies the four overrides after the OS-family tuning: NIC model is computed once and threaded through every interface XML (primary NAT + extras), disk-bus override flips the target letter (`vda`/`sda`) and bumps the cloud-init cdrom to `sdb` on SATA / back to `sda` on virtio, machine type override replaces the `pc-q35-6.2` default, and firmware override emits `<os firmware='efi'>` (added via a conditional template attribute) for `uefi`/`ovmf`. `internal/vm/lifecycle.go::virtioWinISOPathForSpec` checks the per-VM override first (with fallback to daemon config when the override path is missing on the host), called from `applyVirtioWin` on Create/Clone/Update. `internal/api/validation.go::validateDeviceOverrides` rejects unknown values up front with stable 400 codes (`invalid_disk_bus`, `invalid_nic_model`, `invalid_machine`, `invalid_firmware`); virtio_win_iso is treated as a free-form host path (validated only for NUL bytes — the LibvirtManager logs a warning + falls back if the file is missing at create time). CLI: `vmsmith vm create --disk-bus --nic-model --machine --firmware --virtio-win-iso` (all lowercased + trimmed before forwarding). OpenAPI: five new properties on `VMSpec` with enum constraints (regenerated `schema.d.ts`). GUI: a new "Device Tuning" subsection in the Advanced tab of the Create-VM modal — 4 selects (disk_bus / nic_model / firmware) + machine + virtio_win_iso path inputs, all keys stripped from the request body when left empty so the daemon resolves the OS-family default. Mock server mirrors the validation contract so Playwright assertions exercise the 400 paths. Coverage: 16 unit cases (`TestVMSpecResolvedDiskBus` × 6, `TestVMSpecResolvedNICModel` × 5, `TestVMSpecResolvedFirmwareAttr` × 6, `TestIsValidMachineType` × 12), 10 domain-layer cases (`TestDomainParamsFromSpec_DeviceOverrides` × 9 spanning linux/windows defaults, virtio-on-windows post-driver, UEFI shorthand, OVMF alias, explicit-bios attribute-less, custom machine, NIC override, mixed-case lowercase, plus a dedicated `TestDomainParamsFromSpec_NICModelOverrideAppliedToEveryInterface` regression), 5 API integration cases (`TestCreateVM_DeviceOverrides_Accepted`, `_EmptyResolvesToDefaults`, `TestCreateVM_InvalidDiskBus`, `_InvalidNICModel`, `_InvalidFirmware`, `_InvalidMachine`), 1 CLI case (`TestCLI_VMCreate_DeviceOverrides`), 1 Playwright case (`device tuning overrides round-trip through the Advanced tab` — fills all five inputs and re-fetches the VM to confirm every field persisted on `spec`). |
| 5.6.16 | **Windows E2E test tier** — add a `tests/e2e` scenario that provisions a prepared Windows eval image, waits for the DHCP IP, and verifies RDP (3389) reachability and optional SSH login; gate it behind a `--windows-image` opt-in like the existing `--rocky-image` flag | M | Unit coverage is in place (domain XML, user-data); this validates a real boot |
| 5.6.17 | **`admin_password` lifecycle hardening** — optionally generate a strong random Administrator password when none is supplied and surface it exactly once in the create response/CLI output (never re-readable), and document a rotation path via cloudbase-init re-run on update | S | ✅ Done — `internal/vm/admin_password.go` adds a crypto/rand-backed 20-char generator that always satisfies the Windows complexity policy (one upper / lower / digit / symbol from a shell-safe alphabet, Fisher-Yates shuffled). `LibvirtManager.Create` and `MockManager.Create` both call it when `spec.IsWindows() && spec.AdminPassword == ""`, inject the password into the provisioning ISO, then redact `spec.AdminPassword` from the stored record and set the transient `vm.GeneratedAdminPassword` field on the response copy (set *after* `store.PutVM` so it never lands in bbolt). `types.VM.GeneratedAdminPassword` is `omitempty`-tagged so Get/List never carry it. CLI: `vmsmith vm create` prints a one-time banner with the password and a "save it now" warning. GUI: `VMList.jsx` captures the create response and, when `generated_admin_password` is set, opens `GeneratedAdminPasswordModal` — a one-time-reveal dialog with the password, a copy-to-clipboard button, and a dismiss-only path (the value disappears as soon as the modal closes; refreshing the page or reopening the VM never shows it again). OpenAPI documents the field on the `VM` schema as create-response-only (regenerated `schema.d.ts`). Mock server mirrors the auto-generate + transient-on-response contract so Playwright assertions see a realistic value. Rotation path documented in `docs/WINDOWS_GUESTS.md` — recreate the VM with an explicit `admin_password` (or generated again by omitting it). Coverage: 5 unit cases (`TestGenerateAdminPassword_Length`, `_SatisfiesComplexity` over 50 iters, `_Uniqueness` over 100 iters, `TestGenerateAdminPasswordN_RejectsTooShort`, `_MinLengthOK`), 3 API integration cases (`TestCreateVM_GeneratesAdminPasswordForWindowsWhenOmitted` verifies the response carries the password AND a follow-up GET returns nothing, `TestCreateVM_DoesNotGenerateAdminPasswordWhenSupplied`, `TestCreateVM_DoesNotGenerateAdminPasswordForLinux`), 3 CLI cases (`TestCLI_VMCreate_WindowsGeneratesAdminPassword`, `_LinuxDoesNotShowPasswordBanner`, `_WindowsWithExplicitPasswordDoesNotShowBanner`), 1 Playwright case (`creating a Windows VM without admin_password surfaces a one-time reveal` — covers modal visibility, password length, copy-button "Copied" feedback, and post-dismiss disappearance). |
| 5.6.18 | **`os_type` immutability + clock/timezone controls** — reject changing `os_type` on `PATCH /vms/{id}` (device profile is baked at create), and add an optional per-VM clock/timezone control so Windows `localtime` guests dual-booting or syncing with Linux peers don't drift | S | ✅ Done — `pkg/types/vm.go` adds `OSType` / `OSVariant` as `*string` rejection sentinels on `VMUpdateSpec` (any value, including `""`, returns 400 `os_type_immutable`) and a new `ClockOffset` field on `VMSpec` (string) plus `VMUpdateSpec` (`*string`, pointer semantics — nil = no change, pointer-to-`""` clears the override and falls back to the OS-family default at next render). `VMSpec.ResolvedClockOffset()` is the single source of truth (explicit value wins; otherwise `localtime` for Windows / `utc` for everything else). `internal/vm/domain.go::DomainParamsFromSpec` applies `ResolvedClockOffset()` last so an operator override beats the OS-family default in both directions. `internal/vm/lifecycle.go::Update` folds clock-offset changes into the existing CPU/RAM domain-redefine path. `internal/api/validation.go::validateVMUpdateSpec` rejects `OSType` / `OSVariant` before any other check; `validateClockOffset` rejects anything other than empty / `utc` / `localtime` with 400 `invalid_clock_offset` on both create and update. CLI: `vm create --clock-offset utc\|localtime` and `vm edit <id> --clock-offset utc\|localtime\|""`. OpenAPI: `VMSpec.clock_offset` and `VMUpdateSpec.clock_offset` documented with the `utc`/`localtime` enum + the immutability contract on `os_type` / `os_variant`. Coverage: 11-case `TestVMSpecResolvedClockOffset` unit table, 5-case `TestDomainParamsFromSpec_ClockOffsetOverride`, 9 new API integration cases (`TestUpdateVM_OSTypeRejected`, `TestUpdateVM_OSVariantRejected`, `TestUpdateVM_OSFieldOmittedIsOK`, `TestUpdateVM_ClockOffsetChange`, `TestUpdateVM_ClockOffsetClear`, `TestUpdateVM_ClockOffsetInvalid`, `TestUpdateVM_ClockOffsetMixedCase`, `TestCreateVM_ClockOffsetInvalid`, `TestCreateVM_ClockOffsetAccepted`), 2 CLI cases (`TestCLI_VMEdit_ClockOffset`, `TestCLI_VMCreate_ClockOffset`). |

**Open questions / design notes:**

1. **Windows 11 gating (5.6.9).** Should `os_variant: windows-11` *imply* UEFI+SecureBoot+TPM automatically (and hard-fail if `swtpm`/OVMF are absent), or stay opt-in via explicit flags? Recommendation: imply-on for `windows-11` with a clear preflight error, opt-in for everything else.
2. **Provisioning model (5.6.3 vs 5.6.11).** The shipped path assumes a *prepared* cloudbase-init base image (the Windows analogue of a Linux cloud image). The raw-ISO unattended path is a fundamentally different create flow (empty disk + install ISO + boot order) and should be a sibling code path, not a branch inside the overlay flow.
3. **Licensing.** VMSmith does not inject product keys or manage activation/KMS. Document that the base image's licensing/activation is the operator's responsibility; optionally allow a `product_key` passthrough into the cloudbase-init datasource (5.6.15-adjacent).
4. **Guest-agent dependency (5.6.10).** Windows IP discovery currently relies on the DHCP-lease ping path (works, but slower and can't see additional NICs). qemu-ga would make IP/shutdown/metrics first-class — same trade-off the Linux side documents in §4.1.

---

## Phase 6: Developer & Community (Ongoing)

### 6.1 Developer Experience

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 6.1.1 | Add `make dev` target that runs both `dev-api` and `dev-web` in parallel (e.g., via `goreman` or `concurrently`) | S | ✅ Done — `make dev` now launches both targets in parallel and cleans up both child processes on Ctrl-C |
| 6.1.2 | Add pre-commit hook: `make fmt && make lint` | S | ✅ Done — versioned `.githooks/pre-commit` hook added with `make install-githooks` helper and contributor docs |
| 6.1.3 | Add `CONTRIBUTING.md` with setup instructions, PR conventions, test requirements | S | ✅ Done — `CONTRIBUTING.md` added with setup, workflow, testing, and PR guidance |
| 6.1.4 | Add `.editorconfig` for consistent formatting across editors | S | ✅ Done — root `.editorconfig` defines Go/Makefile tab rules and 2-space defaults for docs/web assets |

### 6.2 Packaging & Distribution

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 6.2.1 | Create DEB package build (for Ubuntu/Debian) | M | ✅ Done — `make deb` now stages the release binary, `/etc/vmsmith/config.yaml`, and `vmsmith.service` into `bin/packages/*.deb` via `scripts/build-deb.sh` |
| 6.2.2 | Create RPM package build (for Rocky/RHEL/Fedora) | M | ✅ Done — added `make rpm` + `scripts/build-rpm.sh` to produce an RPM that packages the linux/amd64 release binary, systemd unit, and default config for Rocky/RHEL/Fedora installs |
| 6.2.3 | Create container image for VMSmith daemon (requires privileged mode for libvirt) | M | ✅ Done — added multi-stage `Dockerfile`, `scripts/docker-entrypoint.sh`, `.dockerignore`, and `docs/CONTAINER.md` for privileged local/lab usage |
| 6.2.4 | Add installation script (`curl -sSL https://... \| sh`) | S | ✅ Done — added `scripts/install.sh` to download the published Linux amd64 release asset and install it to `/usr/local/bin/vmsmith`, plus README/production docs and a local smoke test |
| 6.2.5 | Build identification: `pkg/version` + linker-set vars, `GET /api/version` (public, no auth), `vmsmith version` / `--version`, GUI footer | S | ✅ Done — `Makefile` now wires `-X $(VERSION_PKG).Version=...` plus `Commit` and `BuildDate`, the daemon serves `/api/version` outside `/api/v1` so health checks and the GUI footer can read it without an API key, the CLI ships `vmsmith version [--json]`, and `web/src/components/Layout.jsx` renders the live build label with commit/date in a tooltip. Covered by `pkg/version`, `internal/api`, `internal/cli`, mock-server, and Playwright tests |

### 6.3 Documentation Expansion

| # | Task | Effort | Notes |
|---|------|--------|-------|
| 6.3.1 | Write production deployment guide (systemd, TLS, reverse proxy, firewall rules) | M | ✅ Done — `docs/PRODUCTION_DEPLOYMENT.md` covers systemd, TLS via reverse proxy, firewall rules, logging, backups, and upgrade guidance |
| 6.3.2 | Write networking deep-dive (NAT vs macvtap vs bridge, when to use each, troubleshooting) | M | ✅ Done — added `docs/NETWORKING.md` covering mode selection, tradeoffs, examples, and troubleshooting |
| 6.3.3 | Add example automation scripts (bash/python) for common workflows | S | ✅ Done — added `examples/` with bash and Python API automation examples for common create/wait/port-forward flows |
| 6.3.4 | Create short video/GIF demos for README | S | |

---

## Summary: Suggested Next Priority Order

With the initial platform hardening work mostly done, the next highest-value roadmap items are:

| Priority | Area | Key Tasks | Why |
|----------|------|-----------|-----|
| **P0** | VM Resource Metrics | 4.1.10 | REST stats, SSE streaming, Prometheus export, dashboard rollups, CLI support, and live VMDetail charting are shipped; the main remaining work is the deeper unit/integration/E2E coverage in 4.1.10, including counter-reset edge cases and a real-VM stress path |
| **P1** | Events | 4.2.17 | Core event API, SSE transport, connection observability, Activity views, webhook delivery, and the Settings/test-delivery UX are shipped; the biggest remaining work is the last integration/E2E coverage pass plus the libvirt-state cleanup called out in 4.2.4 |
| **P1** | VM Cloning | 2.1.2 | Main clone flows plus integration/E2E coverage ship today; the last notable cloning gap is the real libvirt-backed implementation |
| **P1** | OpenAPI Tooling | 4.3.1 – 4.3.3 | Spec, Swagger UI, and typed frontend client are in place; remaining work is maintenance and follow-on SDK ergonomics rather than first delivery |
| **P2** | Console Access | 5.1.4 – 5.1.11 | Ticket issuance, endpoint discovery, config defaults, auth/TTL/log-redaction checklist items, and operator/docs guidance are already shipped; the remaining work is the websocket proxy, active-session lifecycle, browser console UI, and password/serial follow-ons |
| **P2** | Scheduled Operations | 5.2.1 – 5.2.6 | Useful automation once observability and lifecycle features are in place |
| **P3** | OVA Import/Export | 5.3.1 – 5.3.3 | Helpful interoperability feature, but less urgent than core ops gaps |
| **P3** | Multi-Host Management | 5.5.1 – 5.5.4 | Still a long-term architecture track rather than near-term delivery |

---

## Notes

- Effort estimates assume familiarity with the codebase. First-time contributors should add ~50% buffer.
- Phases are not strictly sequential — items from Phase 2 can begin as soon as Phase 1 CI is in place.
- Each task should be a single PR where possible. Larger tasks (L/XL) may need multiple PRs.
- All new features should include: API endpoint, CLI command, frontend UI, tests, and CLAUDE.md update.
